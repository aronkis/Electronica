> Evidence ledger, moved verbatim from `two_jup/HANDOFF_20260815.md` on 2026-09-09. Relative paths inside refer to the tree at tag `archive/pre-cleanup-2026-09-09`.

# HANDOFF 2026-08-15 — overnight autonomy (operator away until 07:00 EST)

Directive: "Continue work without authorization and use hardware as needed.
Solve the remaining PER issues." Goal: delivered PER <1% both directions,
ARQ off.

## GOAL STATUS — final acceptance matrix (02:00–02:03, end-state stack)

| direction | runs | PER | CP95-UB | verdict vs <1% |
|---|---|---|---|---|
| forward 146→148 | 8.23 / 8.32 / 8.27% | pooled ~8.27% | ~8.5% | **NOT MET** — the singles class stands, mechanism re-aimed (below) |
| reverse 148→146 | 1.43 / 1.49 / 2.25% | pooled 1.72% | 1.78% | **NOT MET but transformed** — was 9.12% on the banked run; the gap is now scattered small holes |

End-state stack: 146 = TMR 433fd8dab393; 148 = **skid3 v3 witness-wire
6c06ecb7e888** (datapath production-equivalent to e49c011b — validated: PER
identical — plus the live marker-free gap witness at 0x9D300008);
e49c011b rollback banked on-board; daemons + watchdogs verified up;
rx_drain_budget=4 deployed on both boards via the shipped default.

## SHIPPED (committed + pushed)

1. **`bf2398a` — rx_drain_budget DEFAULT 4.** The causally-proven wedge fix
   had been env-gated only and NEVER reached production bring-up; both first
   baseline attempts wedged on exactly the old class. Reverse went **9.12% →
   1.2–2.3%** with it. Single largest win of the night.
2. **v3 witness-wire image on 148** (`6c06ecb7e888`, `a3db317`): transparent
   datapath + marker-free gap witness. Health gate passed on pass 1; forward
   PER bit-consistent with e49c011b across 4 runs.
3. RXCYC_A opt-in in bringup (cyclic on 148 now runtime-selectable).
4. skidfix/ab_score.py (ledger-consistent per-run scorer), sim_byte_dip.cpp
   (ready-dip replay harness), tb_dma_contract.v (DMA-contract testbench).

## MEASURED AND REFUTED tonight (each with data, all banked in SKID_BUILD.md)

- **v1 naive skid**: silicon deadlock (wcnt=0) — reproduced in the contract
  TB (frozen tuser=0 beat kills SYNC observation).
- **v2 guard-preserving skid**: +5.6 pp forward harm, replicated
  (13.78/13.93%); witness blind because **-M production mode carries NEITHER
  frame marker** (tlast_en=0 gates TLAST; TUSER never fires) — measured via
  6689-sample witness trace with a single transition.
- **Brief DMA backpressure as the forward generator**: EXONERATED — ready-dip
  replay (26 dips at the exact 8-frame cadence, 1–3 word-times) is
  bit-identical to clean; the DUT's 64-deep ByteRxFifo absorbs, as designed.
  The whole skid/backpressure fix class is dead.
- **146 RX tracking cals as the reverse tick source**: refuted by freeze +
  3-way bisect (no config beats baseline; 633-flagged counts flat in all).
- **0x184 CFO poke**: inert on 433fd8da (canary regs, not loop-gain).
- **TX power / RX gain / RXM**: no headroom (TX at 0 dB atten; AGC ch0;
  RXM=16 already default).
- **Cyclic RX on air**: engages but the reader wedges in 12 s (validated only
  on loopback) — a gap, not a lever.

## SURVIVING FORWARD HYPOTHESIS (the ~8.3%, 8-comb singles class)

Triangulation: CP1 (corruption INSIDE the DUT, checksum-matched) + replay
CLEAN on corrupt-frame IQ + tonight's dip immunity + comb invariance vs -M
⇒ the generator is an **implementation-level effect in 148's silicon byte
plane that the RTL netlist cannot reproduce** — same altitude as the
wit-build rail-rehosting forensic (synthesis CE/rail restructuring was the
only implemented-level delta there, with deterministic silicon divergence).
Next discriminators, in order:
1. dcp_rail_dump glitch-path analysis on **e49c011b/skid3's own routed DCP**
   (census exists; needed: the CE-LUT → BUFGCE glitch-window timing of the
   byte-plane enable network).
2. On-die ILA on ByteSerializer→breakout→DMA (needs a debug-core build).
3. The v3 witness with frame-structural gaps excluded (current excess:
   ~500/s multigaps above the 1245/s structural rate — quantify against the
   ~57/s corrupt-frame rate; a refined threshold or histogram witness is a
   1-line RTL change on the v3 lineage).
4. NEW observation: 148-side mid-capture delivery wedge (01:37, one event,
   budget=4 active) — a different flavor than the budget-fixed 146 class;
   watch for recurrence.

## REVERSE RESIDUAL (~1.4–2.3%, scattered k≤2 holes, ~60 biterr/event)

Signal-domain doubles, no 8-comb, no big bursts. The ~1.79 s super-period
matches 148's loopback class-B beat (header-sparing scrambles + the byte0
bit6 magic flip) ⇒ prime suspect: 148-TX-side digital generator. Run-to-run
spread (1.43→2.25%) suggests an additional slow environmental term. Next:
class-B unification analysis on banked loopback captures (offline), then a
148-TX-path discriminator.

## Process notes
- Flash chains now standard: two-pass health gate (first pass after reboot
  is routinely degraded ~400/1245 and recovers on one re-bring-up) +
  witness-read-BEFORE-rollback.
- All overnight A/Bs scored with skidfix/ab_score.py (ledger-consistent).
- Fabricated-notification discipline maintained: every verdict in this file
  traces to an on-disk log named inline.

## Morning queue (proposed)
1. Decide forward path: DCP glitch-path forensic vs ILA build (item 1 vs 2
   above). 2. Reverse class-B unification analysis (offline, cheap).
3. Cyclic-reader air debug if cyclic is still wanted as a boundary-killer.
4. Witness v3.1 (structural-gap-aware) if the fabric route is chosen.

## 03:00–04:45 ADDENDUM — implementation-forensic ladder on the shipping image

1. **Glitch-path probe** (`skidfix/dcp_glitch_probe.tcl` → glitch_probe_skid3.txt)
   on skid3's routed DCP: every BUFGCE CE pin is tied to VCC; the enable
   gating sits on buffer I inputs; zero pulse-width violators.
2. **skid4 built** (`33a962a85a22`, banked, NOT flashed) with
   GATED_CLOCK_CONVERSION auto on all 19 synth runs: netlist came out
   **bit-identical in WNS/rail census** — nothing to convert in the DUT.
3. **RTL ground truth**: all 129 DUT modules clock ONLY on `posedge clk` —
   no gated-clock idiom exists; the 60k-load enb rail drives CE-class pins
   (measured: FDCE/CE ×3235 on the FrameStatFifo repeater).
   ⇒ **LUT-generated-clock glitch hypothesis REFUTED.** The rail is a
   synchronous enable on a clock-network repeater: glitch-tolerant by
   construction, setup/hold met in every gate.

### Where the forward mechanism now stands (end of night)
Refuted tonight, each with data: DMA backpressure (dip replay), skid repair
class (v1 deadlock, v2 +5.6 pp), rail/clock glitch (above), reverse-side
cals/CFO/power (reverse ladder). Surviving constraints: corruption INSIDE
the DUT (CP1 checksum-matched), replay-clean on the same IQ from reset
state, 8-frame comb / 32-frame pairs, M-invariant, ~8.3% forward. The
remaining hypothesis space is **state- or environment-dependent behavior the
reset-state replay cannot reach** (long-uptime state wraps; concurrent AXI
activity — note framestat bridges byte-plane data into AXI-read logic, the
same structural family the wit-forensic flagged; host traffic patterns).
**The decisive instrument is an on-die ILA** on ByteSerializer→breakout→DMA
triggered on CRC-fail frames — a debug-core build on the skid3 lineage.
That is morning-queue #1.
