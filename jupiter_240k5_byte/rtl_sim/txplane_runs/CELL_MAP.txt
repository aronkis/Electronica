# TX byte-in plane netlist matrix -- cell map (2026-08-28). All cells: wrap_byte_bf2/wrap_byte_ce on s1_rtl_beatfix3
# (flashed lineage), internal loopback, tx_data_source=1, 385-word (3080 B) transfers with byte_first on word 0 unless nwf=191.
# Scorer: txplane_score (rx_seam_checker semantics; --dewhiten for whitened cells; --tgen accepts the TGEN CRC constant).
# cell  content            whitening  words/frame  arrival
# c01   idle len0 (daemon keepalive, real CRC, PN pad)   off   385   line-rate continuous (source always valid, DUT ready paces)
# c02   idle len0          off   385   rate 0.98 x line (one 385-word transfer released per 1/0.98 frame period) -> underrun every ~50 frames
# c03   idle len0          off   385   rate 0.95 x line -> underrun every ~20 frames (Test-A-like 5 %)
# c04   idle len0          off   385   rate 1.00 x line + uniform jitter 0..20000 clk on each release
# c05   idle len0          off   385   rate 1.02 x line (source faster than line: back-pressured, cont-like)
# c06   fill 1516 PN payload (daemon data frame, real CRC)   off   385   continuous
# c07   idle len0          ON (PN9, as the daemon with QPSK_WHITEN=1)   385   continuous
# c08   fill 1516          ON    385   continuous
# c09   TGEN exact format (fill 1516, CRC constant)  off   191   continuous (191-word frames back-to-back = hardware Test B source)
# c10   TGEN exact format  off   191   gap 100000 clk after each frame (~50 % duty, Test-B 620 f/s-like)
# g1-g6 idle len0          off   385   gap G clk after each transfer's last accepted word (G = 2000/3500/4500/6000/8000/20000), ORIGINAL netlist (16-word ByteWordBuffer)
# f3-f7 idle len0          off   385   same gap model on the CANDIDATE netlist s1_rtl_txfix (ByteWordBuffer 64 words, ready threshold 54): G = 4500/6000/8000/20000/15000
# Score window: first 20 good frames skipped (SKIPGOOD, 8 for g/f), then NF-SKIPGOOD-10 frames scored (trailing zero frames after the source ends are excluded).
# "magic_bad" = frame at the decoder output whose header is not QK/len<=1516 (all-zero frames); crc_fail = header ok, CRC wrong.
