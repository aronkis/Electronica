# SDD ledger — plan: (none; overnight autonomous campaign) TX-origin hunt for the 120.2 s beat — 2026-09-02 21:20 → 07:00 EST
Operator directive 21:15: "Debug to the transmitter now. Away until 7am EST; use the hardware as needed without approval; find the true error source." Standing rails kept: 148 only, 146 never; no flash without full rails; never kill an arm; polls ≥1 s; sentinel stays stopped; pre-register + positive controls; provenance labels.
Starting evidence [silicon]: 7/7 data departures (0→rung, rung→None, 0→None) in both sel6 captures are preceded by exactly one record by an extra Transmitter_txFrameStart (= Bit_Packetizer_dataStart) pulse; 0/5 returns; regular TX cadence intact (48 records before each demod mark, period 12320). Rungs = half frame + 16..388 symbols in ~64-symbol steps (64 symbols = 128 bits = 16 bytes = one ByteWordBuffer word?). Beat phase-locked to the arm (6/6 arms: trigger at +40 s of the watch window). Prior fact: TX byte-in ByteWordBuffer starvation is a known fabric defect (rtl_sim/TXPLANE_SIM_RESULTS.md).
Three parallel tracks:
- A (netlist trace, opus): what asserts Bit_Packetizer_dataStart, the mode-1 TX byte source, all cadences, beat arithmetic for 120.2 s.
- B (sim, sonnet): forced TX starvation / forced extra dataStart in the sim_burst_force harness — does it reproduce the beat signature (rung, data bit-exact, marker unmoved)?
- C (silicon, sonnet): sel8 (Transmitter_dataOut) arm, TX stream at the extra-pulse records — does the TX stream itself restart a frame?
- 21:25 dispatched: A (opus, RTL trace → two_jup/TX_ORIGIN_TRACE_A.md), B (sonnet, sim TX kicks → two_jup/TX_KICK_SIM_B.md, §86-B), C (sonnet, silicon sel8 arm → SEL8_PREREG.md, sel8_tx_restart.py, §86-C). Controller monitor armed for the sel8 rig unit.
- 21:28:16 sel8 first launch exited 127 (relative script path, no board contact); relaunched 21:28:30 as beatcap2-sel8-212830 with the absolute path — armed, profile found. Controller watching.
- 21:45 Track A done (199dc85, df3c590). NOTE: agent A PUSHED the branch to origin (not instructed; operator's own branch; 52+ commits now on origin). Top mechanism: Data_Bits_FIFO pop-enable latch cleared mid-frame by frameCount==0 (ufix2 wrap) → pops stop, stale content, data resumes bit-exact delayed; cadence/markers untouched. A also claims the 'extra txFrameStart pulse' cannot exist in RTL (dataStart = sampleCount==26, monotone) and that the DDRCAP FEC mark is a sticky latch → T0 test decides.
- 21:50 B retargeted to T0 + Data_Bits_FIFO pop-abort force (#1) + frameCount=3 (#2); C got pre-registered P-TX' (stall, not restart) as an addendum before scoring.
- 21:33:49 beatcap2-sel8-212830 SUCCESS: TRIGGER 21:31:17 at +40 s (errps=47969 — lower than the 63600 of the six earlier arms), mid.bin credited with pre AND post GOLDEN (first arm where the mid pre-read was not a rung word), onset.bin credited golden/golden. Whether either window contains the burst is for C's frame-by-frame scan (tref-indexed) to decide.
- 22:20 Track C first pass (295e4d4..332c859): P-TX (restart) FALSIFIED — no genuine mid-frame txFrameStart; P-TX' (pop-abort stall) PARTIAL — a controlled constant-(I,Q) run of thousands of symbols (value 5736,5736 / −5737,−5737) at the modulator output in BOTH credited sel8 windows, starting 6020-6443 symbols into the frame [silicon]; lengths confounded by a marker-chain frame-merge bug; continuation check not run. Fix round dispatched: tref-built frame windows, geometry table (rung = stall length = 6176+64k, start = 6144−64k), continuation check, constellation value.
- 22:15 §87 [silicon] (controller): every offset transition in both sel6 windows is a constant-symbol stall run from mid-frame to frame end (± one whole stalled frame) and new_offset = old − L mod 12320 within ≤10 symbols (errors 10/1/1/2/5). None state = predicted rungs 6489/6548, word 0xBFED37AC, not a pure delay (open). Receiver fully exonerated; mechanism = TX Data_Bits_FIFO pop stall (A #1); causal sim proof (B) and the 120.2 s rate question remain.
- 22:17 (clock check) B's flat build (txkick-build5, fifth attempt) compiling since 22:15; no sim runs yet.
- 22:30 Track C final (…1cffd04): sel8 (TX modulator output) 4/4 stall events run to the frame END (whole stalled frame follows on 2/4), constant full-magnitude QPSK point; L fits the rung set within ±16 on 2/4, misses by 19-22 on the other 2 (full-rate tap, 20-40 % record drops); marker on cadence after the stall 3/4. Controller reading: consistent with §87 (which is precise at sel6, no drops); the stall-to-frame-end geometry is confirmed at the transmitter output [silicon]. Track C closed.
- 22:31 B's flat build finished (exit 0, 22:30:53); B nudged to run T0 (+occupancy drift logging), #1 k=0, k=1..6 sweep, #2. Track D (sonnet) dispatched: None-state content via bit-level cyclic cross-correlation vs the golden frame (pairings, rotations, parity), with rung-state positive control.
- 22:38 Track D (55cb986, §88, NONE_STATE_D.md): the None state = the golden payload resumed at exactly the §87-predicted offset (6490 / 6548) with I bit-exact and Q bit-exact but ONE SYMBOL LATE — a fixed +1-symbol I/Q skew, identical in both captures [silicon]; explains the unmapped 16-symbol word. [inferred] odd bit-slot pop deficit → I/Q lane phase (HDL_Counter3 half-rate tick) shifted by one slot. Open: TX-side vs demux-side, and why 2 of 9 transitions.
- 22:51 B T0 [sim]: exactly one txFrameStart per frame (A's T0 confirmed); RAM occupancy drifts +26 bits/frame (producer pushes 24,666-slot frame images, consumer pops 24,640 data bits; 26 = preamble slots), frameCount steady 2, fullRAM 0; threshold 49,279 reached 946.7 frames = 3.04 s after the post-arm level (24,665) — matches the ~3 s burst DURATION, not the 120.2 s period. Phase-coincidence hypothesis for the period (producer/consumer boundary phase advances 26 slots/frame, recurrence 12,333 frames = 39.61 s; ×3 = 118.8 s) handed to B to check against the RTL, plus a near-threshold forced experiment. k=0 popabort: force took (armed→0 at clk 641342, 63 pop-quiet ticks) — scoring pending.
- 22:54 B k=0 [sim] REPRODUCES: single-shot clear of the Data_Bits_FIFO pop-enable latch at sampleCount 12,314 (force witnessed: armed→0, 63 pop-quiet ticks) → single-frame jump to offset 6144 (≡ −6176: stall L=6176 symbols to frame end), bit-exact (46/46 frames in the injective map), sustained 40 frames, marker cadence 12320 constant 45/45. Positive control (no force) 46/46 at offset 0. CAUSAL PROOF of the §87 mechanism. Sweep k=1..6, frcwrap, dstart, near-threshold in flight.
- 23:22 B sweep runs finished (k=1..6 biterr 2174/2252/2363/2213/2440/2603 vs baseline 51); frcwrap (frameCount=3 forced, no clear) biterr=51 = NO EFFECT in 20 frames; dstart (forced extra dataStart pulse: 49 pulses) biterr=51 = NO data effect — the extra pulse alone does nothing. B rebuilding (build6) for the near-threshold instrumentation.
- 23:22 SWEEP [sim] k=0..6 → offsets 6144/6080/6016/5952/5888/5824/5760 = 64 symbols per 128-pop step = the silicon rung family reproduced on demand; frcwrap and dstart NO EFFECT (offset 0 throughout).
- 08:30 (2026-09-03) Overnight: Track B's near-threshold runs finished 23:39 (nearfull v0/v1/v2: count forced to 49,227/41,227/33,227 at frame 3) — NO EFFECT: biterr 51 baseline, offset 0 throughout, no frameCount 3→0, only a shifted 1↔2 cadence. B never reported (its monitor was reaped); no commits after 1432c85. Rate question (120.2 s recurrence, the natural 3→0 wrap) still OPEN in sim. Dispatching a fresh rate-track agent (E).
- 09:25 Track E (7e1f4c8, §90): natural frameCount 3→0 abort demonstrated in sim after forcing the base to 3 (txrate_fc3; negative control clean) — but it self-cancels within ~1 frame; free-running drift falsified as the 120.2 s clock; fullRAM runaway = second defect (producer pushes every tick until uint16 count wraps, 16,256 RAM bits overwritten). E's frame time (3.21 ms) is WRONG — corrected by controller (802.93 µs, 1247 f/s): drift ticks at 0.76 s / 3.04 s. 120.2 s trigger NOT identified. Decision offered to operator: fix build (guarded clear + saturating frameCount + runaway fix) with sim gate + full rails, vs continue rate hunt.
