# Task 8 brief — flash 146 with the seqbist image, then stage 3 (fabric-only RF legs) and radio probes with the BIST
Preconditions: stage 2 done (rig free, 148 on seqbist a1ff3c876d91), keeper hold in place.
## Flash 146 (rails)
- Image boot_known_good/BOOT.BIN.146.seqbist.3378861d30bd (md5 3378861d30bd3d85663b31cfdd9c6296, WNS +0.166); rollback boot_known_good/BOOT.BIN.146.txfixF3vendh.6b4744ca73f8 (current). Chain two_jup/skidfix/flash_146_txfix.sh + txfix_flash146_go.sh (env FLASH_MD5=3378861d30bd FLASH_BAK=6b4744ca73f8 FLASH_TAG=seqbist — confirm the 12-char/tag conventions in its header; DRY first). Its gate fingerprints 148's daemon nakstat==4 — with no daemons deployed for the fabric-only legs that gate is moot: if the chain insists, rebuild the plain daemon on 148 first (capture_r3's own build line) or run the chain's gate stage with the daemon present, then quiesce. One chain run, rollback on its own failure, no retry.
- Post-flash on 146: GPIO segments 0x9D400000/0x9D410000/0x9D450000 respond; tgen_mode set; SINK=cyclic (one-shot cyclic S2MM ring by devmem — first silicon use, verify acc/ovf witness analogues: 0x1B0 byte_fifo_ovf constant during a TGEN loopback trial on 146 itself: run a 60 s MODE=loopback trial on 146 at the 148-calibrated mission gap as the 146 sanity check; slots 0-15 read 0 by design on 146).
## Stage 3 — fabric-only RF legs (no daemons)
- Arm both boards RF via a new two_jup/seqbist/bringup_fabric_only.sh derived from bringup_r2r3.sh (its RF arm, LO/SSI settings, 0x114=1, 0x158=1 on both, WATCHDOG=0, NO start_daemon), DRY-tested first. Forward: TGEN on 146 at the mission gap, checker on 148 (SINK=tgenrx), 600 s. Reverse: TGEN on 148, checker on 146 (SINK=cyclic), 600 s. Score: lost_slots/emitted with the gap1/gap2 split, garbage vs filler expectation, crc_fail, int_last series → period (32/33 emitted-frame intervals ⇒ the 26 ms comb), compare with the daemon legs (fwd 8.1 %, rev 3.7 %, period 26.0 ms).
- PREREG: if the fabric-only legs reproduce the loss and the 32.4-frame comb with no DMA/host anywhere, the defect is in the fabric/RF chain (deframer/sync) and the host is exonerated; if the loss is materially lower, the host/DMA path is back in play.
- Radio probes with the BIST (optional, if time): with the checker running, toggle 148 RX gain-control mode (automatic ↔ spi) and observe the int_last series live — a knob that changes the comb is a fix lead.
Report two_jup/sdd_archive/2026-09-03-seqbist/task-8-report.md; append to two_jup/OVERNIGHT_20260904_SEQBIST.md; SEQBIST_STATE.md results; commit -s; hold stays; at the very end (or by 06:30 EST at the latest): restore the plain daemons on both boards (capture_r3's build line), release the keeper hold (two_jup/comb/keeper_hold.sh release) and confirm the sentinel is running.
