# Task 27 — deploy the host resync fix (Task 26) to 148's daemon and judge it on the forward leg, with a same-binary A/B control (rig, ~50 min)

Read: two_jup/sdd_archive/2026-09-04-rxfix/task-26-report.md and two_jup/comb/RXFIX_HOSTFIX_PREREG.md (P1–P13, F1–F2; the 0.08 % bound has ~5 % headroom over the achievable floor 0.0764 %), two_jup/comb/deploy_daemon_go.sh (deploy procedure: on-board aarch64 build from the committed sources, md5 fingerprint, nakstat gate = 4; see how 09-03's deploy148-T1r ran), two_jup/comb/FWD_RESIDUAL_0p22.md.

Steps (rig serial; keeper hold before, release after; every step a launch_rig_unit.sh unit with watch_unit.sh; never edit a live unit's script):
1. Bank the CURRENT 148 daemon before replacing it: copy /root/host_app_k5/qpsk_tun from 148 to boot_known_good/daemon/qpsk_tun.148.<md5> (or the repo's existing daemon bank if one exists — check deploy_daemon_go.sh) and record its md5 (Task 10 recorded app 1f834433). Rollback = redeploy that binary.
2. Deploy: `launch_rig_unit.sh deploy148-resync <abs>/two_jup/comb/deploy_daemon_go.sh BOARD=148 DRY=0` (DRY=1 first). The sources are at the committed HEAD (Task 26 commit a00ab93 + 609b367); the on-board build must use -DQPSK_CARVE_2MB -DQPSK_ARQ_NAKSTAT -DQPSK_RXQ_STAT (whatever the script does today — verify by `strings qpsk_tun | grep -c nakstat` = 4 and record the aarch64 md5). Restart the daemon through the normal bring-up (bringup_r2r3.sh r3) so the sentinel's health probe sees it.
3. Judge leg A (fix ON, the default): `launch_rig_unit.sh hostfix-on <abs>/two_jup/rxfix/w1leg_go.sh MODE=air LEG=A BOARD=148 DUR=600 R4B=1 RSSI=1 EXP=9f13705d9fb0 FIXCTL_BASE=0x0 DRY=0` — the daemon env must NOT set QPSK_RXQ_ZEROHDR (default off) and must leave QPSK_RX_RESYNC at its default (on). Score: accept_analyze.py PER on host_seq (lost frames in the denominator), the checker series (events unchanged ~3/10 s), the daemon's resync_568 / resync_fail / resync_other counters from its stats line, burst bins, r4b witnesses.
4. Control leg B (same binary, fix OFF): same command with the daemon started with QPSK_RX_RESYNC=0 (DAEMON_ENV_A carries it; check how w1leg_go.sh/legrun pass env to bringup_r2r3.sh — DAEMON_ENV_A/B). Expected: PER back at ~0.18–0.22 %, resync counters 0.
   One re-run per wedge per leg (the wedge class hits both directions now; budget it).
5. Verdict per the pre-registration: P1 PER ≤ 0.08 % (PARTIAL 0.08–0.10 % with resync_568 ≈ the event count = the fix works but the floor is higher, per F2); events unchanged at the pins; A vs B difference ≥ 0.10 pp. Falsifier F1: PER unchanged with resync counters ≈ 0 → the re-anchor never fires (design or deploy fault: check the binary md5 and the flags); F3: PER unchanged with resync_568 ≈ event count → displaced frames are corrupt, not shifted → report.
6. Leave the fixed daemon on 148 if leg A passes P1 or PARTIAL and the link is healthy; otherwise redeploy the banked daemon. Hand back: bringup restore, hold released, sentinel restarted, daemon md5 from the board in the report.
Rails as before (labels, sample counts, heartbeats ≤ 5 min as task27, ledger `Task 27:`, commit -s + trailer, commit only touched files, no subagents, no polls). If T18's operator flash arrives meanwhile, finish the leg in progress first, then T18/T19. Report: two_jup/sdd_archive/2026-09-04-rxfix/task-27-report.md.
