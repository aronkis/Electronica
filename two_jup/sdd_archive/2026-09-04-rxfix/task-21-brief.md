# Task 21 — S2: RXFIX_R4E push-drop steering, cut + sim gate (desk, ~3.5 h)
Plan T21. The natural FULL-edge event is a DROPPED PUSH (Rate_Handle push_on_full: output valid density unchanged, one payload symbol vanishes). R4E steers that drop so the vanished symbol is a guard-band symbol: R4B unchanged on the EMPTY side; on the FULL side, when locked and occupancy ≥ 24, drop exactly ONE push per pcEnd, scheduled so that the dropped symbol would have been popped inside the structural window [pcEnd+1, pcEnd+13]. The push side leads the pop side by the ring occupancy, so the drop must be scheduled by occupancy: at pcEnd (window slot 0) the symbol that will be popped at slot k is the one (occupancy − 1 + k) pushes back… i.e. the push to drop is the one arriving (window_start − occupancy) symbols before the window — derive it exactly from Rate_Handle.v/FIFO_block.v pointer arithmetic, state the formula, and make the window-landing check part of the gate (the dropped symbol's would-be pop slot, computed from the pointers, must be in 1..13). Registered decision; witness `r4e_drops[15:0]` + `r4e_land_slot` histogram in a TENTH W1 word 0x238 (document in W1_REGMAP.md §6); no new IP port; _PFX anchors; mutual exclusion with R3/R3S/R4/R4D but composable with R4B's own skip (R4E CONTAINS the R4B skip side — clone R4B, do not stack); W1+R4E kit-shaped test; lint both lineages with logs banked.
- Pre-register `two_jup/comb/RXFIX_R4E_SIM_GATE.md` before code: K1 n_p000 content identity with R4B (never ≥ 24); K2 n_p10 LOSS ≤ 1 % on both denominators (b_p10 4.99, R4B 6.65); K3 n_p10 push_on_full 0 after lock (baseline 21, R4B 28); K4 ~37 drops at one per ~8.1 frames, every drop's landing slot in 1..13 (histogram), occupancy plateau 22/23; K5 pdPof 0 and pdOcc 12333 flat on every leg; K6 n_m10 / n_m10c / tiled / lol content-identical to R4B where the ring never reaches 24 after lock, else a short 31→23 ratchet then identical; K7 the three-way witness agreement; K8 FALSIFIER: any lost frame within ±1 of a drop beyond R4B's own losses, or K2 > 3 % with K3/K4 passing → a guard-band push drop still kills the frame → per-stage stagewin dump around one drop.
- Harness: wrap_byte_sro4e.v cloned from wrap_byte_sro4d.v (provenance print WRAP4E), obj_byte_sro_rxfix4e, stimuli on disk (n_p000/n_m10/n_m40/n_p10/n_m10c/s_m10/lol). Legs as systemd-run units t21_<leg>; heartbeat unit t21hb (`HEARTBEAT task21`); END YOUR TURN while legs run; ledger `Task 21:`; commit early and often (rxfix_inject.py is shared with Task 20 — rebase, never force); commit -s + trailer; sim only; no subagents; report two_jup/sdd_archive/2026-09-04-rxfix/task-21-report.md; T7_NAIR set in scorers; commit compact result files.
