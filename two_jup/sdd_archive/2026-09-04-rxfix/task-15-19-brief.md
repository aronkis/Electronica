# Tasks 15–19 — NIGHT-2 RIG DRIVER: pre-registration, reverse BEFORE, forward replicate with RSSI, cross-lineage W1 flash on 146, reverse witness leg

Plan: /home/tcollins/.claude/plans/happy-bubbling-owl.md (read the Context, rails, T15–T19, decision points DP0–DP3). You own the rig serially. Report each decision point to the controller with a ledger line `Task NN: DPx …` and keep going unless the plan says stop.

## T15 (0.3 h) — pre-registration + preflight
- Write and COMMIT `two_jup/comb/RXFIX_NIGHT2_PREREG.md` BEFORE any leg: the reverse credit rule (live window ≥ 300 s AND deliver-rate gate ≥ 900 f/s, no watchdog relaunch; wedge-truncated windows scored by accept_analyze.py and labelled), T16's predictions/falsifiers, T17's, the T19 witness branch table (W/a/b/c) verbatim from the plan, the 05:30 flash cutoff, the hand-back rule.
- Preflight: readback `md5sum /boot/BOOT.BIN` on both boards via two_jup/anyssh.sh (expect 148 = 9f13705d9fb0ea6ae6af3c4c1ab5e95d, 146 = 3378861d30bd3d85663b31cfdd9c6296), sentinel/keeper unit names running (sentinel-204437 / sentinelkeeper-204437), no hold files, hdl-dev-2 has no txfix-build-* unit active and ≥ 25 GB free. DP0: any mismatch → ledger PHYSICAL ATTENTION / desk-only, stop the rig path.

## T16 (1 h) — reverse BEFORE ×2 on the current pair
- `DRY=0 two_jup/comb/keeper_hold.sh hold`; then `two_jup/launch_rig_unit.sh rev-before1 <abs>/two_jup/comb/legrun_go.sh DRY=0 LEG=B DUR=600 TAG=rev_before1` (+ watch_unit.sh), then rev-before2. Shipped LOs/RXQ=1, failhdr/txlog env both ends (the DAEMON_ENV defaults legrun resolves). One re-run per wedge (a wedge-truncated ≥ 300 s window is still scored and labelled). If both wedge before 300 s, a third leg with RXM_146=8 (the m8rx config).
- Score each: accept_analyze.py (PER, lost frames in the denominator, CP95), comb_autocorr.py (lag family), comb_period_ms.py (the first reverse ms-domain period), comb_census.py (fail classes). PREREG [inferred]: PER 3.0–4.5 %, lag-32 family, period 25.3 ± 0.6 ms. DP1: credited PER < 1 % → note "no FULL comb on this pair"; > 6 % or > 100-frame bursts → "wedge class dominates". Either way continue to T17.

## T17 (0.4 h) — forward AFTER replicate with an RSSI/gain timeline
- First (desk, DRY-tested with the ssh shim in tests/test_rxfix_rig_scripts.py): add to `two_jup/rxfix/w1leg_go.sh` an `RSSI=1` knob = a THIRD sequential ssh read per 10-s reading on the RX board: `cat in_voltage0_rssi in_voltage0_hardwaregain` (and decimated_power if present) from `/sys/bus/iio/devices/iio:device2/` (find the right device by name at runtime, fail loudly), appended as columns; the reading period must stay 10.0 s (if it exceeds 12 s, poll RSSI every second reading). Also add `LEG=A|B` and `BOARD=148|146` (LEG=B → RX board 146, legrun_go.sh LEG=B, EXP = the 146 image md5) — needed by T18/T19. Keep DRY=0 MODE=ctrl on 148 byte-identical to Task 13's control table except the new columns (regression control).
- Then `launch_rig_unit.sh fwd-after2 <abs>/two_jup/rxfix/w1leg_go.sh MODE=air LEG=A DUR=600 R4B=1 RSSI=1 EXP=9f13705d9fb0 FIXCTL_BASE=0x0 DRY=0`. PREREG [silicon, Task 13]: PER ≤ 0.5 %, CP95 < 0.6 %, comb absent, lag-32 < 0.1, r4b_skips 394 ± 60 per 10 s, pop_on_empty 0, push_on_full 0, occupancy 8–10, hardwaregain 34.0 dB on every read. Falsifiers: PER > 1 % with skips ~394 → R4B did not replicate: ledger, freeze T18/T19 and stop; RSSI dips ≥ 3 dB on ≥ 50 % of burst intervals → "RF margin" note for T23.

## T18 (1.2 h) — cross-lineage W1 flash on 146 + Step-2 controls
- Bank a byte copy: `cp boot_known_good/BOOT.BIN.148.rxfixw1.2728dab3979a boot_known_good/BOOT.BIN.146.w1x148.2728dab3979a` (md5 line appended to the bank's MD5SUMS if one exists; commit).
- Read two_jup/skidfix/flash_146_txfix.sh and txfix_flash146_go.sh first (the chain brings up BOTH boards and re-arms 148 as a side effect; 148 must be restored to its mode after). DRY=1 run first, then `launch_rig_unit.sh flash146-w1x <abs>/two_jup/skidfix/txfix_flash146_go.sh FLASH_MD5=2728dab3979a FLASH_TAG=w1x148 FLASH_BAK=3378861d30bd ROLLBACK_BANK=<abs>/boot_known_good/BOOT.BIN.146.seqbist.3378861d30bd DRY=0`. Never kill mid-flash/arm; the chain rolls back itself; no retry loop.
- postflash_check.sh BOARD=146 (the SEQ-BIST judge on the 148-lineage image; SINK: 146 has no tgen_rx → use the cyclic sink path as seqbist_run.sh does for 146).
- Step-2 controls: `launch_rig_unit.sh w1ctrl-146 <abs>/two_jup/rxfix/w1leg_go.sh MODE=ctrl BOARD=146 EXP=2728dab3979a FIXCTL_BASE=0x0 DRY=0`: 146 loopback, three reads 10 s apart, ONE re-arm, three more, one 10-s freeze hold: all eight W1 words live (not const_0, not stuck), freeze holds all eight, census advances at 12,333 × frame rate with the five pre-discard stages within ±4, arm-transient pop_on_empty > 0 after the re-arm, stuck-at sweep. DP2: chain rollback or any word const_0/stuck → roll back to 3378861d30bd under the chain (if not already), ledger, stop the 146 silicon path (T19 cancelled), continue to hand back.

## T19 (0.5 h) — reverse witness leg with W1 on 146
- `launch_rig_unit.sh w1air-146 <abs>/two_jup/rxfix/w1leg_go.sh MODE=air LEG=B BOARD=146 DUR=600 RSSI=1 EXP=2728dab3979a FIXCTL_BASE=0x0 DRY=0` (10-s reads: w1_read.sh → seqbist_read.py if the checker is usable on 146 → rssi, sequential, never concurrent on the DRA). One re-run if wedge-truncated < 300 s.
- PREREG branch table (quote verbatim in the report): (W) occupancy 30–32 on every in-window read, push_on_full 394 ± 60 per 10 s with mean interval within 1 % of the PER comb period, pop_on_empty 0, Rate_Handle-out and downstream census SHORT by the push_on_full count vs the strobe; (a) occupancy 31/32 and push_on_full 0 → counter dead/unexercised; (b) occupancy 0–1 and pop_on_empty ~394 → sign model wrong, 146 is EMPTY-trending; (c) neither edge. DP3: ledger the branch that fired with the raw numbers. Optional T19b (only if before 00:30): the runtime enSlack A/B — same leg with `LOOP_POKE=0x208=0x8` on the RX board pre-window and FIXCTL_BASE=0x8; PREREG null.
- Then leave 146 on the W1 image if T18 credited it (rollback on-board), restore 148's mode, and END YOUR TURN with the rig still under hold — the controller decides T22/T24 and dispatches the hand-back. Do not release the hold.

## Rails
As the plan: launch_rig_unit.sh + watch_unit.sh for every rig step (absolute script paths), snapshotted scripts, polls ≥ 1 s, write-only regs verified by effect, FIXCTL_BASE stated on every W1 read, one re-run per wedge, positive controls before nulls, labels, every number with command + sample counts; HEARTBEAT task15 (then task16… as you progress — use the task id you are on) ≤ 5 min including while parked on a unit (heartbeat unit ok); ledger lines `Task 15:` … `Task 19:`; commit -s + trailer `Claude-Session: https://claude.ai/code/session_019NZLGbxoaPRkDnKFMPiBrq`; commit implies push; no subagents; no background sleep polls — watch units in the foreground of one Bash call with a covering timeout, or end your turn and the controller resumes you on UNITEXIT. Report: two_jup/sdd_archive/2026-09-04-rxfix/task-15-19-report.md (per task: commands, credits, numbers, P/F verdicts verbatim, images from readback). Return status/commits/one-line summary/concerns.
