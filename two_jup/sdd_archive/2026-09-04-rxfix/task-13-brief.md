# Task 13 — W1+R4B image for 148: kit + build (start now) → bank → flash under rails ONLY after the R4B sim gate passes → controls → witnessed forward air leg → PER judges

## Context (read RXFIX_STATE.md "W1 on silicon" and "Campaign brief" first, then task-12b-brief.md and task-12b-report.md when it lands, task-10-report.md for the rig procedure that worked today)
Mechanism proven on silicon (Task 10): the Rate_Handle ring sits at its EMPTY edge on the forward leg and the guard suppresses one pop every 25.38 ms (394 per 10 s); the PER comb period is 25.39 ms; the census shows no symbol deleted; PER 8.309 %. R4B (Task 12b) steers that skip into a structural 13-slot window after End_Generator's pcEnd, at occupancy ≤ 8 once locked, self-centring the ring at 9/10; in sim R3S (same idea, cruder arming) took −10 ppm loss from 10.93 % to 0.95 %.

## Phase A — build now (do not wait for the gate; the flash waits)
1. Kit: `two_jup/skidfix/jupiter_byte_rxfix_kit.sh` (Task 9) applies W1 on the SEQ-BIST tree; extend it (variable, W1 default unchanged) to apply W1 + RXFIX_R4B, in that order, using the injector at the commit Task 12b names as its cut (verify `RXFIX_KIT_VERIFY_OK`: 3 loose mirrors + both zip members + verify_zip for BOTH markers; the kit-shaped W1+R4B test from Task 12b must pass on the kit tree). Record the injector commit in the kit's provenance file.
2. Build on hdl-dev-2 with build_txfix.sh, IMPL_STRATEGY=explore, as a systemd-run --user unit; add `report_timing_summary -max_paths 10` after place_design if the flow allows (Task 9b's note) so a mid-route failure is diagnosable. Gate on the MODEM-CLOCK intra-clock routed WNS ≥ 0 (the overall WNS is the vendor IDELAYCTRL path) — record both. If the modem-clock WNS < 0: do not bank as flashable; apply the margin lever from RXFIX_W1_TIMING.md (fold the eight W1 words into the existing address_select_level1 case) and rebuild once; report if still negative.
3. Bank `boot_known_good/BOOT.BIN.148.rxfixr4b.<md5>` (md5 re-verified), update two_jup/rxfix/W1_REGMAP.md (ninth word 0x234 per Task 12b) and w1_read.sh/w1_score.py to read and score it (r4b_prefilled/armed, r4b_skips, r4b_window_opens; deltas; the at-rest expectations change: occupancy after lock 8–10, pop_on_empty delta 0).

## Phase B — flash + silicon judges (ONLY after the controller ledgers "Task 12b: complete" with the gate PASS; if the gate fails, stop after Phase A and report)
4. Flash 148 under the full rails exactly as Task 10 did (keeper hold; txfix_flash_go.sh FLASH_MD5=<r4b md5> FLASH_BAK=2728dab3979a FLASH_TAG=rxfixr4b; two-pass gate; auto-rollback; one re-arm allowed; never kill mid-flash/arm). Rollback = the W1 image 2728dab3979a (on-board .bak verified by the chain).
5. Step-2 controls in loopback, adapted: census counters positive control as before; freeze path; witA occupancy after lock in 8–10 (positive control that steering engaged: r4b_skips advances during the first ~10 frames after lock, then flat; r4b_window_opens advances at the frame rate; r4b_armed = 1); pop_on_empty delta 0 in steady loopback; stuck-at check on all nine words.
6. Forward air leg 146→148 with w1leg_go.sh MODE=air (10 s reads, FIXCTL_BASE=0x0, checker at the pins, capture_r3 PER with lost frames in the denominator, RATE_GATE 900, one re-run per wedge).
   PRE-REGISTERED [silicon] (from Task 10's numbers): occupancy 8–10 on every in-window read; pop_on_empty delta 0 (≤ 3 per 10 s); r4b_skips ≈ 394 ± 60 per 10 s (the holes become skips); push_on_full 0; checker gap events per 10 s < 100 (was 656); PER ≤ 3 % (was 8.309 %; hole-aligned share ≈ 6.3 pp); lag-32 < 0.1 (was +0.613); no 25 ms comb. FALSIFIERS: (F-A) r4b_skips ≈ 394 but PER unchanged and lag-32 comb present → the skip position does not matter on silicon (sim/silicon divergence; dump the checker interval series and stop); (F-B) r4b_skips ≈ 0 and pop_on_empty ≈ 394 → steering never engaged on silicon (lock/window logic; read r4b_armed/window_opens and stop); (F-C) PER improves but a NEW loss class appears (e.g. long gaps, re-lock events) → report the class with the checker's interval histogram; do not iterate on the board.
7. Hand-back as Task 10 (one restore re-run allowed; leave the R4B image on the board if the leg passed its gate and the link is up; otherwise roll back to 2728dab3979a under the chain and say so); images from readback; sentinel restarted.

## Rails
As Task 10 (launch_rig_unit.sh units + watchers, snapshotted scripts, polls ≥ 1 s, write-only regs verified by effect, labels, sample counts, commands). HEARTBEAT task13 every ≤ 5 min including while parked on the build; ledger `Task 13:`; commit -s + trailer `Claude-Session: https://claude.ai/code/session_019NZLGbxoaPRkDnKFMPiBrq`; commit implies push; no subagents; no background sleep polls. Report two_jup/sdd_archive/2026-09-04-rxfix/task-13-report.md; return status/commits/one-line summary/concerns.
