# Task 26 — host delivery-path resync (the forward 0.22 % residual is a byte-alignment cascade) — desk + tests; daemon deploy only when the rig is free

Task 23 [silicon, banked data] found: the forward residual 0.224 % is a BYTE-ALIGNMENT CASCADE in the receive delivery path. One decode error at the decoder pins (CRC-fail, 157/159 bursts) costs the host 8.59 frames because the next ~6–15 frames arrive DISPLACED BY A FIXED 568 BYTES inside the carve slice until the stream resyncs (1,259 of 1,305 magic-carrying class-1 failures sit at exactly offset 568; chance floor 2.3 %). The events are born at/upstream of the pins one-for-one (144 vs 143 events, CRC 129 vs 128 in the common window) and the delivery path multiplies their cost ~4×: at the pins 309 lost slots (0.054 %), at the host 1,229 (0.2145 %). The "delivery stall" reading is refuted (instrument artefacts). Read two_jup/comb/FWD_RESIDUAL_0p22.md and task-23-report.md first, then two_jup/comb/README_hostlog.md §3.3 (fail classes, magic_off), host_app_k5/qpsk_frame.c / qpsk_tun.c (the carve/slice parser, RXQ=1 queued DMA mode, how a slice is scanned for magic 0x51 0x4B, the 12-byte header, CRC32 poly 0xEDB88320), host_app_k5/test_* (existing tests), two_jup/comb/deploy_daemon_go.sh (how a daemon is deployed to 148: nakstat fingerprint gate, md5).

Deliverables:
1. Explain the 568-byte displacement from the parser (what slice geometry makes a lost frame shift the next ones by 568 bytes: 1528-byte logical frame vs the 3080-byte air frame vs the carve slot? compute and cite), and why resync takes 6–15 frames today.
2. Implement resync in qpsk_frame.c/qpsk_tun.c: on a header failure, scan the slice for the next valid magic+len+CRC at any offset (bounded scan, at least the 568 offset), re-anchor immediately, and count re-anchors (`resync_568`, `resync_other` in the stats line / failhdr record) so the effect is measurable; keep the fail-class taxonomy; no change to the fabric. Unit tests: synthetic slices with a dropped frame → 1 lost frame instead of 8; the W1 baseline's comb failures (dominant offset 64) must still be classified as before; existing tests green. Build with the same flags as the deployed daemon (-DQPSK_ARQ_NAKSTAT; `strings qpsk_tun | grep -c nakstat` = 4).
3. PRE-REGISTER (two_jup/comb/RXFIX_HOSTFIX_PREREG.md, committed before any deploy): forward leg on 148 with the fixed daemon: PER ≤ 0.08 % (from 0.224 %; events unchanged ~0.30/s, cost per event → ~1 frame), checker gap events unchanged (~3/10 s), resync_568 ≈ the event count, r4b witnesses unchanged. Falsifier: PER unchanged with resync_568 counting → the displaced frames are not recoverable (content corrupted, not just shifted) → report.
4. DO NOT deploy or touch any board yourself. Report DONE with the binary md5 and the pre-registration; the controller schedules the deploy + judge leg on the rig (deploy_daemon_go.sh BOARD=148 + w1leg_go.sh MODE=air LEG=A) when the rig driver is free.
Rails: ledger `Task 26:`; HEARTBEAT task26 ≤ 5 min; commit -s + trailer `Claude-Session: https://claude.ai/code/session_019NZLGbxoaPRkDnKFMPiBrq`; commit only files you touched; no subagents; report two_jup/sdd_archive/2026-09-04-rxfix/task-26-report.md.
