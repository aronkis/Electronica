# Task 20 — S1: R4D + R1 sim gate (desk, 0.75 h)
Plan T20 (/home/tcollins/.claude/plans/happy-bubbling-owl.md). Question: does the +1 valid from R4D's extra pop become harmless when the Preamble_Detector realignment FIFO pops on occupancy (R1, already cut: `Delay10_out1 = Delay8_out1 & (numEntries == 12333)`) instead of the tick-delayed valid — i.e. was the R4D collapse the FIFO's deletion (fixable) or the +1 valid itself in the Peak_Search/Timing_Adjust epoch spaces (not fixable this way)?
- Tree: `cp -a jupiter_240k5_byte/rtl_sim/s1_rtl_rxfix_R4D jupiter_240k5_byte/rtl_sim/s1_rtl_rxfix_R4DR1`, then `python3 two_jup/skidfix/rxfix_inject.py <tree> R1 --sim-tree` (check first that R4D's mutual-exclusion guard does not exclude R1; if it does, add R1 to the allowed set with a test). `build_sro_rxfix4dr1.sh` = build_sro_rxfix4d.sh with the new VD and -Mdir obj_byte_sro_rxfix4dr1, same +define+RXFIX_R4D and wrapper (provenance print must show the new tree).
- Pre-register `two_jup/comb/RXFIX_S1_SIM_GATE.md` before running: r4dr1_p000 byte-identical to r4d_p000 (_deliv/_seq); r4dr1_p10 LOSS ≤ 1 % on both denominators (b_p10 4.99, r4b_p10 6.65, r4d_p10 70.3) with push_on_full 0 after lock, pdPof 0, ring parked 22/23, extras one per ~8.1 frames, 0 losses within ±1 of an extra; r4dr1_m10 ≤ R4B's 0.48 % with the 31→24 ratchet harmless; tiled s_m10 0.00 %. Falsifier: losses aligned to extras with pdPof 0 → the +1 valid itself breaks framing → S1 dead (then a per-stage stagewin dump around one extra, as Task 11 did, naming the first stage that differs: Peak_Search epoch vs Timing_Adjust).
- Legs as systemd-run units t20_<leg>; heartbeat unit t20hb (`HEARTBEAT task20`); END YOUR TURN while legs run; ledger `Task 20:`; commit -s + trailer; sim only; no subagents; report two_jup/sdd_archive/2026-09-04-rxfix/task-20-report.md; set T7_NAIR in every scorer call; commit the compact _res/_skipwin/_seq files.
