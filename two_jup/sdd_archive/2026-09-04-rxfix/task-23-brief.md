# Task 23 — forward residual 0.224 %: what class is it? (desk, read-only on banked data, 1.5 h)
Plan T23. Data: two_jup/comb/runs/20260904_201814_w1_air/ (cap/frames.bin 893,975 records, cap/failhdr.bin 5,440 records, cap/txlog_peer.bin, chk.jsonl 48 lines, w1_reads.csv, verdict.txt, capture_r3.log with five "delivery stalled 4s" warnings) and, when it lands, the T17 replicate run (two_jup/comb/runs/*fwd-after2* / *w1_air* newest, with RSSI columns). Tools (corrected paths): two_jup/comb/comb_census.py (fail_class census MAGIC/LEN/CRC/ZEROTAIL, 64-B corruption-onset histogram, NEVER_SENT vs SENT_NOT_DECODED via the txlog join), two_jup/loss_period.py, two_jup/singles_cadence.py, two_jup/accept_analyze.py (run-length bins; add per-burst timestamps if it lacks them — a new option, tested), two_jup/frame_taxonomy.py, two_jup/comb/comb_period_ms.py (extend the lag scan to 1–120 s for this class).
Questions, each answered with a number and a label: (1) burst timestamps of the 5–20-frame events — clustered, periodic (chi-square against uniform over 1–120 s lags), or Poisson? (2) fail class of the residual failures and NEVER_SENT share (host TX starvation on 146?); (3) reconcile the fabric checker's 0.053 % lost slots with the host's 0.224 % on a COMMON window (the 470-s read window vs the 715-s live window) — how much of the residual is downstream of the decoder pins (host/DMA seam)? (4) are the five 4-s delivery flatlines and the six ~10 % census dips the same events as the bursts (align on wall time)? (5) RSSI/hardwaregain vs bursts from the T17 leg (AGC railed at 34.0 dB expected). PREREG [inferred]: ~93 % of residual frames in the 5–20 bin, ~one event per 4.4 s, no single period, MAGIC-dominated, RSSI flat, most of the residual downstream of the pins → delivery/stall class. Deliverable two_jup/comb/FWD_RESIDUAL_0p22.md naming the class and the next instrument (no fix tonight). No board contact; ledger `Task 23:`; heartbeat task23 ≤ 5 min; commit -s + trailer; no subagents; report two_jup/sdd_archive/2026-09-04-rxfix/task-23-report.md.
