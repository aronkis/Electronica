# SDD ledger — plan: docs/superpowers/plans/2026-09-02-ddrcap2-joint-timing-capture.md
Spec: docs/superpowers/specs/2026-09-02-ddrcap2-joint-timing-capture-design.md (binding; §6 frozen)
Branch per-under-1pct-2026-07, existing checkout (untracked netlists/images/captures required; same ruling as the beat plan).

## Preflight rulings
- Ruling: execute in the existing checkout, not a worktree — plan depends on untracked s1_rtl_final, build trees, boot bank, captures — cost if wrong: interleaved edits from other sessions; mitigated by per-task commits.
- Ruling: Task 4 PART A must ALSO cover the bit-domain selectors 9,10,11 (spec §4: every channel) — the plan's driver skips them; the v1 gate cannot be reused because it tests marker==0x7FFF. Only sel 7 (dead) is skipped. Carried into the Task 4 dispatch — cost if wrong: a repack regression on the bit taps goes unseen.
- Ruling: the flash (Task 6 Step 4) is an operator-gated checkpoint even though the build+flash were approved — irreversible-class action, one flash event per session.

## Preflight conflict scan
| pair / task | produces vs consumes | finding |
|---|---|---|
| T1 tables/helpers / T2 patchers | PD_PORTS, SS_PORTS, add_ports, decl_lines, _insert_* | consistent |
| T1 build_ddrcap2_sim.sh / T2 main, T4 sim_ddrcap2.cpp | script calls ddrcap2_inject main (T2) and compiles sim_ddrcap2.cpp (T4); NETLIST_ONLY path used until T4 | consistent, sequenced |
| T2 composite / v1 composite | COMPOSITE_PRE inserted before mux (uses Receiver_ddrcap_dc_underflow wire declared earlier); COMPOSITE_POST replaces the marker assigns after ddrcap_valid_beat | consistent (declare-before-use respected) |
| T3 decode keys / T7 pc, T8 analysis | mark_demod, mark_fec, toff, slot, tref | consistent |
| T4 CAT(FTS,RHCTR_REG) | REG grepped WITHOUT the FTS prefix, pasted with it | consistent |
| T6 gate parses arm148_mode1 'fps=' | ARM_OK line carries fps= | consistent |
| T7 sel13 underflow bit / T2 packing I[15] | sticky uf in I[15] | consistent |
| T8 analysis frames_by_index=len/12333 | sel6 symbol-domain only | consistent (documented) |
| each task self-consistency | tests match code signatures (decode/check_common/check_sel/analyse) | clean |

## Task log
Task 1: dispatched implementer (sonnet) 08:50:48, BASE 80fd501
Task 1: implementer DONE (0acd1fc, 4/4 incl. lint); reviewer dispatched (haiku)
Task 1: complete (commits 80fd501..0acd1fc, review clean)
Task 2: dispatched implementer (sonnet) 08:54:49, BASE 0acd1fc
Task 2: implementer DONE (56f6349, 6/6 incl. TxRxComposite lint; 2 anchor whitespace fixes + count assert corrected 4->7 with derivation; zip path smoke-tested manually); reviewer dispatched (sonnet)
Task 2: complete (commits 0acd1fc..56f6349, review clean). minor (deferred): plan Task 2 Step 5 pairs grep -c with an occurrence threshold (doc only).
Task 3: dispatched implementer (haiku) 09:05:21, BASE 56f6349
Task 3: implementer DONE (ea8cbf8, 2/2); reviewer dispatched (haiku)
Task 3: complete (commits 56f6349..ea8cbf8, review clean)
Task 4: dispatched implementer (sonnet) 09:09:06, BASE ea8cbf8; carries ruling: PART A includes sel 9,10,11 (skip only 7)
- Ruling (Task 4, 10:25): the gate as briefed ran ALL parts on the flat build with NF=40 (15 selectors x ~8M clks at ~4 kclk/s ≈ 8 h) and its stdout was fully buffered (log empty after 60 min) — my brief's "~40 min" estimate was wrong. Stopped the unit. Restructure: driver takes a mode arg (A|B|C); PART A+C run on the threaded obj_ddrcap2 with NF=20, PART B on obj_ddrcap2_flat with the force at cnt_frame_start==10, recording from frame 6, NF=8; run A/C and B as two concurrent systemd units; line-buffered stdout. Same checks, same expectations — only run lengths and build assignment change. Cost if wrong: shorter PART A windows (≈20 frames of records instead of 40) reduce the marker/slot statistics but stay far above the thresholds.
- Task 4 gate FAIL (12:0x): "sel12 one dominant correlator peak per frame" — 343 records above half-max over 26 frames (~13/frame) vs my a-priori 0.5..3 rule. All other sel12 checks PASS (live). Ruling: the peak-shape rule was a controller guess, not an RTL-derived expectation; implementer must MEASURE the per-frame magnitude structure (contiguous runs above half-max per frame, their widths and separations) and replace the rule with a run-count rule justified from the correlator RTL (26-tap matched filter at 2 sps -> main-lobe width), recording the observed structure in §80 as DATA (multiple separated runs per frame would be exactly the secondary-peak structure candidate #2 predicts and must not be hidden). sel12's real positive control is the PART B forced readback. Cost if wrong: a mis-set liveness rule; the forced control still guards the channel.
- Task 4 sel12 diagnosis [sim, controller read of beat_runs/ddrcap2_sel12_mag.txt]: main correlator peak is ONE record wide (in-frame offset 12320 = the preamble slot), ratio 1.0; ~16 deterministic SECONDARY peaks per frame at fixed positions, ratios 0.48–0.61 (largest 0.60 at −1399 symbols rel. to the main peak); NONE within 60 symbols of any rung (6176..6548) relative to the main peak. So in clean sim the correlator has a dense ~50% sidelobe floor (ROM payload vs preamble partial correlations) but no rung-positioned sidelobe. Ruling: replace the peak rule with "exactly one record > 0.8×frame-max per frame" (PASS) + a recorded secondary-peak census (count, max ratio, position) as §80 data. Bears on candidate #2: a sidelobe would need ~+40% relative gain (or the main peak −40%) to win; rung-specific geometry absent in clean sim.
- Task 4 gate B FAIL: 'force Rate_Handle ctr=0xA5 -> sel15 I[15:8] expect=0xA500' — check() compares the FULL 16-bit I against 0xA500, but sel15 I = {rhctr[7:0], rhpush[4:0], 3'b0}: rhpush is a live 5-bit counter, so bits [7:3] are nonzero and the equality can never hold. Ruling: check bug — compare (I & 0xFF00) == 0xA500 (and if that still fails, the forced reg is not the one driving beatobsRhCtr). B otherwise 8/8 PASS.
Task 4 gate: A PASS after sel12 rule (census: mean 16 secondaries 0.45-0.8x, largest 0.61 at -1399 rec), C PASS (sel14 diff 1181864/1331660); B RHCTR re-verify (mode R) running 12:34:51
- Task 4 RHCTR re-check still FAIL with the mask. Root cause [sim, RTL read]: Rate_Handle.beatobsRhCtr = BfGridPace.y, and with bfGridEn (fixctl) = 0, y_1 = {6'b0, c} where c is the 2-bit mod-4 HDL_Counter — the forced BfGridPace.a never reaches y. So the "RhCtr" byte is the beat-fix grid pacer, a 2-bit counter at fixctl=0, NOT a FIFO occupancy (my spec §3 wording was wrong). Ruling: sel15's controlled fields are the FIFO push/pop counters (I[7:3], Q[15:11]); the forced control is re-targeted at the Rate_Handle FIFO push-counter register and checked in I[7:3]; the RhCtr byte is documented as BfGridPace pacer (values 0..3 at fixctl=0) with its own liveness (cycles 0..3) but no forced control — reported as such, not interpreted as occupancy. Spec §3/§4 text corrected in §80. Cost if wrong: sel15's occupancy claim is dropped; the FIFO witness (push/pop) — the part §66 cared about — is kept and controlled.
Task 4 gate: R PASS 13:03 (FIFO push=0x15 -> sel15 I[7:3], mask 0xF8). All parts PASS: A (recheck), B 8/8 + R, C. Awaiting §80 + commit.
Task 4: implementer DONE (676ff41; 117 PASS, 0 FAIL, DDRCAP2_GATE PASS); reviewer dispatched (sonnet). Note for Task 7: sel15 RhCtr byte = BfGridPace pacer (0..3), controlled field = FIFO push/pop.
Task 4: review Needs fixes — Critical: sel15 forced check not falsifiable (free-running 5-bit push counter passes through 0x15 naturally; no committed run shows the FINAL check failing without the force). Same non-falsifiability applies to the tref force (free-running mod-12333 counter passes 0x1234 every frame). Important: (1) final log's sel12 lines are from the NF=40 re-verify but the section header says NF=20 — provenance must be stated; (2) partA has no selector-specific liveness for sel13 (countReg sweep, underflow ~1/symbol) or sel15 (push advances, RhCtr cycles 0..3) though the spec rows claim them. Minor: pr() doesn't print observed n for the 9-11 floor; dead sel7 branch. Fix round 1/5 dispatched to the original implementer. Ruling on the fix design: forced checks must require the expected value in >= K consecutive captured records inside the 128-clk hold window (K=8 for per-beat fields, K=3 per slot for sidecar fields) AND each free-running field (tref, FIFO push) gets a NO-FORCE negative control run that must FAIL — falsifiability demonstrated in the committed log.
- Task 4 fix round 1: A(sel13,15) re-run: sel13 underflow mean = 0.250 vs my 0.5±0.05 expectation → FAIL. Ruling: expectation was wrong — the enb_1_2_0-valid taps capture 4 records per symbol (same log: sel15 push counter advances +1 exactly every 4th record, 332915/1331659 = 0.250; and the sample-domain golden streams are 49,332 = 4×12,333 records/frame), so one underflow per symbol = mean 0.25. Set the sel13 rule to 0.25±0.03 with that justification; carry the same correction into Task 7's ddrcap2_pc.py (underflow_per_symbol: |mean−0.25|≤0.03) and note in §80 that enb-domain taps are 4 records/symbol (spec §3 said 2 samples/symbol — corrected). Cost if wrong: none to the instrument; a mis-stated rate in the spec.
Task 4 fix round 1: B2 PASS 15:12 (K-consecutive rule; no-force controls FAIL-AS-EXPECTED: tref maxRun=0, FIFO push maxRun=5 < K=8 — margin 3 records, note: a free-running push value persists 4 records at 4 rec/symbol). A(sel13) PASS with 0.25 rule. Awaiting consolidation + fix commit.
Task 4: fix round 1/5 commit eda2ab9 (falsifiable PART B, provenance, sel13/15 liveness); scoped re-review dispatched (sonnet), FIX_BASE 676ff41
Task 4: fix round 1/5 (4 addressed, 0 open; commits 676ff41..eda2ab9)
Task 4: complete (commits ea8cbf8..eda2ab9, review clean). DDRCAP2_GATE PASS with falsifiable PART B. Deferred minors: only re-run rows show the new n= format (cosmetic).
Task 5: dispatched implementer (sonnet) 15:18:27, BASE eda2ab9
Task 5: build launched on hdl-dev-2 15:19 (Vivado ~50-60 min)
Task 5: Vivado build DONE 16:05 on hdl-dev-2 — BOOT.BIN 7203552 B, 0 ERROR, routed WNS +0.105 / TNS 0 / WHS +0.010 [build]. Implementer nudged to fetch/verify/bank/commit.
Task 5: implementer DONE (a846b0e, bb59345; image 638b36de3493, WNS +0.105); reviewer dispatched (haiku). Task 6: dispatched implementer (sonnet) — steps 1-3 only (script, dry-run, commit); step 4 flash requires operator go. BASE bb59345
- 16:2x: 148 UNREACHABLE (100% ping loss); sentinel probe failing every 5 min since first failure (see grep) and NOT launching a restore. No board writes from this session since the 08:xx read-only checks; no capture ran. FLASH (Task 6 step 4) BLOCKED until 148 is back — needs physical power-cycle (no remote power). Operator notified.
Task 5: complete (commits eda2ab9..bb59345, review clean). Minor (pre-existing): MD5SUMS lists a missing rxfifo4k_v5debug image — irrelevant to the ddrcap2 precondition.
Task 6 (steps 1-3): implementer DONE (7d45ef0; dry-run clean; found+fixed a real gap: gate() lacked a DRY guard and would have armed 148 under DRY=1). Reviewer dispatched (sonnet). Step 4 blocked: 148 down + operator go.
Task 6: review Needs fixes — Critical: rollback()'s 'not back after rollback' exit leaves SENTINEL_STOP (my template's defect); Important: witness ssh unbounded (wrap in timeout); Minor: rollback brd status checks, md5 arg validation. Fix round 1/5 → original implementer. FIX_BASE 7d45ef0
Task 6: fix round 1/5 commit 269d426 (EXIT trap releases sentinel; witness timeout 180; rollback copy verify; argv check); scoped re-review dispatched (sonnet)
Task 6: fix round 1/5 (3 addressed, 0 open; commits 7d45ef0..269d426)
Task 6 (steps 1-3): complete (commits bb59345..269d426, review clean). Step 4 (real flash) BLOCKED: 148 unreachable since ~08:46 (needs physical power-cycle) AND operator go required. Deferred minors: fake_anyssh.sh not updated for the witness path (dry mode doesn't use it); empty WOUT line on timeout (cosmetic).
- 16:3x: 148 back (power-cycled, up 0 min) on 1cd0cd752aa6; /boot free 700 MB; no rollback .bak for 1cd0cd752aa6 yet (chain creates+verifies it); SENTINEL_STOP set pre-go to stop the sentinel's post-reboot r3 (both-board) restore from racing the flash. Awaiting operator go.
- 16:52:44 baseline arm on current image 1cd0cd752aa6 launched (operator: 'baseline first')
- 16:54 baseline arm on 1cd0cd752aa6: ARM_OK fps=1247 errps=55 capTAP=0xBCF94856 [silicon]. Board healthy on the current image. SENTINEL_STOP still set. Awaiting operator go for the flash.
- 17:27:13 OPERATOR GO: flash chain launched as ddrcap2-flash-172713 (image 638b36de3493, rollback 1cd0cd752aa6)
- 17:27:35 first launch exited at the argv usage check (launch_rig_unit passes no positional args; no board contact); relaunched via flash_go.sh wrapper as ddrcap2-flash-172735
- 17:28:47 FLASH CHAIN ABORTED at [3/5] readback: wait_back returned on PING (board answers ping before sshd is up) so the md5 ssh returned EMPTY 67 s after reboot; chain went to rollback, whose restore-copy ssh also returned empty -> FLASH_DDRCAP2_FATAL "PHYSICAL ATTENTION before reboot", exit (no rollback performed, no second reboot). EXIT trap released SENTINEL_STOP -> re-set by controller at 17:29:06 to block a sentinel r3 (both-board) recovery. State: /boot on 148 = 638b36de3493 (written+FLASHED line), /root/BOOT.BIN.1cd0cd752aa6.bak verified in step 1. Chain defect: wait_back must wait for SSH (md5 readable), not ping. Rail "no retry loop" honoured: nothing re-run. Awaiting ssh + operator decision.
- 17:29:51 ssh back on 148: booted image = 638b36de3493 (readback MATCHES the flashed image), rollback copy /root/BOOT.BIN.1cd0cd752aa6.bak intact. The flash itself succeeded; only the chain's [3/5]-[5/5] verification (readback, two-pass gate, witness) did not run. SENTINEL_STOP held. Awaiting operator decision: run gate+witness by hand on the new image, or restore.
Task 6: fix round 2 commit fdf17bb (wait_back/readback poll ssh md5, 360 s bound; empty = not ready). Scoped re-review dispatched (sonnet). Real-run abort at 17:28 recorded above.
- 17:35 DEFECT (chain, DRY path): the fix-round DRY runs touch SENTINEL_STOP and their EXIT trap removes it — that DELETED the controller's manual hold set at 17:29:06 (sentinel probed at 17:33:49). Re-set at 17:35. Fix required (fix round 3 after the running re-review): the trap must remove SENTINEL_STOP only if the chain created it (flag set at touch time), and DRY=1 must never touch or remove it. Ruling: manual SENTINEL_STOP holds are now to be verified after any DRY run.
- 17:37 SENTINEL_STOP removed AGAIN by the re-reviewer's DRY run (same trap defect). Re-set. Between 17:29 and 17:33 the sentinel keeper relaunched the sentinel and it ran its post-reboot r3 bring-up: "byte re-arm done" x2, "r3 BRING-UP COMPLETE" — i.e. 148 was armed in r3/link mode on the NEW image and 146 WAS RE-ARMED (not flashed/rebooted; uptime 1 d). Read-only probe of 148 afterwards: fps=312, capTAP=0x121D8572 (r3/link-mode arm, not the mode-1 gate; 312 f/s is the profile-mismatch signature, unverified). Task 6 fix round 2: re-review clean (fdf17bb). Fix round 3 dispatched: trap removes SENTINEL_STOP only if this run created it; DRY never touches it. STANDING RULE for this session: nobody runs the chain (even DRY) while a manual hold is in place until round 3 lands.
Task 6: fix round 3 commit 3e80318 (SS_MINE ownership; DRY never touches SENTINEL_STOP); hold survived its DRY runs. Scoped re-review dispatched (haiku).
- 17:41 SENTINEL_STOP removed a THIRD time — by the fix-round-3 implementer's own verification scenario (it created and then deleted the file at the same path). Ruling: the file-flag hold is not tooling-proof; STOPPED the sentinel and keeper user units (sentinelkeeper-004023, sentinel-112524) for the remainder of the flash campaign — the operator's flash-discipline note calls for stopping the sentinel anyway. Re-enable after the campaign with `systemctl --user start sentinelkeeper-004023.service`. Cost if wrong: no automatic link recovery while stopped — acceptable while the operator is present and 148 is under manual verification.
- 17:43 sentinel-173338 unit stopped, keeper stopped, delivery_sentinel.sh killed; sentinel fully down for the campaign; SENTINEL_STOP file also present.
- 17:45 CORRECTION: the "byte re-arm done / r3 BRING-UP COMPLETE" lines I attributed to a 17:33 sentinel recovery are the 08:52 MORNING recovery — they are untimestamped and my `awk $1 >= time` filter passed them regardless of time. The ordered tail of sentinel.log shows: 16:32:15 "STOP-file present, sentinel exiting"; 17:33:38 "(re)launched by keeper"; 17:33:49 one failed probe; nothing else. **146 was NOT re-armed this afternoon** (uptime 1 d, no recovery ran). 148 at fps=312/non-golden capTAP = unarmed post-boot default on the new image (the gate never ran), not a sentinel arm. My 17:38 report to the operator stating 146 was re-armed was WRONG — corrected to the operator at 17:45.
Task 6: fix round 3/5 (1 addressed, 0 open; commits fdf17bb..3e80318). Task 6 (chain script): complete, review clean; real flash (step 4) ran once at 17:27 and aborted at readback on the pre-fix waiter; flash-side rails held; image on 148 = 638b36de3493, gate not yet run.
- 17:39:23 OPERATOR chose option 1: verify in place launched as ddrcap2-verify-173923 (gate x2 + sel6 witness; restore on failure)
Task 6: complete (step 4 flash done 17:27 + verify-in-place OK 17:42; §81 committed). Image 638b36de3493 live on 148. Tier-2 tOff non-null satisfied (d0=26 vs sim 12323).
Task 7: dispatched implementer (sonnet) — Tier-2 five captures on the CURRENT arm (board armed by gate pass 2 at 17:42; no re-arm), ddrcap2_pc.py + tests; underflow rule 0.25±0.03; sel15 RhCtr = pacer 0..3 (liveness only), push/pop = controlled fields.
Task 7: implementer DONE (4180bfb): sel6 PASS; sel12/13/14/15 FAIL. Controller diagnosis [silicon]:
 (a) sel12 FAIL = the OLD peak rule still in ddrcap2_pc.py (0.5..3 above half-max) — my miss when I updated only the underflow rule; on silicon sel12 shows exactly 1 record >0.8xmax per frame, 16 secondaries at 0.45-0.8, 2nd/max 0.60 — IDENTICAL to the sim census (§80). sel12 is LIVE.
 (b) sel13/14/15 FAIL "demod_marks_periodic" = record-index periodicity broken by DMA RECORD DROPS at full rate: sidecar tref deltas on sel13 are 1 (=4 records/symbol, exact) for 99.8% of samples, with 0.2% bursts of ~155 symbols (~620 records) every ~2k records + rare 7.5k-symbol gaps → ~20% of records lost in bursts at 61 M rec/s; sel6/sel12 (15 M rec/s) show only isolated drops (0.00-0.03%). Same signature as sel1 (§72 "witness-dead", modal gap 37,185) and sel2 (§75 "index-dead") — unifying explanation: full-rate enb-domain taps exceed the rx2 DMA's sustainable rate (S2MM transfer-boundary stalls, the delivery-plane comb mechanism). The in-record tref/tOff/markers make per-record frame position recoverable regardless of drops.
 Ruling: sel13/14/15 are LIVE (their field controls passed); the periodicity rule is a record-index assumption invalid at full rate → replace with a tref-cadence rule (slot-1 delta==1 for >=95%) + report drop-burst stats; analyses on full-rate taps must index by tref, not record index. sel12 rule → single main peak >0.8xmax + census (as §80). Fix round 1/5 to the Task 7 implementer. Cost if wrong: a rate-limited tap is credited on its field controls; mitigated by the mandatory tref indexing.
Task 7: fix round 1 commit cd8f027 — sel13/14/15 PASS under tref_cadence (99.80%); sel12 99.82% frames single-main-peak, 10 outliers = 5 missed-demod-mark events (frame segmentation by a dropped marker record). Implementer refused to loosen the literal rule and escalated — correct behaviour.
- Ruling: sel12 rule gets the same tolerance band as every other Tier-2 rule: PASS iff >=99.5% of marker-segmented frames have exactly one record >0.8xmax, and the report lists the exception frames with their marker-gap anomaly (so a genuine multi-peak frame could not hide). Under this, sel12 = PASS. Loss-fraction estimate: implementer's structural estimate 38-40% vs my burst-sum ~20% — both [inferred]; record both; not load-bearing (the ruling is "index by tref"). Fix round 2/5 dispatched (tiny), then one scoped re-review of rounds 1+2.
Task 7: fix round 2 commit ae63cc3 — all five TIER2 PASS (sel12 99.82% under the 99.5% band, exceptions listed). Controller's ~20% derivation for the record: 1950 bursts x ~620 records = 1.21 M lost vs 4.0 M kept in the 4 M-record window => 23%; implementer's structural estimate 38-40%; both [inferred]. Scoped re-review (rounds 1+2) dispatched (sonnet).
Task 7: NOTE — the original commit 4180bfb went to fix rounds without its task-scoped review; dispatching that review now (haiku) in parallel with the rounds-1+2 re-review.
Task 7: original-commit task review (haiku, e8fe313..4180bfb) clean — capture script honours mux-after-arm / golden pre+post / stat-before-rm / refuse-overwrite / 2 s poll spacing; five captures 536870912 B each; TDD RED->GREEN; one Minor (launcher workaround invocation not in the diff — accepted, launch_rig_unit.sh was used). Rounds-1+2 scoped re-review (sonnet, 4180bfb..ae63cc3) clean: 3/3 addressed, no new breakage.
Task 7: complete (commits 4180bfb..ae63cc3; task review + re-review clean). All five Tier-2 selectors PASS on silicon.
Task 8: dispatched implementer (sonnet) at 18:41 — capture script + fakes verbatim from beat plan Task 7 (lines 868-973), TDD analysis, ONE attended arm on 148 via launch_rig_unit.sh (operator present since 17:39; sentinel stopped).
- 18:48:21 Task 8 arm launched by the implementer as beatcap2-sel6-184821 (mode-1 arm on 148, sel6 burst-phased capture, out beatcap/20260902_184821_sel6). Implementer parked awaiting the capture; controller watching the unit and will resume it when the unit exits.
- 18:51:18 beatcap2-sel6-184821 EXITED with no capture: ARM_OK (fps=1247, capTAP golden), TRIGGER at +40 s (errps=63600), then "ABORT mid: capTAP 0xD71F70D3 != golden". 0xD71F70D3 is the KNOWN rung-6299 word (§72 table, line 1466) — i.e. the trigger WAS a genuine beat burst and the verbatim beat-plan script's pre-capture golden check aborted it, because capTAP is non-golden during a burst by design (§24/§72). Script defect, not a rig fault. [silicon]
- Ruling: beat_tap_capture.sh revision 1 — the pre/post 0x20C checks for mid and onset RECORD the value and abort only if it is neither golden nor one of the seven known rung words (link lost); a capture is credited if pre and post are each golden-or-rung. The arm-time golden check stays as is. Then ONE more attended arm (operator present; this is a re-run after a procedure defect, not a retry loop). Cost if wrong: a capture taken with the link genuinely lost would be credited — mitigated by the Tier-2 PASS requirement on onset.bin and the demod-marker cadence check.
- 18:55:52 Task 8 fix round 1: script revised (golden-or-rung credit), dry runs passed per implementer; SECOND attended arm launched as beatcap2-sel6-185552 (out beatcap/20260902_185552_sel6). Controller watching the unit; implementer parked.
- 19:01:09 beatcap2-sel6-185552 SUCCESS: TRIGGER 18:58:40 at +40 s (errps=63602; both arms triggered at +40 s with near-identical errps — observation only), mid.bin 512 MB pre=0xD71F70D3 (rung 6299) post=golden credit=yes; onset.bin 512 MB pre/post golden credit=yes. Implementer resumed for Tier-2 + verdicts + §82. [silicon]
Task 8: implementer DONE_WITH_CONCERNS (7a8b0ce) — verdict P2 on onset.bin AND mid.bin (tOff holds at d0=12314 while d_data sits on a rung ≥3 frames); Tier-2 sel6 PASS both; cross-arm d0 26 (Task 7) vs 12314 (this arm) vs 12323 (sim) → tOff field live across arms. Task review dispatched (sonnet) with the P2-earned-not-trivial question as the load-bearing item.
Task 8: task review (sonnet) = Needs fixes, write-up only: (1) IMPORTANT sim d0=12323 comparison missing (mod-12320 framing: 26/−6/+3, pairwise different → live); (2) frame-count gap asserted without arithmetic; (3) run.log not committed. Reviewer independently reproduced onset detection (onset.bin 0→6363 at frame 1897, mid.bin 0→6432 at frame 1014, ≥10 frames held both) and the verdict JSONs byte-for-byte; fold-modulus fix confirmed real. Ruling: non-null criterion met on the OR (all three d0 differ pairwise); P2 stands. Fix round 1/5 dispatched (resume implementer).
- Ruling for Task 9 (P2 branch, prepared while Task 8 fix round runs): full-rate taps (sel13/14/15) hold ~1.1 s per 512 MB window (4 records/symbol) versus 4.4 s at sel6; the sel6 onset landed 1.52 s into a window opened at T_trigger+PERIOD−1.5 s, i.e. at T_trigger+120.22 s [silicon]. So secondary arms run with LEAD=0.5 (onset expected ~0.5 s into the window, ±0.5 s tolerance) — [inferred] from one arm's onset timing; cost if wrong: onset window misses the transition and only mid.bin (in-burst state) is usable, which still tests the P2 interpolator prediction in the displaced state. Analyses index by tref (drop bursts at full rate), never by record index.
Task 8: fix round 1 commit 58bfa62 (sim d0 comparison + mod-12320 framing, frame-count arithmetic, run.log committed). Scoped re-review dispatched (haiku).
Task 8: fix round 1/5 (3 addressed, 0 open; commit 58bfa62; .gitignore change scoped to the two run.log files).
Task 8: complete (commits 7a8b0ce..58bfa62; task review + re-review clean). §82 verdict P2 [silicon]: tOff holds at d0 while d_data steps to a rung (6363 onset.bin, 6432 mid.bin) and holds ≥10 frames; positive control: d0 26/12314/12323 pairwise different.
Task 9: dispatched implementer (sonnet) at 19:17 — P2 branch: pre-register (TASK9_PREREG.md, committed before arming), then sel13 ONE arm with LEAD=0.5; sel14/sel15 only if the previous falsifies; max three arms, no retries; tref-indexed detector with injected positive control + negative control.
- 19:20:51 Task 9: pre-registration committed (b969269, TASK9_PREREG.md) BEFORE arming; sel13 arm launched as beatcap2-sel13-192051 (LEAD=0.5, out beatcap/20260902_192051_sel13). Implementer parked; controller watching the unit.
- 19:26:12 beatcap2-sel13-192051 SUCCESS: TRIGGER 19:23:40 at +40 s (errps=63602), mid.bin credited (pre=rung 6299 word), onset.bin credited (pre=0xD8A04817 = rung 6240 word → burst already on when the onset window opened; transition may be missed at the 1.1 s window). Observation [silicon]: three arms, three triggers at +40 s, errps 63600/63602/63602, mid pre-read rung 6299 each time — beat appears phase-locked to the arm. Implementer resumed for controls + scoring.
- 19:36:32 Task 9: sel13 verdict FALSIFIED (34d22e6, §83; detector had one revision + controls — see §83); per the stop rule the sel14 arm launched as beatcap2-sel14-193632. Controller watching.
- Ruling (19:40): sel13/14/15 records carry no data word, so a re-anchor event can be placed inside a window only by timing (errps.csv bins + journal timestamps). The sel13 onset window opened with the burst already on (pre-read rung 6240) while the sel6 arm put the onset 0.5 s later — burst phase jitters between arms. "FALSIFIED" is earned only for a window in which an event (onset / in-burst rung step / return-to-quiet) is placed with evidence; otherwise "phase continuous in the displaced state; UNINFORMATIVE for the transition". Implementer told to reconstruct the timeline for every sel13/sel14 window, re-word §83 and the 34d22e6 claim, check for the return event, and HOLD before any sel15 arm. Cost if wrong: one extra reporting round; cost of not ruling: a null on an empty window presented as a falsification.
- 19:41:38 beatcap2-sel14-193632 SUCCESS: TRIGGER 19:39:20 at +40 s (errps=63600; fourth arm, fourth +40 s trigger), mid.bin credited (pre=rung 6299), onset.bin credited (pre AND post golden — window opened before the burst; onset-inside-window undetermined until the timeline is built). Implementer resumed with the timeline ruling; HOLD before sel15.
Task 9 (stage report, commits b969269..4919290): sel13 = FALSIFIED for the return-to-quiet event (rung→golden bracket inside both windows, zero countReg mean-shift events) / UNINFORMATIVE for the quiet→rung onset; sel14 = UNINFORMATIVE because its detector used raw record-index framing (violates the tref-indexing ruling) and failed its own positive control at the ~1-in-2049 drop rate. Original sel13 overclaim (34d22e6) corrected in 7d84140.
- Ruling (20:12): (1) sel14 detector revision 1 — tref-indexed, drop-aware; re-score the EXISTING captures, no new arm. (2) sel15 = the third and last arm, pre-registered in a displaced-state LEVEL form (FIFO occupancy push−pop in-burst vs quiet), which does not need the transition inside the window; transition detector kept as secondary. Cost if wrong: one arm spent on a level that turns out not decodable (UNINFORMATIVE), within the approved three-arm budget.
- 19:58:19 Task 9: sel14 detector revision (9c2b7be) → CONFIRMED on existing captures (pending review); sel15 pre-registered (4aa87f2, LEVEL form) and the third arm launched as beatcap2-sel15-195819. Monitor armed.
- Ruling (20:02) on sel14 "CONFIRMED": sel14 = Delay8 = the interpolated SAMPLE stream (data content). A frame-correlation dip there LOCATES a re-anchor event inside the window and shows the displacement is present at the interpolator output; it does NOT show the buffer is the mover (transparent-stage logic; frame-identity limit makes a shift visible only at its boundary). Earned wording: "displacement present at sel14; event located in-window; mover at or upstream of the interpolator output; not the interpolator phase (sel13)". Only an occupancy LEVEL change at sel15 would implicate the FIFO itself. Implementer instructed (addendum to §84, §85, report). By-product: the tref-indexed frame-correlation detector places events inside full-rate windows.
- 20:04:18 beatcap2-sel15-195819 SUCCESS: mid.bin credited (pre=rung 6299), onset.bin credited (pre=rung 6240, burst already on at window open). Third and last Task 9 arm done; six arms on 148 today, all ARM_OK. Implementer resumed for LEVEL scoring + §85 + §84 addendum.
Task 9: implementer DONE_WITH_CONCERNS (b969269..5a905e2, 18 unit tests). Verdicts: sel13 FALSIFIED for the return event / UNINFORMATIVE for the onset; sel14 "displacement present, event located in-window" (mover at or upstream of the interpolator output, not the phase loop); sel15 LEVEL form FALSIFIED (occupancy push−pop constant =1, zero variance, both windows). Three arms used. Task review dispatched (sonnet).
Task 9: task review (sonnet) = Needs fixes, write-up only. Reproduced all numbers (sel13 0 events; sel14 dips 1112-1115 / 858 with overlap in-band → not drop artefacts; sel15 occupancy 1 on 16.7 M clean samples, push/pop sweep 0-31 → live), rails clean, prereg-before-arm by timestamp, sel14 wording verbatim. Two Important: (1) capTAP post-reads are taken after the 512 MB copy, so brackets span window+copy (~13-14 s) not the 1.1 s window — uncaveated; (2) sel15 occupancy observed range is {0,1} (1 at every clean sample) — LEVEL form's diagnostic reach not stated.
- Ruling: (1) sel13 verdict → "FALSIFIED [inferred: event placement transferred from sel14's in-window location under the six-arm phase-lock observation]; phase continuous through the whole window [silicon]". (2) sel15 → "FALSIFIED under the LEVEL form [silicon]; interpretive limit: occupancy pinned at 1, so an occupancy-based mover is excluded but a re-anchor in the FIFO control/pacing path is not". Fix round 1/5 dispatched (resume implementer).
Task 9: fix round 1 commit fbd8261 (bracket-width caveat with journal timestamps; sel13 verdict re-labelled inferred via cross-arm transfer; sel15 {0,1} range + interpretive limit). Scoped re-review dispatched (haiku).
Task 9: fix round 1/5 (2 addressed, 0 open; commit fbd8261).
Task 9: complete (commits b969269..fbd8261; task review + re-review clean). Three arms; verdicts as ruled: sel13 FALSIFIED [inferred placement]/null [silicon]; sel14 displacement present + event located [silicon]; sel15 LEVEL FALSIFIED [silicon] with interpretive limit.
ALL 9 TASKS COMPLETE. Final whole-branch review dispatched (fable) on review-final-80fd501..fbd8261.filtered.diff.
FINAL whole-branch review (fable) = Needs fixes — instrument, rails and rig procedure sound; 34 tests pass. Findings: (1) ddrcap2_beat_analysis uses P=12333 for sel6 frames_by_index but the demod-marker period at sel6 is exactly 12320 → §82 drop table is a constant artefact; three-way check actually PASSES. (2) Both sel6 captures hold an unreported ~1250-frame d_data=None episode. (3) NEW [silicon]: 9/9 d_data transitions in both sel6 captures are preceded by exactly one record by an EXTRA Transmitter_txFrameStart pulse (ch2 bit 14) with the regular TX cadence intact — the strongest un-excluded alternative: the displacement originates on the TX/loopback side and every RX tap is transparent. (4) §83 "phase loop exonerated" overclaimed: the revised detector tests sustained rate changes, not one-shot steps. (5) task9 detector invocations uncommitted. (6) doc: spec sel13/14 strobe enb_1_2_0_gated vs injector enb_1_2_0; tOff modulus is 12333 not 12320.
- Ruling (retroactive, recorded now): Tier-2 tOff "arm-settle transient" control was substituted by cross-arm d0 difference (26/12314/12323, pairwise different) — the weaker "field is not an instrument constant" claim; accepted, stated as such. The spec §6 sel12 secondary arm (operator priority #1) was made P1-only by the plan and not run under P2 — accepted for this plan; it remains open for the operator.
- ONE final fix dispatch (sonnet, fresh) per brief final-fix-brief.md: constant + §82/§83 addenda + TX-marker scanner with controls + reproducible task9 driver. NO arm. The sel8 (TX output) arm recommended by the review is a NEW experiment → operator decision, not launched.
Final fix round: commits e82e6d4..64d24e9 (58 tests). P2 holds with the measured 12320 period; task9 JSONs reproduce byte-identical from task9_run.py; TX-marker coincidence re-derived by ddrcap2_txmark_scan.py with controls: 7/12 overall = 7/7 DEPARTURES (0→rung, rung→None, 0→None) preceded by exactly one record by an extra TX frame-start pulse, 0/5 RETURNS (the reviewer's 9/9 counted one return 12,321 records away). Full-rate captures (sel13/14/15): 0 extra pulses in any of six captures, including around sel14's located events — open discrepancy (drop loss of a 1-record pulse, or the located sel14 events are returns). Scoped re-review dispatched (sonnet).
Final re-review (sonnet): A/B1-3/C/D/E ADDRESSED and reproduced; one Important residual — txmark scanner positive control tautological. Controller fixed it directly (commit above): real control = modal regular-pulse→demod-mark offset ≥99 %; sel6 PASS (48, 100 %), all six full-rate captures FAIL (246 at 87-89 %) → full-rate extra-pulse counts UNINFORMATIVE; sel6 7/7 departures stands. Residual closed. Deferred (Minor): transition_score/score_onset_window duplication.
