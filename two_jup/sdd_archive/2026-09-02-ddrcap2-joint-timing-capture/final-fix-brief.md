# Final-review fix round (ONE dispatch) — write-up, one constant, reproducibility. NO arm, NO board contact.

Repo /mnt/onetb/scratch/qpsk-jupiter-modem, branch per-under-1pct-2026-07. Captures on disk: two_jup/beatcap/20260902_185552_sel6/{onset,mid}.bin (sel6), *_sel13/*_sel14/*_sel15. Decoder: two_jup/ddrcap2_decode.py (ch2 bit15 = demod mark, bit14 = TX frame-start mark carried as `mark_fec`, bits13:0 = toff; ch3 slot/side sidecar; tref valid on slot-1 records).

## A. Frame period constant at sel6 (Important)
`two_jup/ddrcap2_beat_analysis.py` uses P=12333 for `frames_by_index` (lines ~15, ~45). On silicon the sel6 demod-marker period is exactly 12320 records (5444/5444 full frames of onset.bin; the 13 preamble symbols do not reach the demod-input tap). Fix: compute `frames_by_index` from the measured modal demod-marker gap of the capture (fallback 12320 for sel6), keep 12333 only where it is true (sel12-15 full-rate taps and the tref wrap detection). Update `two_jup/tests/test_ddrcap2_beat_analysis.py` so the synthetic fixtures are built at the same period the code measures (parametrise the period; the three-way test must still be meaningful, not trivially true). Re-run both sel6 captures and commit regenerated `*_verdict.json` (verdict word must remain P2; report if it does not).

## B. §82 addendum in two_jup/SESSION_20260830_AUTONOMOUS.md (Critical write-up) — addendum, do not rewrite
1. Correct the frame-count paragraph/table (the "0.066 %/0.084 % drop, 2.2x/2.8x" numbers were a P=12333 vs 12320 artefact, not a measurement): with P=12320 the three counts agree within partial-frame slack (marker 5445 / tref 5445 / index ~5447), and the pre-registered three-way check PASSES. Label [silicon].
2. Report the unreported episode: in both sel6 captures ~1250 frames (23 %) have d_data = None (the 16-decision word maps to no offset under any QPSK rotation 0/90/180/270). Give the record bounds (reviewer measured onset.bin ≈ records 51.37M–66.83M, mid.bin ≈ 40.41M–56.02M — re-derive and state your own numbers), and list every d_data transition in both files (0→rung, rung→None, None→0) with record index. Label [silicon]. State that the P2 verdict concerns the 0→rung step and that tOff is unchanged through the None episode too (verify and state).
3. tOff modulus: tOff is latched from `timing_Reference`, a mod-12333 counter (jupiter_240k5_byte/.../Peak_Search.v lines ~89-107 — cite the actual path you find); the §82 residual framing must use mod 12333 (26 → +26, 12314 → −19, 12323 → −10, pairwise different; conclusion unchanged). Correct the addendum accordingly.
4. TX-marker coincidence [silicon, observation — do not interpret beyond what is measured]: the reviewer found that every d_data transition in both captures (9/9) is preceded by exactly one record by an anomalous EXTRA `Transmitter_txFrameStart` pulse (ch2 bit 14), while the regular TX cadence (48 records before the demod mark, period 12320) continues undisturbed (onset.bin extra pulses at 10306769, 23432845, 38579674, 51372965, 66820094; transitions at 10306770, 23432846, 38579675, 51372966, 66832415). Re-derive independently with a small committed script (`two_jup/ddrcap2_txmark_scan.py`: lists regular-cadence TX pulses vs extra pulses, and for each d_data transition the distance to the nearest extra pulse), run it on both sel6 captures, commit its JSON output, and state the result with the 9/9 (or whatever you find) count. Also run it on the sel13/14/15 captures (they carry ch2 too) and report whether extra pulses exist there and where they sit relative to the sel14 located events (frames 1112-1115 onset, 858 mid). Positive control for the scanner: the regular cadence must be found at period 12320 (sel6) / 49332 (full-rate) with the expected count; negative control: a synthetic stream with no extra pulses reports zero.

## C. §83 addendum (Critical write-up)
The revised sel13 detector's positive control is a SUSTAINED +40-count-per-symbol offset (a timing-rate change); a one-shot phase step of the size a symbol re-anchor would produce in the NCO accumulator lies inside the ~6 %-of-symbols >64-count jitter the windowed statistic averages away. Reword: earned [silicon] = "no sustained change in the interpolator's mean phase increment across the window"; NOT earned = "the mover is not the interpolator's phase control loop" — strike/qualify that sentence (§83 and any echo in §84/§85/TASK9_PREREG.md/the Task 9 report) and state that the underflow-cadence alternative named in the pre-registration was not scored.

## D. Reproducibility (Important)
`task9_sel13_detector.py`, `task9_sel14_detector.py`, `task9_sel15_detector.py` have no `main`; the committed `*_analysis.json` came from uncommitted invocations (countReg extraction `I & 0x7FF`, quiet-stretch selection, injection points). Add a committed driver (`two_jup/task9_run.py` with subcommands per selector, or a `main` in each) that reproduces every committed JSON from the .bin files with the exact parameters, run it, and confirm byte-identical or explain any difference in the report.

## E. Minor (do if cheap, otherwise list as deferred)
- `task9_sel15_detector.py:22` unused import `pair_baseline`; inner re-imports at ~:90; `transition_score` duplicates `score_onset_window` — parametrise if a 10-line change.
- `ddrcap2_beat_analysis.py:37` unused `offs`.

Rails: no board contact of any kind; no arm; never touch 146; `git commit -s` per logical change, local only, no .bin; trailer:
Claude-Session: https://claude.ai/code/session_019NZLGbxoaPRkDnKFMPiBrq
Report to /mnt/onetb/scratch/qpsk-jupiter-modem/.superpowers/sdd/2026-09-02-ddrcap2-joint-timing-capture/final-fix-report.md. Return ONLY: status, commit hashes, test summary, the TX-marker scan headline numbers (sel6 and full-rate), P2 still holds yes/no, deferred items.
