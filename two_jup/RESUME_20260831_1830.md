# RESUME STATE — 2026-08-31 18:30. Supersedes RESUME_20260831_1700.md

Read this, then `SESSION_20260830_AUTONOMOUS.md` §0 and §25–§28 (newest findings, all provenance
labelled). Branch `per-under-1pct-2026-07`, committed locally, not pushed.

---
## 1. WHAT IS RUNNING

| agent | id | doing |
|---|---|---|
| DDR capture T2 (RTL) | `a58e27668a23ca68e` | **fix round 1** — 5 review findings |
| DDR capture T3 (BD) | `abbca9ffb12d5a5a1` | tcl written, sel3/sel4 sim check, validation DEFERRED |
| governor watchdog | Monitor `b8fb3qn38` | 10-min liveness, hourly heartbeat |

Rig is FREE, no `RIG_HALT`, both boards up. **No hardware work is in flight and none is queued
until the T2 fix lands** — a flash before then would burn ~54 min of synthesis on an image whose
capture ports are optimised away.

---
## 2. THE ONE THING THAT MUST NOT BE FORGOTTEN

**T2's injector does not patch `hdl/TxRxCompo_ip_dut.v` or `component.xml`.** The five ddrcap
ports therefore terminate unconnected below the IP boundary and are silently optimised away —
the build succeeds, the flash succeeds, and the capture returns nothing. Task 3's BD wiring
targets `TxRxCompo_ip_0/ddrcap_*` pins that **do not exist yet**. Do not build, do not flash,
do not validate the BD until the T2 fix round is verified against a real archive.

---
## 3. METHOD RULES — all four earned by a wrong answer this week

1. **§0 positive control.** No witness may produce a null until shown capable of a non-null.
   Caught: the DBGCAP counters (§20/§22), `pgrep -c` in govwatch, govwatch's compute detector
   (watched a binary name that never existed), and the injector's `main()` returning success
   with zero zips patched.
2. **Anchor boundary (§25).** Witnesses with different capture anchors are not comparable; a
   chain cannot be bisected across one. "TXCAP clean, AGC out deviates" does NOT localise
   between them.
3. **Shift tests state their overlap and their null (§28).** A 2-bit-overlap test reported
   28/51 "matches" against a chance expectation of 68 — noise reading as a result.
4. **Partial-fill reads (§26).** A capture word with a zero top byte is the register sampled
   mid-fill. Exclude before scoring. `0x00000000` is excluded but NOT explained.

---
## 4. WHERE THE INVESTIGATION STANDS

### Established on silicon
- Transmitter **exonerated** (TXCAP 100 % golden quiet AND burst).
- All four RX taps, DEMODCAP and all three FEC capture points **deviate** ~46–50 % in bursts.
- **The burst is a deterministic recurring state, not noise (§25).** 7–11 distinct non-golden
  words per witness; cap_in/cap_deint 100 % recurring. Recurrence **298,945 ± 25 frames**
  (~242.6 s), consistent across all four taps to 0.02 %.
- **It reproduces across reset cycles (§27).** Two runs 3 h 42 m apart share 86–100 % of their
  burst words. Rules out thermal/RF/stochastic causes.
- **Anchor-only window displacement excluded for shifts ≤ 8 symbols (§28).**
- Start-pulse hypothesis DEAD; BIST scores only the first 120 of 2240 bits; SSI path exonerated;
  beat phase-locked to reset.

### ANSWERED 19:00 — §31
**POSITION, not values.** During bursts the demod-input capture window is anchored **≈561–585
symbols later in the frame**, and the values there are **bit-exact** matches to simulation.
Positive-controlled (golden `0xBCF94856` reproduced at all 22 frames), 5 of 8 silicon burst words
found at single fixed offsets, null 5·10⁻⁴. Confirms the 2026-08-20 ILA reading.

**ACT ON THIS BEFORE T4/T6 BUILD: T6's lag search must span a FULL FRAME** (12,320 strobes /
1,120 symbols). A ±few-sample window would place the answer outside the instrument's range.

Still open from §31: three of eight burst words match **no** within-frame offset (coverage was
complete, so they need their own explanation); and this is a **per-tap** result — the
FEC-anchored `cap_*` witnesses were not swept and §25's anchor rule forbids carrying it across.

### Open
2. **Where the deviation enters.** Cannot be bisected from current witnesses — rule 2 above.
3. Selector 7 (TX modulator out) reads identically zero — declared DEAD, not shipped.
4. Selectors 3/4/8 sample on a borrowed valid; sel3/4 beat count differs from sel5's native
   valid by ~14 %. Being fixed in T2 fix round; T3 measuring independently.

### Withdrawn — do not cite
§20 (every RX stage invariant) — the counters were dead. Any localisation built on it is void.
Any run that swept `iq_debug_mux` during the poll has meaningless DBGCAP columns.

---
## 5. NEXT STEPS
1. T2 fix round verified (esp. dut.v + component.xml against a real archive) → T3 validates BD.
2. T4 build on hdl-dev-2 (~54 min, source-only resynth).
3. T5 flash 148, full rails, no retry loop + **4-part capture-path positive control**.
4. T6 capture across bursts, lag scoring → settles position vs values.

Registers, images and scripts: see RESUME_20260831.md §5 (still accurate).
