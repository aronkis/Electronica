# RESUME STATE — 2026-08-31 ~17:00. Supersedes RESUME_20260831.md

Read this first, then `two_jup/SESSION_20260830_AUTONOMOUS.md` (§0 standing rule, §5, §8, §20–§24).
Everything is committed on `per-under-1pct-2026-07`, local only, not pushed.

## 1. THE HEADLINE RESULT

**The transmitter is exonerated on silicon.** Agent MEASURE, image `0f203cf887d3`, run
`two_jup/measure/20260831_113104`, silicon positive control PASS:

| witness | quiet % golden | burst % golden | verdict |
|---|---|---|---|
| TXCAP (transmitter output) | 100.0 | **100.0** | MEASURED-CLEAN |
| DEMODCAP (demod slice/serialise) | 100.0 | **48.0** | MEASURED-DEVIATES |

**§20 IS COLLAPSED, NOT MERELY WITHDRAWN.** FINAL's 0x20C positive control returned plausible,
tap-dependent, non-zero captures matching sim byte-for-byte (tap3 `0xBCF94856`, tap0 `0x121D8572`,
TXCAP `0xF00003FF`, DEMODCAP `0x8F9ED095`) with the counter incrementing exactly once per frame
(6228 in 5 s at 1245.6 f/s). §20's all-zero counters were therefore a DEAD INSTRUMENT, and §24's
golden-constancy measurement of the same tap found it DEVIATES (99.7 % → 45.0 %) — the opposite of
§20's claim. Do not cite §20.

**The open question** is position-versus-values: TXCAP is anchored on the TRANSMITTER's frame start
and is 100 % golden; every witness anchored on the DEMODULATOR's frame marker deviates (~46–50 %).
That is consistent with the demod's frame alignment moving while the data stays bit-identical.

## 2. CAMPAIGN IN FLIGHT: DDR raw capture (6 tasks)

Spec `docs/superpowers/specs/2026-08-31-ddr-raw-capture-design.md`,
plan `docs/superpowers/plans/2026-08-31-ddr-raw-capture.md`.

Route any block's raw I/Q into DDR via the **idle** `util_adc_2_pack` → `axi_adrv9001_rx2_dma` → DDR
chain (RX2 is never `rf_enabled`; its packer channels 2/3 are tied to GND). Channel allocation, operator
decision: **ch0/ch1 = selected block I/Q, ch2 = demod frame marker, ch3 = FEC frame marker.**
Markers are FULL 16-bit words (`0x7FFF`/`0x0000`), never packed — a dropped strobe reads as a lag jump
and would fake the result under test.

| task | gate it protects | state |
|---|---|---|
| T1 characterise the RX1 "ramp" | the 2 h BD build | **COMPLETE — PROCEED** |
| T2 RTL: 12-way mux, postCoarseFreq port, dual markers, bit packing | T3 | **RUNNING** |
| T3 BD change: repoint util_adc_2_pack, clock from adc_1_clk | T4 | queued |
| T4 full MATLAB/BD build on hdl-dev-2 (~2 h) | the flash | queued |
| T5 flash + 4-part capture-path positive control | any interpretation | queued |
| T6 capture ~200 frames into a predicted burst, score marker-vs-data lag | — | queued |

**T1 verdict detail:** RX1 IQ is a free-running counter (Q steps +256/+1 alternately, net +257 per two
samples, clean wrap `7967 → 7936 → 0,1,257,258`). Decisive point: `iio_readdev` exited 0 with the full
262,144 bytes, so **the packer and DMAC work end to end** — they are faithfully carrying a test pattern
injected at the ADC interface. The fault is ADC-side, so feeding the packer from internal fabric bypasses
it. Near-miss: the draft probe used `xxd`, which is NOT on the board and silently returned empty output;
that would have read as "no data" and killed a valid design.

**T6 is where the campaign's question gets answered:** lag constant ⇒ values changed; lag jumps ⇒ marker
moved; demod and FEC markers diverging from each other ⇒ shift localised between demod and FEC decoder.

## 3. THINGS THE NEXT SESSION MUST NOT UNDO

- **Selector 7 warning.** Selector 7 is the TX-side `QPSKConstellationPoints` — the exact signal that read
  identically ZERO and killed TX-INT's modulator witness. T2 was pre-warned to expect trouble there and to
  DECLARE IT DEAD rather than work around it. 11 good selectors + 1 declared dead is an acceptable T2
  outcome; a selector shipped without a demonstrated positive control is not.
- **Three golden values being protected.** Any new image must still show DBGCAP tap3 `0xBCF94856`,
  TXCAP `0xF00003FF`, DEMODCAP `0x8F9ED095`. New instruments are ADDITIVE; never trade a working one away.
- **§0 positive control** — no witness produces a null until shown capable of a non-null. It has now
  stopped three agents from shipping (DBGCAP counters, RX-FE, TX-INT) and that is the system working.
- Register map: `0x10C` iq_debug_mux · `0x20C` capture · `0x210` counter (**unusable** — frame-8 reference
  latches during acquisition) · `0x13C/0x140/0x144` cap_in/deint/out · `0x208` fixctl (bits 0-4 fix arms,
  12 TXCAP, 13 DEMODCAP, 14-17 TX-INT taps, 20+ RX-FE) · `0x114` rx_input_select.
- Set `iq_debug_mux` **after** the `0x000` soft reset — the reset clears AXI write registers.

## 4. AGENTS THAT STOPPED CORRECTLY (do not re-run blindly)

- **RX-FE**: CFCAP architecturally unscoreable (4 quadrant rotations, period 16 frames, upstream of carrier
  resolution). RFCAP sim-gated clean but positive control NOT DEMONSTRATED — and it proved this by applying
  the same lever to DEMODCAP, a known content-sensitive witness, which also failed to move. So the FORCING
  MECHANISM does not reach TX content; `tx_data_source=1` byte-plane injection is not a usable lever.
- **TX-INT**: 3 of 4 taps validated with genuine positive controls (fixctl 14 Bit_Packetizer, 15 Scrambler,
  17 TX RRC). QPSK_Modulator `[16]` dead. Not pursued further because TXCAP already exonerated the TX.
- Also: `wrap_byte_ce` needs `clk_enable` driven explicitly or every frame counter silently reads zero.

## 5. RIG STATE

148 on `0f203cf887d3`, 146 on `ec414d2df8bc` (**never flashed, never touched**). Link up at shipped
defaults, `qpsk_tun` and `lock_watchdog` on both boards, sentinel running, no `RIG_HALT`, mutex free.
Arbiter (`two_jup/agents/rigmutex.sh`) is positively controlled — Task 7 observed it excluding.
Dashboard: **http://nemo.local:8090/chain.html**, `chaindash.service`.
Governor sweep: `bash two_jup/agents/sweep.sh` — silent + exit 0 when healthy.

## 6. NOT A KNOWN ISSUE

The operator asked about a "demod_marks doubling". **No such finding exists in this session's state.**
T2 (which introduces the dual markers) has not reported. If a doubling is later observed, note that a
doubled marker is exactly the failure mode §4.4 of the spec warns about — it would corrupt the lag
measurement — and it must be resolved before T6's result is believed.
