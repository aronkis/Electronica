# RESUME STATE — written 2026-08-31 07:57 for a Claude restart

Read this first, then `two_jup/SESSION_20260830_AUTONOMOUS.md` (full detail, §0–§24, provenance labels on
every claim). Campaign ledger: `two_jup/SINGLES_CAMPAIGN.md`. Everything is committed locally on branch
`per-under-1pct-2026-07` (not pushed).

---
## 1. SOMETHING IS RUNNING RIGHT NOW — do not take the rig until it finishes

`multitap.service` (systemd --user transient unit) started 07:49, output
`two_jup/multitap/20260831_074947/`. It is a **transient user unit and survives the Claude session ending**,
so it will complete on its own.

- What it does: 4 taps × 400 s dwell on 148, mode-1 internal digital loopback, then a **gated link restore
  at the shipped defaults** and restarts both watchdogs.
- It holds `RIG_LOCK` (present now) and `SENTINEL_STOP`; both are removed by its EXIT trap, after which
  `sentinelkeeper-004023.service` relaunches the sentinel within 2 min.
- Expected finish: **~08:25**. Progress: was on tap 0 at 07:51.

**On resume:** `systemctl --user is-active multitap`; when inactive, score with
```
cd /mnt/onetb/scratch/qpsk-jupiter-modem
python3 two_jup/score_multitap.py two_jup/multitap/20260831_074947/multitap.csv
```
Then confirm the rig came back: `ARM GATE PASS` in that run's `restore.log`, watchdogs/daemons on both
boards, `RIG_LOCK`/`SENTINEL_STOP` gone, sentinel process alive
(`pgrep -af "[d]elivery_sentinel.sh"` — use the bracket idiom, a plain pattern self-matches).

---
## 2. STANDING RULES (operator, this window)

- **Flash authorised on 148 as often as needed.** Full rails every time: restore point banked and named,
  readback verify, two-pass health gate, auto-rollback. **NO RETRY LOOP** — a flash that fails *after
  touching the board* means roll back and stop rig work. (A refusal at the pre-flash precondition, before
  anything is staged, is not a failed flash; that happened once and relaunching with a corrected path was
  fine.)
- **146 is never to be flashed or touched**, except by the standard gated restore.
- **§0 POSITIVE CONTROL RULE (the most reusable output of this week):** no witness may produce a null
  result until it has been demonstrated capable of a non-null one. A clean-run health gate is NOT a
  positive control — a dead counter passes every clean-run gate ever written. It has already caught two
  instruments in this campaign, one of them mine.
- Priorities: a witness that passes both its health gate and a positive control → TX-vs-RX verdict →
  bisect downstream. **The header discrepancy is CLOSED (§5) — do not reopen. The start-pulse hypothesis
  is DEAD — do not revive.** No new candidates.
- Keep `SESSION_20260830_AUTONOMOUS.md` current with provenance labels, written to be read cold.

---
## 3. WHERE THE INVESTIGATION STANDS

### Established on silicon
- **Start-pulse hypothesis DEAD.** 1.0000 startIn / vitReset / startOut per frame through 801k bit errors,
  in both mode 1 and mode 2. Also kills "trellis restarted mid-stream". (§8)
- **The BIST comparator scores only the first 120 of 2240 bits** (`tmp_3 <= 120`, 120-entry golden array,
  `msgLen=2240`). Every bit-error number in this campaign is frame-start damage. This **dissolved** the
  0.24 % vs 0.09 % header discrepancy — its premise was false. (§5)
- **SSI/LVDS path exonerated** for beat and floor: mode 1 ≡ mode 2, burst sizes identical to the digit in
  the same session. (§4, §9)
- **The beat is deterministic and phase-locked to reset**, ~239.5 s two-state cycle, reproduced across six
  captures, three days, four arms, two signal paths, including alternation parity (BIG sml BIG sml BIG).
- **`FecCapture` is the one instrument that has been reliable throughout**: cap_in 0x13C, cap_deint 0x140,
  cap_out 0x144 (golden 0x04922282). 99.5–100 % golden in quiet, 42–52 % in bursts, on every run.
- **Tap 3 (constellation = demod INPUT) deviates in ~55 % of burst seconds** (99.7 % → 45.0 % golden),
  properly measured, tap verified applied, scored by golden-constancy. (§24)

### Withdrawn — do not cite
- **§20 is WRONG and withdrawn** (§22, §24). It claimed every RX stage up to the demod input was invariant
  through bursts. The counters were dead (all-zero across 400 samples and three bursts; the same RTL in
  another image incremented continuously), and §24 measured the opposite. Any localisation built on it,
  including "TX/AGC/timing/carrier exonerated", is void.
- Any run that **swept `iq_debug_mux` during the poll** has meaningless DBGCAP columns (§21, §22 re-run):
  the mismatch counter has ONE global reference latched at frame 8 against whatever tap was selected then.

### Open
1. **TX vs RX.** Still unsettled. TXCAP exists in the flashed image but its frame-8 mismatch counter is
   unusable; it must be scored by **capture-constancy** — hold `fixctl[12]=1` for a whole run and score
   `0x20C` the way cap_in is scored.
2. **How far upstream the deviation goes.** The running sweep answers this for AGC out, postSymbolSync,
   postCarrierSync, constellation. An accidental tap-0 measurement (run `20260831_071043`, which believed
   it was on tap 3) scored **AGC out 99.7 % quiet → 47.6 % burst** — if confirmed, the error is present at
   the RX chain input and the transmitter/loopback path is implicated.
3. **Values vs position** at the demod output: DEMODCAP (demod-anchored) vs cap_in (FEC-anchored), same
   bits. Needs capture-constancy scoring too. The 2026-08-20 ILA reported "value-perfect coded bits at a
   shifted sequence position at/before the FEC input", which favours a position shift.

---
## 4. TWO INSTRUMENT FAULTS FOUND 2026-08-31 (§23) — avoid re-tripping them
1. **The `0x000` soft reset clears the AXI write registers.** A tap selected *before* the reset is wiped.
   Run `20260831_071043` read tap 0's pattern throughout while believing it was on tap 3. **Set
   `iq_debug_mux` AFTER the arm**, and verify by reading `0x20C` (tap 0 = 0x121D8572, tap 3 = 0xBCF94856,
   TXCAP = 0xF00003FF, DEMODCAP = 0x8F9ED095 — all match simulation byte-for-byte).
2. **The frame-8 reference is latched during acquisition** and is a bad baseline. DEMODCAP showed 3.45
   mismatches/s in quiet while cap_in — the same bits by golden-constancy — was 100 % golden.
   **Use golden-constancy on `0x20C`; ignore the `0x210` mismatch counters.**

The counters ARE alive (positive control: parked tap mismatches once per frame exactly, 6228 in 5 s at
1245.6 f/s) — they are just referenced to the wrong thing.

---
## 5. ASSETS

**Images banked in `boot_known_good/`** (148 currently runs `0f203cf887d3`):

| file | md5 | contents |
|---|---|---|
| `BOOT.BIN.148.pdwit.786dce9fafc8` | 786dce9fafc8 | beat witness; the pre-session image |
| `BOOT.BIN.148.stagesig.0ce3caa2d00b` | 0ce3caa2d00b | stage signatures (unbounded) — instrument failed |
| `BOOT.BIN.148.stagesig2.ed44769ed7aa` | ed44769ed7aa | stage signatures (bounded) — instrument failed |
| `BOOT.BIN.148.stagesig3.1977cbb39877` | 1977cbb39877 | bounded + sign bits, never flashed |
| `BOOT.BIN.148.dbgcap.0daa708f5e79` | 0daa708f5e79 | DBGCAP — **counters dead in this image** |
| `BOOT.BIN.148.combined.4abe8c4563a2` | 4abe8c4563a2 | DBGCAP + TXCAP |
| `BOOT.BIN.148.final.0f203cf887d3` | **0f203cf887d3** | **DBGCAP + TXCAP + DEMODCAP — ON 148 NOW** |

**Registers** (word address × 4; mapping self-checked against known 0x104/0x108/0x114/0x20C/0x210):
`0x10C` iq_debug_mux (0 AGC out, 1 postSymbolSync, 2 postCarrierSync, 3 constellation) ·
`0x13C/0x140/0x144` cap_in/cap_deint/cap_out · `0x124` cnt_frame_start (FEC startIn) · `0x128`
cnt_vit_reset · `0x134` cnt_bist_start · `0x130` cnt_dec_bits (**saturates in ~200 s — void**) ·
`0x208` fixctl (bits 0–4 fix arms, **bit 12 = TXCAP readout, bit 13 = DEMODCAP readout**) ·
`0x20C` capture · `0x210` mismatch counter (unusable, see §23).

**Scripts** (all in `two_jup/`, syntax-checked): `multitap_run.sh` + `score_multitap.py` (capTAP
golden-constancy per tap — **the working method**) · `tap_run.sh` (single fixed tap) · `dbgcap_run.sh`,
`final_run.sh` (sweep taps — **superseded, their tap columns are void**) · `score_dbgcap.py`,
`score_stagesig.py`, `score_caploc.py`, `score_startcnt.py` · `skidfix/flash_148_stagesig.sh` +
`skidfix/run_flash_stagesig.sh` (full-rails flash; **invoke with an ABSOLUTE `BB` path** — a relative one
caused a precondition refusal).

**RTL injectors** in `two_jup/skidfix/`: `dbgcap_inject.py`, `txcap_inject.py`, `demodcap_inject.py`
(apply in that order), `stagesig_inject.py`/`stagesig3_inject.py` (superseded).
Patch **every** copy — 4 loose HDL copies **and both packaged zips** — Vivado synthesises the zip/ipshared
copy, not the loose file.

**Build hosts.** Local (12 cores, Vivado `/tools/Xilinx/2025.1`) and **hdl-dev-2 = 10.0.0.11**
(8 cores, Vivado `/opt/Xilinx/2025.1/Vivado`, builds in `~/qpsk-builds/`). hdl-dev-2 is **absent from
HOSTS.md** and should be added (canonical copy `picard:~/dev/infra/HOSTS.md`). Relocating a build tree
there needs path rewriting including an **ancestor root** `jupiter_byte_beatfix2_build` referenced by
`system.bd` for `mem_init_sys.txt` — which also means **the local builds depend on that tree still
existing; do not delete old build dirs.**

Source-only resynth (~50 min local, ~54 min hdl-dev-2) is enough for all of these — no BD change, no
MATLAB regeneration. The `beat_ila` in the image probes the **byte plane**, not the DSP chain.

---
## 6. IMMEDIATE NEXT STEPS ON RESUME
1. Wait for `multitap` to finish (~08:25), score it, confirm the rig restored.
2. If several taps deviate: run TXCAP capture-constancy (hold `fixctl[12]=1`, poll `0x20C`, 400 s) to
   settle TX vs RX with a properly-scored witness.
3. Then DEMODCAP capture-constancy (`fixctl[13]=1`) against cap_in for values-vs-position.
4. Keep the running summary current, with the §20 retraction and why, written to be read cold.
