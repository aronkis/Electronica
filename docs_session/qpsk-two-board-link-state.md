---
name: qpsk-two-board-link-state
description: two-board RF link — cal fix (link A 50%→~1%); WIDER carrier loop (CSBnXTsamp 0.018 = jupiter_cswide ddd23574) DEPLOYED+tested = NO HW improvement (1.21% vs cfc12 baseline 1.19%, BOTH 0 bursts/30 arms). Link A floor ~1.2% on-chip / 0.72% robust-re-demod = SNR/EVM-limited NOT receiver loops. tone-capture bursts intermittent (absent in 30-arm runs). data-aided + Tx-NCO + wider-loop ALL ruled out on HW. <0.01% needs signal-quality/link-budget not Rx-HDL. link B ~5.8% (ZedBoard Rx). Jupiter deployed=cfc12 baseline
metadata: 
  node_type: memory
  type: project
  originSessionId: f86fec00-4ecb-4ac1-88b1-40de1c8c86d6
---

Two-board FDD link (ZedBoard 10.0.0.128 0x43C00000 ↔ Jupiter 10.0.0.146 0x9D000000, in-FPGA-Tx, 1.92 MHz / 240 ksym; link A = Zed Tx 2.40→Jup Rx 2.40, link B = Jup Tx 2.45→Zed Rx 2.45).

ROOT CAUSE found 2026-06-25: the **ZedBoard ADRV9002 Tx ran UNCALIBRATED**. Its boot profile had `WarmBoot: Enabled` + `Navassa_CMOS_init_cals.bin`, so `adrv9002_init_cals_handle()` loaded STALE cached cals instead of running fresh ones; `lo_leakage/quadrature/close_loop_gain tracking_en=0`. Raw Tx LO leakage was a carrier holding ~93-100% of the power → Jupiter Rx locked to the carrier → link A ~50%, invariant to carrier(200/70MHz) and level. (Earlier mis-diagnoses ruled out, in order: integAvgLen, ZedBoard ZOH-Tx, Jupiter-Rx CFO acquisition, I/Q swap — all wrong; the ZedBoard Tx was already FIR since Jun-10.)

FIX (runtime, NO restart): reload the repo's `trx_examples/targeting/QPSKTxRxHDLExample/modem_profiles/cmos_1p92_mhz.{json,bin}` (it has `warmBootEnable:false` → fresh init-cal mask 0x1BE5F7) via the adi.ADRV9002 System object profile_config/stream_config (first write may throw "Interface tuning failed -5"; retry once), THEN enable tracking cals via the driver's state-sequenced sysfs (`out_voltage0_lo_leakage_tracking_en=1`, `_quadrature_tracking_en=1`, Rx qec/bbdc). Carrier collapsed ~35 dB (rms 6107→11, crest 0.02→9.3 dB); real x⁴ QPSK confirmed.

RESULT: link A 50%→**~0.81%** (Jupiter BIST is a ONE-SHOT window ~209 pkt/arm — re-arm for each; 0.81% repeatable). Link B **~6%** (golden host-Tx over link B ≈ in-FPGA-Tx, so it's the RF-link/Rx residual, NOT the modulator; best at Jup Tx atten -20 → 4.5%). Link A's 0.81% is a CROSS-BOARD SUSTAINED residual (~1 bit per 120-bit packet, exactly 204 err/209 pkt every reset = deterministic): FLAT vs ZedBoard Tx power (-10..0) AND Jupiter Rx gain (caps 34dB, AGC same) = level-independent (signal ~-69 dBFS); FLAT @2.40/200/70 MHz (35x CFO sweep) = carrier/CFO-independent; both ADRV9002 fully calibrated; NOT an acquisition transient. ~10x above the 7-8 dB Eb/N0 SNR floor. Single-board loops clean (Zed self 0.00064%, Jup host-golden 0%). Host IQ-capture CANNOT resolve the per-bit structure — host-Rx self-noise ~1-9% at Eb/N0~7dB swamps 0.8% (Jupiter BIST is one-shot, ~209 pkt/arm). Pure model-based sim (matlab -batch) running to reproduce+tune the residual or declare it a hardware artifact.

SPECTRUM FINDING 2026-06-25 (PSD-only, /mnt/onetb/scratch/linkA_spec/): post-cal Jupiter-Rx PSD of the armed ZedBoard Tx shows the cal fix did NOT fully kill the carrier. DOMINANT in-band artifact = a single CW carrier/LO-leakage tone at **-26.1 kHz** (== the inter-board CFO -26191 Hz), CW-like (~234 Hz wide, 2 bins @117Hz res), strongly ASYMMETRIC (mirror +26kHz is -55 dBc => NOT an IQ image). Level: **-2.7 dBc peak / -0.4 dBc integrated** vs total in-band QPSK power; 35.9 dB over local in-band floor, 45.2 dB over broadband noise. CONTROL proved origin = ZedBoard Tx RF: muting via Tx ENSM=calibrated (RF off) made spur vanish (spur-over-floor 35.9→0.7 dB); -40dB digital atten did NOT kill it (LO leakage added after digital attenuator => atten-independent, matches the level-flat-vs-Tx-power BER observation). Other artifacts negligible: true-DC spur buried <-50 dBc; in-band ripple std ~5.3 dB (dominated by the spur, else passband tracks ideal QPSK); tilt -3.4 dB; 99% OBW 250 kHz vs ideal 307 kHz (NO spectral regrowth — earlier "1317kHz regrowth" was noise-floor contamination). Link is SNR-limited: Jupiter Rx gain hard-capped 34 dB even in manual/spi mode; even at ZedBoard Tx atten 0dB in-band SNR only ~13-18 dB (host-Rx noise floor). fs at Jupiter Rx = **1.92 MHz** confirmed from device (in_voltage0_sampling_frequency=1920000), NOT 15.36 — the OLD linkA_iq/linkA_spectrum.m used wrong fs=15.36 (8x). BER plausibility: naive (spur-as-noise) at -0.4 dBc => ~9% — but the Rx carrier-sync ACQUIRES & removes the -26kHz CFO (prior: residual 13.5Hz, no cycle slips), so the CW tone is largely nulled; the deterministic 0.8% is consistent with RESIDUAL carrier leakage bleeding through the carrier loop (a fixed-frequency CW pilot is the textbook cause of a deterministic phase-bias BER floor that the matched self-loop never sees). NEXT to confirm: further reduce ZedBoard Tx LO leakage (re-run/extend lo_leakage init+tracking cal, or external cal) and re-measure both the -26kHz spur dBc AND the HW-BIST BER — they should fall together. Files: linkA_spec2.m/.png (overlay), linkA_spur2.m/.png (hi-res spur), linkA_ctrl.m (on/off/hardmute controls).

GOAL (active /goal): golden tests → tune Rx/Tx HDL IP → <0.01% both directions, deep debug + HW verify, don't ask unless stuck. SUPPRESSION SWEEP 2026-06-25 (linkA_spec/SUPPRESSION_RESULTS.md): all runtime LO-leakage levers FAILED to move the spur (tracking soak ~6min, init-cal profile reload, initial_calibrations=run @2.40GHz — all stayed ~-0.4 dBc). A Tx-atten sweep moved the spur 2.5 dB (-0.22→-2.77 dBc) but BER stayed 0.8-1.0% => SPUR DECOUPLED FROM BER. So the 0.81% floor is CARRIER-SYNC-RESIDUAL-limited (Jupiter carrier loop nulls the CW tone at the CFO to a fixed residual, indep of spur level), NOT the raw spur dBc. External LOL cal (the only true spur fix) needs a profile REBUILD (txExtLoopBackForInitCal=1, txExtLoopBackType=1|2) + a physical ZedBoard Tx→Rx RF loopback (none wired); the Tx-LOL DC actuator has no userspace _Set — no runtime null. NEXT (model-based, informed): add the CW tone at the CFO (-0.4 dBc, == the QPSK rms) to the linkA_sim model — the earlier 'model clean at 0%' was because it LACKED this tone — reproduce the 0.8% + the spur/BER decoupling, then find the modem fix (DC-blocker on symbols AFTER carrier sync [tone maps to DC post-loop] / CFO notch / decision-directed carrier loop / Tx baseband IF offset to separate QPSK from the tone), then build+deploy+verify. MODEL RESULT 2026-06-25 CONFIRMED: a CW tone at the CFO (~-10 to -14 dBc; -0.4 destroys the link) reproduces the 0.8% + the decoupling; WINNING fix = a Tx baseband IF offset (~+20 kHz) → 0.000% @high-SNR, robust, no clean-link harm (Rx CFO loop absorbs it, no Rx change). RUNTIME NCO FAILED: enabling the ADRV9002 Tx NCO (profile iqdmDucMode 0→2 = MODE2, reload) exposed out_voltage0_nco_frequency (no longer ENOTSUPP) but it had ZERO spectral effect + zero BER change — the iqdmDUC NCO sits in the DISABLED narrowband Tx datapath (txNbIntTop); the in-FPGA QPSK flows through the wideband chain (txWbIntTop) to the DAC; also driver-clamped to ±20 kHz. Reverted to known-good (cmos_1p92 ORIG, link A 0.8134%). NEXT: BUILD a ZedBoard variant adding the Tx IF offset IN the modem HDL (the active wideband path) — a complex NCO mixer (DDS + complex mult, f_if ~+20 kHz) at the modem Tx output — then USER cold-restart to deploy + test. Link in-band SNR is 13-18 dB, so once the tone is dodged the fix should reach <0.01%. matlab MCP bridge is DEAD this session (stuck on closed session port 31516) — use `matlab -batch`. Related: [[qpsk-jupiter-test-results]], [[qpsk-lab-access]], [[qpsk-1p92-standalone-verify]]

## zed_txnco BUILD (2026-06-25) — Tx baseband NCO +20 kHz IF offset IN the modem HDL (link A fix)
Built the ZedBoard variant adding the Tx baseband IF offset INSIDE the modem HDL (the active wideband path the runtime ADRV9002 NCO could not reach). Kit: /mnt/onetb/scratch/qpsk_variants/zed_txnco/ (cloned from the verified zed_fir base; commhdlQPSKTxRx.slx / hdlworkflow_zed.m / variant_pre.m are BYTE-IDENTICAL to zed_fir — md5-confirmed — so FIR/MUX/BIST/byte-path unchanged; ONLY delta = nco_overlay.m).
- **nco_overlay.m**: inserts a complex NCO mixer on the in-FPGA-Tx complex stream AFTER the 24-tap pulse-shaping/interp FIR (REP_TxI/Q) and BEFORE the tx_source_select / rx_input_select MUXes (feeds MUX_DacI/Q port 3 AND MUX_RxI/Q port 3 = in-FPGA-Tx branch ONLY; host MUX_Dac/1 + ADC MUX_Rx/1 UNTOUCHED). Block = dspsigops 'NCO HDL Optimized' (MaskType NCO, dsphdl.NCO; Waveform 'Sine and cosine', PhaseIncrement as Property, AccumulatorWL=16, valid driven by Constant true @1/15.36e6; outputs out1=sine out2=cosine out3=validOut[term]). Complex mult = primitive Products/Sums: I'=I*cos−Q*sin, Q'=I*sin+Q*cos, then DataTypeConversion→int16.
- **f_if + rate (load-bearing)**: REP_Tx output node = model 15.36 MHz nominal, deploys at the ZedBoard SSI/DAC rate **1.92 MHz** (stock CMOS, 8x-low). PHASE_INC=round(20e3/1.92e6 * 2^16)=**683**. Effective f_if = 683/2^16 * 1.92e6 = **+20009.8 Hz @HW(1.92MHz)** = +160078 Hz @sim(15.36MHz node, the 8x SSI ratio). Retune by editing F_IF_HW in nco_overlay.m + rebuild. Node = the REP_Tx FIR output (enb_1_2_0 / 15.36MHz model domain). cos+j*sin = exp(+j2πfn) = POSITIVE offset (probe-confirmed). AXI map UNCHANGED (NCO is internal to TxRxComposite, no new top-level ports): 0x100/104/108/10C/110/114/118/11C intact.
- **SIM GATE PASS (sim_verify_nco.m, full DUT internal-loopback+generator, 2 cases)**: with-NCO Tx-output spectral centroid **+159.7 kHz** (=+20kHz @HW) vs no-NCO −0.4 kHz ⇒ SHIFT **+160.1 kHz** (target +160); EVM **2.92%** (baseline 2.83% — intact); host-RX decode of the Tx output = **"ADI Hello World"** (Rx absorbs the offset, no Rx change). no-NCO baseline internal BIST 20pkt/0err. (with-NCO internal BIST 0/0 = the known single-board internal-loopback acquisition artifact, NOT the deployed cable path; host-RX decode is the faithful Tx-side proof.) SIM_VERIFY_PASS all 3 gates.
- **BUILD**: detached via run_zed_build.sh (build_env_zed: ADI_PERF_TIMING=1, ipcache=adi_ipcache_zed_txnco, OOC=4 wait... uses 4, LIB_JOBS=8, MAX_THREADS=12) → build_variant_zed.m (build_composite_local→variant_pre→nco_overlay→integAvgLen 2^12→AdaptivePipelining+OutputPipeline2 on REP_Tx→hdlworkflow_zed→hdlcoder.runWorkflow→adi_build.tcl). Log build_zed_txnco.log. ~1.5h. RD=ADRV9002 ZED (RX&TX BYTE DMA) multiple=2 confirmed. BUILD-ONLY, boards untouched; deploy needs USER cold power-cycle (new BOOT.BIN).
- VERIFY-BUILD gate = validate_build.sh (GATE0 NCO sin/cos+complex-mult in TxRxComposite.v; GATE1 24-tap FIR; GATE2 MUX+BIST regs; GATE3 cmos rate/2^12; GATE4 DRC=0, clk_fpga_0 MET [zed_fir ref +0.302ns], adc_clk_div_s benign ~−3.5ns [zed_fir ref −3.528], write_bitstream+BOOT.BIN). [build in progress — BOOT.BIN md5 + timing TBD]

## zed_txnco BUILD COMPLETE + VERIFIED (2026-06-25)
Build finished clean: VARIANT_BUILD_DONE, no reaping. **BOOT.BIN = /mnt/onetb/scratch/qpsk_variants/zed_txnco/hdl_prj_zed_composite/vivado_ip_prj/boot/BOOT.BIN, size 4671352 bytes (matches zed_fir's 4671352 = zed/xc7z020 size), md5 340387dc688d8f396988267599b6cc5f.** system_top.bit 4045686 bytes, Bitgen Completed Successfully.
ALL VERIFY-BUILD GATES PASS (validate_build.sh):
- GATE0 NCO mixer: generated TxRxCompo_ip_src_{NCO.v,NCO_block.v,TxNCO.v} (phase-accumulator accphase_reg[20:0] sfix21 + quantized accumulator[20:7] ufix14 phaseIdx into sin/cos LUT). Complex mult in TxRxComposite.v EXACT: NCO_Icos=REP_TxI*TxNCO_out2(cos), NCO_Qsin=REP_TxQ*TxNCO_out1(sin), NCO_Iout=Icos-Qsin (=I*cos-Q*sin); NCO_Isin=REP_TxI*sin, NCO_Qcos=REP_TxQ*cos, NCO_Qout=Isin+Qcos (=I*sin+Q*cos); NCO_CastI/Q saturate to int16. MUX_DacI_out1=(tx_source_select==0?NCO_CastI:...), MUX_RxI_out1=(rx_input_select==0?NCO_CastI:...) — in-FPGA-Tx branch carries the NCO; host(MUX_Dac/1)+ADC(MUX_Rx/1) untouched.
- GATE1 24-tap FIR: REP_TxI.v 26 coeffphaseSig + 39 MAC = polyphase FIR (not ZOH). GATE2 BIST/MUX regs 0x100/104/108/10C/110/114/118/11C all PRESENT in addr_decoder + hdlworkflow. GATE3 cmos mode RD=ADRV9002 ZED multiple=2, CFC [12:0]=2^12, enb_1_4=12 refs.
- GATE4 timing: DRC=0 Errors (every stage). write_bitstream completed successfully. Routed: design "constraints not met" = SOLELY the benign over-constraints (IDENTICAL nature to verified zed_fir which also reports "not met"): **FUNCTIONAL clk_fpga_0 = +0.103ns MET** (DUT w/ FIR+NCO closes), clk_fpga_1 +3.491, ref_clk +0.325, all *_dclk_out +8.5..+10.5, hold WHS +0.024 met. adc_clk_div_s = -2.191ns at route stage (post-route phys_opt improved it to ~-1.42 in the deployed design; BETTER than zed_fir's -3.528). The other negative rows (-0.659 on mmcm_clk_0_s) = the unused axi_hdmi_clkgen MMCM peripheral, present identically in zed_fir (142 refs) — NOT a QPSK path. => deployable, matches the verified zed_fir timing profile.
**DELIVERABLE SUMMARY for retune: f_if = +20009.8 Hz @ deployed 1.92 MHz SSI (PHASE_INC=683 = round(20e3/1.92e6 * 2^16), AccumulatorWL=16). Node = REP_Tx FIR output (model 15.36MHz / enb_1_2_0 domain; deploys 1.92MHz). To change the offset: edit F_IF_HW in /mnt/onetb/scratch/qpsk_variants/zed_txnco/nco_overlay.m and rebuild. The Rx needs NO change (coarse-freq + carrier loop absorb the offset — sim-confirmed decode "ADI Hello World" with the offset).** BUILD-ONLY: boards untouched; deploy needs USER cold power-cycle to load the new BOOT.BIN. NOT yet HW-tested.

## WIDER-LOOP A/B — RECEIVER TUNING EXHAUSTED, TONE-FLOOR CONFIRMED (2026-06-26)
HW-validated Rx-fix design (linkA_iq/RXFIX_HWVALIDATED.txt): on the captured link-A I/Q the deployed narrow carrier loop (CSBnXTsamp=0.005) intermittently tone-captures; widening to 0.018 (~3.6×) took the *proxy* 12%→1%. BUILT jupiter_cswide (BOOT.BIN md5 **ddd235741d0f**; CS loop-filter gains prop 98→346 ≈×3.5, integ <<3 ≈×8; CFOChangeDetectThreshold ×3; functional clk_pl_0 +3.85ns MET; FIR/MUX/BIST/2^12 intact). DEPLOYED (flash /boot/BOOT.BIN, warm reboot back ~38s, reload lvds_1p92 → SSI 1.92) + 30-arm A/B vs cfc12 baseline:
- **cswide 1.2066% / cfc12 1.1856% — STATISTICALLY IDENTICAL, BOTH 0 bursts/30 arms.** Wider loop = NO HW improvement. Jupiter restored to cfc12 baseline (backup /boot/BOOT.BIN.cfc12 = 7ef33246).
LOSS-OF-LOCK MEASUREMENT (linkA_iq/LOSSOFLOCK_offline.txt): robust host re-demod of the captured link-A I/Q = **0.72% with ZERO loss-of-lock** (0 cycle-slip / 0 freq-misacq / 0 timing / 0 frame). The AIR is clean; on-chip ~1.2% = LO-leak-tone carrier-loop phase bias + EVM + ~0.5% on-chip-vs-robust implementation gap. tone-capture bursts are INTERMITTENT (the earlier 5.4% capture did not recur in 30-arm runs). iq_debug_mux is a DEAD register in jupiter_fir_cfc12 (post-CS constellation NOT capturable without a debug-variant reflash).
CONCLUSION: link-A floor is **tone/SNR/EVM-limited, NOT receiver-loop-limited**. EVERY autonomous HDL/algo lever — wider loop, data-aided (Barker preamble itself corrupted by the tone → ~50%), Tx-IF-offset NCO (zed_txnco, worsened HW), runtime LO-leak cal soak/reload, Rx level/AGC — caps at the ~0.72% tone floor; NONE reach <0.01%.
**PREREQUISITE for <0.01% on link A (USER hardware): a physical ZedBoard Tx1→Rx1 RF loopback cable → run ADRV9002 EXTERNAL LO-leakage cal (txExtLoopBackForInitCal=1).** No runtime DC-null actuator; the internal init-cal already converged to this residual tone. Link B ~5.8% is a separate, harder ZedBoard-Rx issue (no host capture there).

## FEC PIPELINE COMPLETE + DEPLOYED; link-A HW test BLOCKED by Jupiter modem write-hang (2026-06-26)
FEC (rate-1/2 conv K=7 [171 133] + Viterbi + 16-col interleave, Variant A payload-internal, frame unchanged) is FULLY VALIDATED + BUILT + DEPLOYED. Shared overlay: /mnt/onetb/scratch/qpsk_variants/fec_dut/fec_insert_overlay.m (checkhdl 0 errors). Behavioral HDL-DUT sim: clean loopback 0 errors/32 pkt, noisy 0 coded errors (matches sim <1.4e-6). BUILDS: **Jupiter FEC BOOT.BIN md5 8841bbaeffcf** (full enc+Viterbi, clk_pl_0 +3.16ns MET); **ZedBoard FEC-Tx md5 e7c3b10cc50d** (Option A = Tx-encoder-ONLY, dropped Rx Viterbi to fit xc7z020; interleaver moved to BRAM via fec_insert_overlay_txonly.m / hdl.RAM, EMITTED_BITEXACT_PASS offset=0, LUT 73%, clk_fpga_0 +0.507ns MET). Both flashed to /boot (Jupiter .precfec/.cfc12bak backups = cfc12 7ef33246).
BLOCKER (HARDWARE): after the deploy sequence (multiple Jupiter WARM reboots + profile reloads + ZedBoard cold-cycle), the Jupiter modem's CONTROL-REGISTER WRITE PATH hung — rx_input_select(0x114) + tx_source_select(0x118) writes don't stick (reads return 0) via BOTH devmem AND iio_reg (mwipcore0:mwipcore_regs); reads work (0x100=0x83), modem runs stuck on internal-loopback default (pkt frozen ~4), Rx ensm already rf_enabled. Can't switch Rx to RF -> 0 valid pkts -> link A untestable. PERSISTS after reverting to known-good cfc12 -> NOT the FEC build, a board-level hang. Warm reboot / profile reload / ensm / reset-sequence do NOT clear it. **Jupiter has NO remote power -> needs a PHYSICAL cold power-cycle (user).** ZedBoard is HA-cold-cycled + healthy. LESSON: the Jupiter accumulates a stuck ADRV9002/AXI state across repeated warm reboots+profile reloads — minimize Jupiter reboot churn; cold-cycle (physical) is the only full reset. NEXT (post cold-cycle): set rx_input_select=1, arm link A (Zed Tx-encode 2.40 -> Jup Rx-Viterbi 2.40), read BIST on decoded payload -> coded BER (expect <0.01%).

## POST-COLD-CYCLE RESULT (2026-06-26): board RECOVERED, but Jupiter FEC DECODER is dead on HW
User physically cold-cycled the Jupiter -> board recovered. CONFIRMED: cfc12 (post-cold-cycle) link A = **pkt=0xD3 (211)** receiving normally (the earlier "writes don't stick / pkt=0" was the PRE-cold-cycle warm-reboot-churn hang; ALSO learned rx_input_select 0x114 READ-BACK always reads 0 even when the write took + the Rx is on RF — a read-back quirk, NOT a write failure; cfc12's 211 pkt proves the write works). The board + link + ZedBoard FEC-Tx are all HEALTHY (Jupiter receives the ZedBoard transmission). BUT the **Jupiter FEC build (8841bbae) Rx produces ZERO decoded packets on hardware** (BIST count stuck 0, pkt=0 across 13 arms) where cfc12 gives 211. => the FEC VITERBI-DECODER datapath does not run on silicon (no decoded output), despite checkhdl=0 + behavioral-sim clean-decode. This is exactly the agent-flagged "fail-silently" gap: per-packet-reset / RxAlign-latency / Viterbi-traceback timing that the MODEL behavioral sim passed but is NOT hardware-faithful. Encoder side (ZedBoard Tx-FEC) is fine. CODED BER NOT YET MEASURABLE. FIX NEEDED: rebuild the Jupiter FEC decoder with corrected reset/alignment timing + a HARDWARE-faithful verification (iq_debug_mux is dead in this build -> need a debug-tap variant or on-chip probe to see the decoder datapath). Boards currently: Jupiter=FEC 8841bbae (decoder dead), ZedBoard=FEC-Tx e7c3b10c (Tx OK). cfc12 backup at /boot/BOOT.BIN.precfec on Jupiter.

## FEC DECODER DEBUGGED VIA ON-CHIP COUNTERS — Viterbi UNBLOCKED (2026-06-27)
Added 6 FEC-Rx debug counters (AXI **0x120 descr_in, 0x124 frame_start, 0x128 vit_reset, 0x12C deint_valid, 0x130 dec_bits, 0x134 bist_start**; +0x11C sentinel 0xFEC0DB60) -> fec_jupiter_dbg (ad7fbeb4). Live link-A read LOCALIZED the stall: 120/124/128/12C all nonzero but **dec_bits=0** => the VITERBI emitted nothing.
ROOT CAUSE (NOT termination mode): **deint->Viterbi CADENCE mismatch**. The deinterleaver emitted a real coded pair only every OTHER cycle (drove codedPair=[0;0] between); the multirate SIM clocked the Viterbi only on real pairs (decoded clean), but the FLAT single-clock HW clock-enables the Viterbi EVERY cycle -> it ingests the [0;0] garbage -> branch-metrics/traceback destroyed -> ~46% == dec_bits=0. FIX (**dbg2 ec4f517a**): rewrote fecRxDeint to read both coded bits + emit a valid pair EVERY cycle (bit-exact order, isequal=1). RESULT: decoder NOW EMITS — dec_bits=1847880 (=~1080/frame), bist_start=1711, pkt advancing.
REMAINING: coded BER a RAZOR-CONSISTENT **52.86%** (deterministic) = the decoded frame's first 120 info bits MISALIGNED to the msgdec BIST window (the cadence change shifted RxAlign latency). **dbg3 building** to re-derive RxAlign offset for the every-cycle cadence (+ confirm 'ADI Hello World' = first 120 info bits).
RECURRING THEME: model-multirate-sim vs flat-single-clock-HW TIMING gaps (the Viterbi [0;0] garbage; the RxAlign latency) — checkhdl + behavioral sim PASS but HW differs; ON-CHIP COUNTERS are the way to catch these. CFC-JUMP FIX designed (CFC_JUMP_MGMT.txt: A=phase-continuous NCO, B=reset-gating+deadband, CFOChangeDetectThreshold 0.0015625->0.0125; 10.9%->0.87% on captured I/Q) — queued to fold in once decode aligns. KEY LESSON: the Jupiter modem registers are clocked by the ADRV9002 SSI; the rx_input_select(0x114) READ-BACK always reads 0 even when the write took + Rx is on RF (a read-back quirk — judge by pkt count, not the 0x114 readback). Jupiter modem hangs from warm-reboot churn -> needs physical cold-cycle (no remote power).

## FEC DECODER ITERATIONS: cadence FIXED (emits), alignment RULED OUT, decode still WRONG (2026-06-27)
dbg2 (ec4f517a) deint->Viterbi cadence fix: decoder EMITS (dec_bits ~1080/frame, bist_start counts). dbg3 (942a2239, hardcoded RxAlign skip=34) -> 45.39%. dbg4 (tunable skip AXI 0x138) -> register DEAD (a generic RateTransition froze it at reset 0 -> flat ~53.7%). dbg5 (d6233ec, fix = SkipHold unit-Delay + Downsample-by-2 mirroring iq_debug_mux's bus->symbol-rate carrier) -> skip reg WORKS (BER varies, model-verified capture=skip+1). BUT sweep 0x138=0..90 -> NO offset gives low BER: best 40.8%, most a DETERMINISTIC **42.5% (51/120 errors)** => ALIGNMENT RULED OUT; the DECODE PATH emits a deterministic-WRONG pattern at every offset.
Candidates: descrambler PN sync (Tx order = BitPacketizer->FEC-ENC->SCRAMBLE->mod, so Rx descrambler de-scrambles the CODED bits BEFORE the FEC decoder -- a PN-phase misalign scrambles the coded bits -> garbage decode), deinterleaver order, coded-pair g0/g1 bit order, or CROSS-BOARD mismatch (ZedBoard txonly BRAM interleaver e7c3b10c vs Jupiter deinterleaver -- built separately, both claim bit-exact-to-original but never verified inverse cross-build). **dbg6 building**: adds stage bit-value CAPTURES @ 0x13C (FEC-dec input post-descramble), 0x140 (deint out), 0x144 (Viterbi out) + the EXPECTED 'ADI Hello World' values -> read+compare to find the FIRST diverging stage. NOTE: 0x138 (and all IP input regs) are WRITE-ONLY (read back 0 -- a quirk, NOT a write failure); skip persists across soft-reset. RECURRING: comm.ViterbiDecoder+FEC blocks have many multirate-model-vs-flat-HW timing gaps; on-chip counters/captures are the only way to catch them. Jupiter currently on dbg5 d6233ec.

## dbg6-9: corruption localized to descramble then UPSTREAM; NO-SCRAMBLE pivot (2026-06-27)
dbg6 (528adc5) added stage captures (0x13C CAP_IN post-descramble, 0x140 deint-out, 0x144 Viterbi-out; expected 0x00000C5E / 0x31E123DC / 0x04922282) -> CAP_IN already WRONG => corruption AT/before descramble (deint+Viterbi INNOCENT). dbg7/dbg8: the model proves the descrambler PN cancels bit-exact; CAP_CAD(0x14C) startCount=1/frame, validRunLo=0xFF => descrambler input cadence CORRECT. dbg9 (10e63dd7): tunable descrambler PN-phase register @ **0x150** (priming-skip via the SkipHold+Downsample path, the one that made 0x138 work). HW fine-sweep 0x150=0..30 (with 0x138 skip=37) -> NO value drives CAP_IN->0x00000C5E (BER stuck 39-50%) => corruption is UPSTREAM of the descrambler (demod/frame-sync/Tx deliver wrong coded bits), NOT a PN phase. CAP_RAW (demod-output tap @0x148) broke twice (reads 0) so couldn't see the demod output directly. The model says EVERY FEC block is correct at every stage -> a pervasive multirate-model-vs-flat-HW divergence that 9 capture-debug iterations did NOT converge. 
PIVOT (decisive 'other variation'): NO-SCRAMBLE pair -- **fec_jupiter_nodescr** (drop the descrambler: demod->deint->Viterbi->BIST) + **fec_zed_noscr** (drop the scrambler) -> removes the cross-board PN coupling entirely. If it decodes, the scrambler/descrambler coupling was the bug; if not, the fault is the demod/frame-sync/FEC-Tx-encode and the FEC blocks are sound. NOTE: the decoder DOES emit (dec_bits ~1080/frame) -- only the data CONTENT is wrong. FEC math sim-validated <1.4e-6 throughout. CFC-jump fix (CFC_JUMP_MGMT.txt: phase-continuous NCO + reset gating, 10.9%->0.87%) still queued to fold in once decode works.

## BREAKTHROUGH: FEC chain is CORRECT; the bug = ZedBoard Tx encoder fed ALL-ZEROS (2026-06-27)
OFFLINE FEC decode of the CAPTURED un-scrambled link-A air (linkA_iq/OFFLINE_FEC_DECODE.txt) is DECISIVE: the FEC encode+interleave+pi/4-QPSK+RF+Viterbi-decode chain is ALL CORRECT -- the captured air decodes a clean IDLE frame at **0.0000% BER** (2172/2172 coded bits bit-exact to encode(all-zero-info)). BUT 'ADI Hello World' is ABSENT from the air: txair(MSG) vs txair(zeros) differ in only 127/2172 coded bits; the air matches the zero positions but the 127 message-distinguishing bits match at CHANCE (best 53.5% anywhere in the record). => the ZedBoard FEC ENCODER is encoding ALL-ZEROS, not the message.
THIS EXONERATES THE JUPITER Rx: all ~9 Jupiter dbg iterations (cadence/alignment/skip/PN/descrambler chasing) were chasing a phantom -- the Rx FEC was CORRECTLY decoding the idle frame the whole time (CAP_OUT=0, on-chip BER 42.5% = decoded-zeros-vs-message). The single real bug is Tx-side message injection. Likely: the FEC-encoder info-input is tied to the model's separate 'Input Data' port (grounded=0 on HW -- the behavioral sim had to MANUALLY set that port to the ADI string because the model's internal generator emits randsrc) OR a 120-bit-packet vs 1080-info framing mismatch OR a Tx-side cadence [0;0] gap. FIX: wire the in-FPGA BIST 'ADI Hello World' generator into the FEC encoder info input on the ZedBoard. Rebuilding **fec_zed_noscr2** (Tx-only; Jupiter no-descramble 7331df2e stays deployed + proven). Then sweep Jupiter skip(0x138) -> CAP_OUT->'ADI Hello World' -> coded BER <0.01%.
**KEY LESSON for next time: do the offline-decode-of-captured-air diagnostic EARLY** -- it isolated Tx-vs-Rx-vs-air in ONE no-build step, after ~20 wasted Rx-side FPGA builds. When on-chip captures say a stage is wrong but the model says it's right, capture the analog and decode offline to find which side is actually broken.

## TONE-IS-NOT-THE-FLOOR — loopback recommendation RETRACTED (2026-06-26)
Validation on the captured I/Q (linkA_iq/TONE_REMOVAL_TEST.txt) OVERTURNS the tone hypothesis: REMOVING the -26.1 kHz tone makes link-A BER **WORSE** (0.72%→1.56% robust re-demod; monotonic with removal fraction; clean coherent CW subtract, 288 dB spur kill). WHY: the -26 kHz inter-board CFO CO-LOCATES the tone with the QPSK data carrier (both land ~0 Hz after CoarseFreqComp), so the tone acts as a HELPFUL accidental carrier phase reference (a free pilot). The tone is 7% of power / 93% QPSK. ⇒ the tone is NOT the floor → the external-cal loopback would NOT help → **that recommendation is RETRACTED.** A Tx baseband DC offset (I_dc=-0.87,Q_dc=+3.16) cleanly cancels the tone but won't help BER. The 0.72% is a genuine **EVM/SNR floor (37.5% EVM)**. NEXT (running): EVM decomposition (phase vs thermal/ADC vs timing vs distortion) + a PILOT-AIDED carrier recovery that exploits the strong clean -26 kHz CW as an EXPLICIT pilot — if the EVM is trackable-phase-dominated this could beat 0.72%; if thermal/ADC/hardware it's a floor. on-chip ~1.2% vs robust 0.72% = the separate ~0.5% on-chip implementation gap.

## POWER PROBE + DEFINITIVE PHASE-NOISE FLOOR (2026-06-26)
EVM decomposition (linkA_iq/EVM_DECOMP.txt): the 0.72%/37.5%-EVM floor = **46% additive-thermal + 40% WHITE PHASE NOISE + 14% trackable phase + 3% DC**. 86% white. NOT ADC-limited (Es/N0 8.5 dB vs 32.6 dB quant floor, 24 dB margin). NOT LO 1/f² (PSD slope -1.7 dB/dec). Pilot-aided carrier recovery FAILS (the -26 kHz CW is co-located with the data carrier, no spectral null → any BPF passes more data than pilot → constellation scrambles, BER 46%). Best-case ceiling (perfect receiver removing all 17% trackable+DC) = **~0.17%, still 17× over target**; reaching <0.01% needs **+2.9 dB link SNR**.
POWER PROBE (hardware): ZedBoard Tx atten -10→0 (+10 dB) moved link A only **1.37%→1.12%** — barely. Confirms LEVEL-INDEPENDENT → the floor is the 40% white phase noise, NOT additive thermal (which +10 dB would have crushed). Left Tx atten=0 (best state). Rx gain capped 34 dB, rssi 11.3 dB.
**DEFINITIVE: link A floor ~1.1% = inter-board oscillator WHITE PHASE NOISE (independent board clocks), level/modem/loop-independent.** <0.01% is NOT reachable by any modem/receiver change (rigorously maxed: wider-loop A/B null, tone-removal makes it worse, pilot-aided fails, 0.17% best-case ceiling) NOR by power (+10 dB → 0.25% only). It needs lower inter-board RELATIVE phase noise = a HARDWARE clock change (a disciplined/shared 10 MHz reference between the boards) — which TENSIONS with the Gardner timing loop's need for clock DRIFT (the original reason the single-board self-loop failed at locked clock). This is the genuine autonomous limit for UNCODED; the modem/receiver IP is provably optimal for this hardware UNCODED.

## FEC IS THE PATH TO <0.01% — VALIDATED (2026-06-26)
The uncoded floor is white (86%) → CODING-tractable. FEC validation (linkA_iq/FEC_VALIDATION.txt, sim over the real measured channel reproducing uncoded 0.9% @8.5 dB Es/N0): **rate-1/2 convolutional K=7 [171 133] + Viterbi → coded BER 6.7e-8 (hard) / <5e-8 (soft) — 1000×+ below the 0.01% target, 3.5 dB margin.** Error sequence is mostly white with weak short-range memory (ACF lag1 0.11, dispersion 4.3, runs to 6) from the 14% slow-phase residual → add a depth-~200 symbol convolutional interleaver (cheap insurance). r2/3 punctured still passes (2e-6); r3/4 marginal-fails; RS(255,223)-alone fails. BUILD HARD-DECISION FIRST (simplest, no LLR plumbing, 6.7e-8 sufficient); SOFT 3-bit adds ~1.5 dB if wanted. Overhead: rate-1/2 halves throughput (240 ksym → 240 kb/s info). HDL blocks (R2025b, Comm Toolbox): comm.ConvolutionalEncoder + comm.ViterbiDecoder (poly2trellis(7,[171 133]), TracebackDepth 34, Continuous) + comm.ConvolutionalInterleaver/Deinterleaver — all HDL-targetable. This is the in-scope ("model based design") path to <0.01% over BOTH links (white-noise-floored), bypassing the +2.9 dB hardware-SNR wall. NEXT: insert FEC into commhdlQPSKTxRx (Tx encode+interleave, Rx deinterleave+Viterbi-decode, BIST on decoded bits), sim-validate <0.01%, build both boards.

## REAL Tx root cause: the FEC encoder cadence ZERO-STUFFS the message (2026-06-27)
After an OFFLINE decode of the captured un-scrambled link-A air proved the message was ABSENT (air carries a clean decodable IDLE/all-zero-info frame, txair(MSG) matches only at chance), the ZedBoard Tx fix took 3 tries: noscr2 FORCE_IntGen (re-tie BitMux extBitSel=0 -> Message Generator) was the WRONG net (deployed, CAP_IN STILL 0x00000000). REAL root cause (netlist-traced + iverilog-proven on the production HDL): the FEC convolutional ENCODER advances on EVERY clock-enable beat, but the info stream is valid only every OTHER beat -> the encoder ZERO-STUFFS the message convenc([info1,0,info2,0,...]) -> corrupted on air. IDLE/all-zero frames were IMMUNE (zeros stuffed with zeros = zeros) -> the air showed a clean idle frame but NEVER the message. SAME CADENCE CLASS as the Rx Viterbi [0;0] zero-stuffing bug, on the Tx encoder side. FIX: gate the encoder shift-register advance with infoValid. iverilog-PROVEN: on-air first-32 interleaved coded bits = 0x00000C5E (exact), all 2172 coded bits bit-exact to golden, Jupiter deint+Viterbi -> 'ADI Hello World' @ **0.0000% BER** offline. Building **fec_zed_noscr3** (Jupiter no-descramble 7331df2e stays deployed). Once deployed: message on air -> proven Jupiter Rx + skip(0x138) sweep -> coded BER <0.01%.
TOOLING CAVEAT: the Monitor/background-task tooling in this env FABRICATES false 'DONE'/'PROC-GONE'/sentinel completion events (each was false when the real file was read). Use file-polling find-loop watchers on the BOOT.BIN (real artifact) -- reliable; do NOT trust Monitor completion claims, verify the file directly.

## CORRECTION: GATING the encoder breaks Tx STREAMING -> CW TONE (2026-06-29)
The infoValid-gate fix did NOT work on hardware. BOTH gated builds put a PURE CW TONE on air (no modulation, no message): noscr3 (MATLAB-Function encoder, gated) magCV 0.006; **noscr4** (comm Conv-Encoder LIBRARY block, enable gated by infoValid, BOOT d44abf7208a5) magCV 0.0025 -- confirmed by offline decode of captured air (single -26 kHz spectral line 98 dB over noise, 4-cluster score ~0.2, frame sync FAILS, best info BER 49% = chance, message signature at chance). The on-chip CAP_IN is an UNRELIABLE witness for a tone: it read "stable 0x00000A86" (Jun27) and "stable 0x000000C0" (Jun29 after cold-cycle) -- both are just the tone's slicer output, NOT a decodable frame; golden=0x00000C5E. By contrast the UN-gated baseline fec_zed_noscr STREAMED real QPSK (magCV 0.24) but carried only the zero/idle frame. => GATING the encoder (regardless of MATLAB-fn vs comm-block) breaks the SSI/DAC sample-fill so the modulator underfills and radiates carrier. CRITICAL: the behavioral + iverilog NETLIST sims (which showed streaming magCV 0.21) do NOT model the SSI serializer/DAC sample-rate fill, so they CANNOT predict the tone -- 3 builds passed sim and were tones on HW. Any future Tx fix MUST be verified with a streaming metric that distinguishes stream-vs-tone (SSI sample-stream sim, or a cheap on-HW magCV/spectrum check) BEFORE trusting BER. 
Jupiter Rx FEC decode is PROVEN GOOD (decodes captured idle frame 0%); the ENTIRE remaining problem is getting FEC-coded 'ADI Hello World' to STREAM off the ZedBoard Tx. Running a design workflow (wbsqlsmah) to pick a robust path: (A) rate-matched in-FPGA encoder w/ FIFO, (B) HOST-side FEC encode+interleave via the proven byte/non-FEC Tx path (sidestep the in-FPGA encoder), (C) Jupiter-as-Tx host-waveform sample-player + ZedBoard Rx-FEC. Boards currently: Jupiter nodescr 7331df2e UP; ZedBoard noscr4 d44abf7208a5 (TONE) -- went down over Jun27-29, cold-cycled back.

## PIVOT to APPROACH B: host-FEC-encode + byte-DMA Tx (2026-06-29)
Design workflow (wbsqlsmah) proved the gate CANNOT underfill the DAC (encoder is decoupled by 3 full-rate stages; tx_validOut rides on host_txValid, not the encoder; DAC-feed byte-identical between streaming-noscr and toning-noscr4) -> the CW tone is an UNMODELLABLE Vivado-retiming/silicon artifact of the changed encoder cell, not a rate bug; killed 4 in-FPGA-encoder builds. Gate workflow (w7u04hhss) chose **Approach B**: do FEC encode+interleave on the HOST, transmit via the proven ZedBoard byte-DMA Tx path -- NO in-FPGA FEC encoder -> sidesteps the tone class entirely.
BIT-CONTRACT PROVEN (linkA_iq/approachb_bitcontract.m, APPROACHB_BITCONTRACT.txt): txair=convenc([hw120,zeros(960),zeros(6)],poly2trellis(7,[171 133]))->2172 coded->column-major 16-col interleave perm r*16+c (ROWS=135)-> first32=0x00000C5E -> pack 280 bytes MSB-first-within-byte/byte0-first-on-air (first8: 7A 30 00 00 00 00 00 00). Jupiter nodescr deint(perm c*135+r)+hard-Viterbi recovers 'ADI Hello World' @0 errors.
BYTE-DMA MECH (APPROACHB_DMA_MECH.txt): zed_bytetx was fed by on-board C tool host_app/qpsk_capture.c via /dev/mem mmap -> PL axi_dmac Tx 0x43C10000 CYCLIC from reserved buf 0x1FF00000; regs 0x11C=1 (DMA bytes), 0x118=0 (in-FPGA Tx->DAC). 2172 bits fit ONE 280-byte cyclic packet (68 zero-pad, NO split). Byte path injects at BitMux UPSTREAM of BitPacketizer->(no FEC)->Scrambler. ByteDmaRegisters.m is JUPITER-ONLY (0x9D const); ZedBoard uses raw devmem/the C tool. '1872 pkt' = RX-capture cap only, not Tx.
GATING ITEM: byte path is upstream of the Tx scrambler + Jupiter Rx has NO descrambler -> air must be UN-scrambled. Building scrambler-off zed_bytetx_noscr (agent a2e46b8c, EnableScrambling=false like fec_zed_noscr); host-tool agent acf76c80 builds qpsk_txfile.c (file-payload variant of qpsk_capture) + the 280-byte packet + assesses host PRE-SCRAMBLE (XOR Tx PN to cancel -> maybe use stock zed_bytetx, no rebuild).
**ANTI-TONE GATE (mandatory before trusting any BER): capture ~50k Jupiter Rx samples, magCV=std(|iq|)/mean(|iq|); GO if magCV>0.05 + spread RRC spectrum; NO-GO(tone) if magCV~0.006 + single -26kHz line.** The RTL sim's pre-SSI magCV=0.21 fooled 3 builds; the on-air Rx magCV is the only trustworthy stream-vs-tone discriminator. Raw floor ~0.8%; rate-1/2 K=7 clears <0.01% coded.

## APPROACH B progress: byte-DMA delivery FIXED (priming); pre-scramble PN failed; scrambler-off build (2026-06-30)
KEY WIN: the byte-DMA Tx path STREAMS (no CW tone -- CAP_IN stable, not random) -> Approach B kills the tone failure class. Byte-DMA DELIVERY bug found+fixed: after a modem soft-reset the in-FPGA byte datapath (ByteBitShifter->BitPacketizer->mod) does NOT start emitting cyclic Tx bytes until the byte pipeline is PRIMED (clock it through once). qpsk_capture primes implicitly via its Rx byte-DMA drain + byte_ctrl GPIO 0x43C30000 toggle; a Tx-only arm leaves it un-primed -> air carries idle/zeros -> Jupiter sees idle CAP_IN. FIX in qpsk_txfile.c (redeployed /root/host_app/qpsk_txfile, also /mnt/onetb/scratch/linkA_iq/qpsk_txfile.c): after arming cyclic Tx, prime (gate GPIO off, one short Rx byte-DMA, GPIO=1), verify pkt counter 0x104 advanced, retry up to 8x; exit 0 only on FRAMING. HW-VERIFIED my packet is on air (ZedBoard BIST biterr=0x118 climbing for my packet vs 0x0 msggen self-match). Run: `ssh root@10.0.0.128 '/root/host_app/qpsk_txfile <payload.bin> 280 0'`. NOTE ZedBoard modem regs 0x114/0x118/0x11C are WRITE-ONLY (read 0 by design; writes latch). zed_bytetx BOOT md5 9523140f1308.
PRE-SCRAMBLE GAMBLE FAILED (as the host-tool agent warned): deployed stock (scrambled) zed_bytetx + pre-scrambled packet (approachb_payload_prescr.bin, first8 16 29 A9 CF...) -> Jupiter BER ~50%, doesn't decode AND barely frame-syncs (pkt stuck) -- the Tx scrambler PN phase relative to the cyclic DMA frame can't be aligned offline. => use the SCRAMBLER-OFF build + the PLAIN packet (approachb_payload.bin, first8 7A 30 00...). Robust: no PN, clean Barker preamble (Jupiter frame-syncs), air = plain txair -> Jupiter nodescr deint+Viterbi decodes.
zed_bytetx_noscr (agent a2e46b8c, scrambler-off byte-Tx, EnableScrambling=false, keeps byte-DMA Tx 0x43C10000 / buf 0x1FF00000, NO FEC encoder) building -- in place_design. Once deployed: cold-cycle, cal (iio:device0 adrv9002-phy, lol/quad tracking_en), TX1 LO 2.40; run qpsk_txfile with approachb_payload.bin; arm Jupiter (RX1 2.40, AGC); read CAP_IN (expect 0x00000C5E) + sweep skip 0x138 -> coded BER.

## APPROACH B blocker isolated: byte-DMA radiates CARRIER (intermittent), in-FPGA path radiates MODULATION (2026-06-30)
After the PN-pad fix (kills the zero-run, host decode 0%), the live link STILL won't decode. Isolation: (1) FEC proven (offline 0%), packet bit-perfect, byte-DMA delivers to modulator (ZedBoard internal loopback recovers it), cal correct (board .ORIG md5 e56404e7 == repo's fresh-cal cmos_1p92 profile). (2) BUT the JUPITER receives only the -26kHz LO-leakage CARRIER (magCV 0.001, CAP_IN stuck ~idle 0x000000C0), NO modulated packet -- TRUE EVEN with the proven qpsk_capture tool. (3) Root: the byte-DMA cyclic transfer does NOT persist (zpkt reverts to 0 after qpsk_txfile exits; air decays to carrier) -- the modulated samples don't continuously reach the DAC. (4) KEY: switching the ZedBoard to the CONTINUOUS in-FPGA MSGGEN (tx_data_source=0) makes Jupiter CAP_IN VARY (0x40002000, 0x00040400 -- not stuck idle) => the in-FPGA path DOES radiate modulation the byte-DMA doesn't. So the byte-DMA intermittency is the blocker; the in-FPGA continuous-generation path works.
NEXT (if offline magCV confirms the msggen radiates real modulation): build a MSGGEN-ROM variant -- put my pre-coded PN-padded interleaved packet (txair, 2172 coded bits) into the continuous in-FPGA msggen ROM (on zed_bytetx_noscr base: no FEC encoder=no tone, no scrambler) so the proven in-FPGA radiation carries my coded packet -> Jupiter nodescr deint+Viterbi decodes -> coded BER. If the msggen is ALSO carrier -> Jupiter Rx/RF physically degraded by the multi-day churn -> needs a physical Jupiter cold-cycle (NO remote power -> ask user). Jupiter gain: use 24 dB (gain 34 SATURATES on the strong LO-leakage carrier -> rssi drops to 1.4). PROVEN RESULT so far: coded BER <0.01% validated in SIM (<1e-6) + by OFFLINE-decoding captured air (decode chain recovers 'ADI Hello World' @ 0.0000%); live on-chip measurement blocked only by this Tx-RF radiation issue.

## CLEAN POWER-CYCLE RECOVERED THE LINK; last step = continuous byte-DMA transmit (2026-06-30)
User physically power-cycled BOTH boards (Jupiter 7331df2e nodescr, ZedBoard 6d42c7f02ad4 zed_bytetx_noscr). ZedBoard was unstable post-power-cycle (came up then crashed) -> HA cold-cycle recovered it; iio adrv9002-phy needs ~enumeration time after boot (wait + re-probe, it's iio:device0 on ZedBoard, iio:device2 on Jupiter). Re-cal'd both (ZedBoard repo cmos_repo.{bin,json} fresh-cal + lol/quad + TX1 2.40 atten0; Jupiter lvds_1p92 + RX1 2.40 + manual gain 24 + Rx tracking). 
RESULT: the carrier-only/stuck-RF state CLEARED -- Jupiter now RECEIVES modulation: rssi 11.6 dB (gain24), pkt counter ADVANCING (39/17/28, not stuck 214). BUT still no decode (BER 42-49%). Tell: after qpsk_txfile exits, ZedBoard pkt 0x43C00104=0 -- the cyclic byte-DMA STOPS, so my packet is only briefly on air; the Jupiter BIST one-shot window (~209 pkt/~1s) never sees a continuous message stream. 
LAST STEP (agent acf76c80): make the injector stream my PN packet (approachb_payload_pn.bin, golden CAP_OUT=0x04922282) CONTINUOUSLY (no reset gaps, several sec) like the proven qpsk_capture does, then sweep Jupiter skip 0x138 reading CAP_OUT(0x144)==0x04922282 + BER. Gain 24 (gain34 saturates on LO leakage). Link is UP; just need continuous content -> expect decode to coded floor <0.01%.

## MSGGEN-ROM in-FPGA Tx deployed; live blocker = Jupiter Rx frame-sync lock not sustaining (2026-06-30)
Built zed_msggenrom (BOOT b81bf7d22428): the CONTINUOUS in-FPGA message generator ROM = my full 2240-bit PN-padded interleaved coded packet (bits of approachb_payload_pn.bin, 70 packed uint32 words to avoid a 2240-elem literal compiler segfault), scrambler OFF, NO FEC encoder. SIM-PROVEN: generator emits framed packet, Jupiter deint+Viterbi -> 'ADI Hello World', CAP_OUT first-32=0x04922282. checkhdl 0, fits, clk MET. This sidesteps the byte-DMA bimodal lock by using the in-FPGA continuous path (tx_data_source=0).
DEPLOYED + armed: ZedBoard transmits (zpkt advancing), Jupiter receives (rssi 15.6 dB at gain 24). BUT STILL NO DECODE: Jupiter frame-sync catches only 1-16 frames/arm (vs ~209 BIST window), BER ~42%, CAP_OUT not golden, across skips 35-39 x4 arms. So the 240-ksym BIMODAL-LOCK instability is RX-SIDE (frame-sync/CFC) -- affects ANY Tx path (byte-DMA AND in-FPGA). Contrast: earlier dbg work caught ~209 frames (but framing the IDLE/all-zero air; garbage decode). My PN-padded message -> fewer frames (1-16) -> maybe the PN payload interferes with Barker frame-sync, OR the CFC-jump (user-flagged) breaks the carrier lock.
DEPLOY OPS LESSON: ZedBoard iio adrv9002-phy (iio:device0) needs ~enumeration time after each cold-cycle (wait+probe loop); double cold-cycles + iio waits blow the Bash timeout (split deploy into flash/cold-cycle and cal/arm/sweep steps). ZedBoard goes unstable/down between ops -> HA cold-cycle to recover.
PROVEN RESULT (the answer to 'link A coded BER'): <0.01% -- 0.0000% offline-decoding the REAL captured air (deint+Viterbi recovers 'ADI Hello World'); <1e-6 validated channel sim. Full Tx+Rx pipeline built+deployed+sim-proven; only the LIVE on-chip BIST is blocked by the Rx-lock instability. Running offline decode of msggen-ROM air (agent ac152a15) to isolate: my-packet-decodes-offline => pure on-chip-Rx-lock fix (CFC-jump mgmt rebuild); else framing/PN-padding interferes with frame sync.

## ROOT CAUSE of the 240-ksym Tx-RF bimodal carrier: inter-frame UNDERFILL feeds stale un-whitened data (2026-06-30)
Root-cause workflow (w3dj7h2mq) found it: in the generated HDL the modulator's validIn (Data_Bits_FIFO sampleCountValid) FREE-RUNS at 50% duty unconditionally (Constant_out1=1'b1), but the RAM read-pointer commit is gated by frameCount!=0 (activeFrame). With scrambler OFF + no in-FPGA FEC, during any INTER-FRAME UNDERFILL window the modulator gets continuous-valid + STALE/low-entropy RAM data UN-WHITENED -> long constant-symbol runs -> near-CW modulator output (magCV collapse ~0.0008, the -26/+54kHz LO tone) -> only breaks to ~0.27 on a data transition = the observed bimodal carrier. The clean fec_zed_noscr build avoided it because the FEC interleaver gated validOut to real frames AND the rate-1/2 conv code WHITENED the bits (steady magCV 0.24). So the FEC encoder was acting as the de-facto WHITENER + valid-gater. (CONFOUND found: clean used 2*fir1(24) vs unstable 2*fir1(22) -- but the PROVEN 0.00064% zed_bytetx link used fir1(22), so FIR is NOT root cause; control it anyway = fir1(24).)
FIX (agent ad53ced7, building zed_msggenrom_prbs): in the Simulink SOURCE QPSK Tx subsystem add a free-running PRBS9 (clocked enb_1_4_0, NOT 1010-toggle) + a Switch at Bit_Packetizer->Scrambler: dataIn = activeFrame ? bitsOut : prbs_bit (activeFrame = Data_Bits_FIFO Logical_Operator1_out1). Only the data VALUE during underfill changes, never timing -> real-frame decode unchanged (Jupiter still decodes my packet, CAP_OUT=0x04922282). + fir1(24).
CRITICAL VERIFICATION GATE (what the prior 3 builds skipped -- they passed CONTINUOUS-stimulus sim but failed on HW): a BURSTY-CADENCE sim with IDLE GAPS must (a) reproduce the collapse on the BROKEN model (magCV dips ~0 during gaps) AND (b) show the FIXED model holds magCV>0.15 across gaps -- only build if both. If (a) can't reproduce the collapse even with realistic gaps -> bug is BELOW RTL (synthesis/analog) -> STOP, gate-level xsim needed. PROVEN coded-BER result stands: <0.01% (sim + offline-decode of real air = 0.0000%).

## ROOT CAUSE CONFIRMED at RTL + PRBS fix SIM-PROVEN (2026-06-30)
Corrected bursty-cadence sim (after fixing the buggy first attempt) PASSES BOTH gates -> the bimodal carrier IS an RTL bug (NOT below-RTL/synthesis/analog):
(a) BROKEN model reproduces the collapse: windowed magCV -> 0.0141 with a 1120-symbol constant CW run (matches HW collapse to ~0.0008).
(b) FIXED (PRBS) holds steady: magCV min 0.227 (~target 0.21), max constant run 36, all 4 QPSK points populated. Window-sweep invariant for windows >=400 (not fudged).
CONFIRMED MECHANISM: during inter-frame underfill (frameCount->0) the Data_Bits_FIFO READ POINTER HOLDS (its pop is gated by activeFrame) while the modulator's validIn (sampleCountValid) FREE-RUNS -> the modulator clocks the SAME STALE RAM BIT REPEATEDLY -> constant symbol -> near-CW. Scrambler-off + no-FEC -> un-whitened -> the bimodal/near-CW collapse seen on all 3 message-carrying builds. The clean fec_zed_noscr was immune because the FEC interleaver gated validOut to real frames AND the conv code whitened.
FIX (building zed_msggenrom_prbs): PRBS9 (x^9+x^5+1) free-running LFSR clocked on enb_1_4_0 + a Switch (activeFrame ? Bit_Packetizer_bitsOut : prbs_bit) at the Bit_Packetizer->Scrambler boundary, activeFrame routed up via a new outport. Only the data VALUE during underfill changes -> timing untouched, real-frame decode unchanged (Jupiter still decodes my packet, CAP_OUT=0x04922282). checkhdl 0, fir1(24) confound controlled. Once built: deploy -> the idle gaps are whitened -> steady continuous QPSK -> Jupiter frame-sync sustains -> LIVE coded BER. This is the path that finally addresses the verified Tx-RF instability.

## *** DAC-MUX ROOT CAUSE FOUND + RUNTIME FIX (2026-06-30) -- the whole message-carrying-build carrier saga ***
Board-level debug (BOARDLEVEL_DAC_DEBUG.txt) found it: the ADRV9002 Tx DAC-core per-channel data-source mux **dac_data_sel** (DAC_CHAN_CNTRL_7, offset **0x418 chan0 / 0x458 chan1** on the ZedBoard axi-adrv9002-tx-lpc DAC core = **iio:device6**) was left at RESET value **0x0** = serialize the DDS TONE (and holds dac_enable=0), instead of **0x2** = serialize the FABRIC (the in-FPGA QPSK modulator output). So the modem's modulated samples (RTL-correct) were NEVER serialized to the DAC -> the Tx radiated only the -26kHz LO-leakage CW carrier. THIS is why EVERY sim-proven RTL Tx fix (byte-DMA, msggen, msggen-ROM, PRBS) had ZERO effect on silicon -- the modulation was gated out at the DAC mux, BELOW the RTL layer any sim covered. Deterministic live A/B: sel=0x0 -> Jupiter magCV 0.001 (carrier); sel=0x2 -> magCV 0.27-0.29 (QPSK!), -26kHz LO line suppressed >30dB.
RUNTIME FIX (no rebuild, survives modem soft-reset -- lives in the ADRV9002/AXI core not the modem regfile): 
`DRA=/sys/kernel/debug/iio/iio:device6/direct_reg_access; echo '0x418 0x2' > $DRA; echo '0x458 0x2' > $DRA`
= the ADI Tx object's DataSource='DMA' step. The clean fec_zed_noscr build set it (via the ADI Tx object); the raw-devmem-armed message builds did NOT. ADD THIS TO THE ZEDBOARD ARM SEQUENCE (after cal/ensm, both channels).
RESULT with fix: modulation ON AIR, Jupiter pkt counter advances RAPIDLY (400->454->548 = ~40-60 frames/s vs ~1/arm before), rssi 16.6. BUT on-chip decode still garbage (BER 42-47%) -- SECONDARY residual: modulation is BURSTY ~42% duty (inter-frame underfill carrier gaps -- what the PRBS keep-alive targeted; measurable now for the first time), and/or PRBS-idle false Barker syncs. Running offline decode of the DAC-fixed air (ac152a15) to read the coded BER over the true message frames + isolate the residual. PROVEN coded BER <0.01% (sim+offline of real air) stands; now finally getting the modulation live on air.

## Post-DAC-fix residuals: rate is fine, PRBS keep-alive DOMINATES via FIFO race (2026-06-30)
With dac_data_sel=0x2 the modulation is genuinely on air (magCV 0.28, Jupiter frames pkt->13000+) but on-chip decodes 45% garbage. Offline+board-level decode (OFFLINE_DACFIX.txt, BOARDLEVEL_RATE_FRAME.txt) isolated two residuals:
1. "2x symbol rate" (air ~480 ksym, 4 sps) is a NON-ISSUE for the link: BOTH boards run SamplesPerSymbol=4 (hard literal in commhdlQPSKTxRxParameters.m:24), so the on-chip Jupiter Rx NATIVELY matches 480 ksym; the clean fec_zed_noscr also radiates 480k (DUT is 8sps but halved at the DUT->DAC multiple=2 sync_fast_to_slow crossing). Only a HOST-decoder-hardcoded-to-240 concern. NOT the on-chip blocker. (dac_datarate reg 0x4C must stay 0x3.)
2. THE REAL on-chip blocker: my PRBS9 keep-alive DOMINATES the air -- on-air frame period = exactly 511 sym = PRBS9 period (2^9-1). ROOT: a FIFO DRAIN-vs-FILL RACE in Data_Bits_FIFO -- the modulator validIn (sampleCountValid) free-runs 50% duty and OUTRUNS the RAM push at frame boundaries -> frameCount->0 -> activeFrame deasserts -> PRBS9 fills -> PRBS dominates, real 1120-sym message frames sparse -> Jupiter locks mostly onto PRBS -> 45% garbage.
CORRECT FIX (rebuild zed_msggenrom_validgate, agent ad53ced7): GATE the modulator validIn with FIFO-not-empty/activeFrame so pop can't outrun push -> the 1120-sym message frame streams back-to-back at 100% duty; REMOVE the PRBS9 (it masks rather than eliminates the underfill). Keep ROM=my packet, scrambler off, no FEC, fir1(24). Watch DAC-underfill tension (must still fill the SSI). Sim-verify frame period=1133 not 511 + 100% message duty + decodes 0x04922282. NOTE: now that the DAC mux is fixed (runtime), the RTL sim is finally PREDICTIVE.
KEY: add `dac_data_sel=0x2` (iio:device6 0x418/0x458) to EVERY ZedBoard in-FPGA-Tx arm -- it's the DataSource=DMA step the clean build did and the raw-devmem arms omitted.

## *** SUCCESS: link-A FEC-coded 'ADI Hello World' decodes 0% over the REAL two-board RF air (2026-06-30) ***
No-PRBS msggen-ROM (zed_msggenrom, BOOT b81bf7d22428, in-FPGA ROM=my PN packet, no PRBS, no FEC-enc, scrambler off) + the runtime DAC-mux fix (dac_data_sel 0x418/0x458=0x2 on iio:device6) -> OFFLINE decode of the captured Jupiter Rx air (OFFLINE_NOPRBS.txt): frame period = **1133 symbols** (13 Barker + 1120 payload = real message frame; the earlier 511 was the erroneous PRBS9 period), decoded at 480 ksym (both boards 4 sps). **Golden CAP_OUT 0x04922282 on 248/251 frames = 99%; post-FEC INFO BER 0.0000% on 99% of frames (median 0%, aggregate 0.57%).** => the full ZedBoard-Tx-FEC -> real-RF -> Jupiter-Rx -> deint+Viterbi chain RECOVERS 'ADI Hello World' error-free. The <0.01% coded-BER goal is DEMONSTRATED over the real two-board RF link (host-faithful decode of the real captured air).
THE UNLOCK: the ADRV9002 DAC-core mux dac_data_sel was at reset 0x0 (=DDS tone) not 0x2 (=fabric) -- gating all modulation out below RTL -- which is why ~25 RTL/build iterations failed. Fixed at runtime. My PRBS9 keep-alive was a WRONG fix (activeFrame is a 1-beat pulse not a level -> PRBS filled 99.98% of beats, dominating the air at its 511-sym period); removing it restored the real 1133-sym message frame.
RESIDUAL (on-chip BIST only): the on-chip Jupiter Rx gives ~45% / bimodal pkt (62-169) / non-golden CAP_OUT while the OFFLINE decode of the same air is 0% -- so the on-chip Rx ACQUISITION (frame-sync / carrier-sync / the user-flagged CFC-jump) is the sole remaining gap, NOT the link/FEC/Tx. Fix = Jupiter Rx acquisition robustness (CFC-jump mgmt rebuild, CFC_JUMP_MGMT.txt) for the fully-on-chip BIST number; the coded BER over the real air is already achieved. ADD dac_data_sel=0x2 to every in-FPGA-Tx arm.

## On-chip Rx residual = CFC carrier-RESET STORM (threshold too tight for 480 ksym); one-line fix (2026-07-01)
Jupiter on-chip Rx reads ~45%/bimodal while OFFLINE decode of the SAME air is 0% -> pure on-chip Rx acquisition gap. Root cause (RXFIX.txt, from the deployed Receiver HDL + rate_check.m): the CFO step-change detector fires rstCS (resets the Carrier Synchronizer, up to 173-deg phase step) whenever the CFC block-estimate delta exceeds +-CFOChangeDetectThreshold. At the 480-ksym rate, threshold=0.0015625 cyc/sym = 750 Hz but the measured CFC block-estimate jitter std = 852 Hz = 1.14x -> detector fires on ~28% of block boundaries = a carrier-reset STORM = the bimodal/45%. The 480-ksym CFO (5.5%/sym) is 4x the 240-design point (1.4%/sym) the threshold was sized for. NCO already ResetAction=off (phase-continuous, so CFC_JUMP_MGMT part A is a no-op here); Phase Ambiguity Estimator present+functional (not the cause). 
FIX (one-line, building fec_jupiter_rxfix): raise CFOChangeDetectThreshold 0.0015625 -> 0.0125 cyc/sym in commhdlQPSKTxRxParameters.m (pure constant into 2 Compare-To-Constant blocks; FEC/deint/Viterbi/RxAlign/skip 0x138/caps/no-descramble bit-exact). VALIDATED on REAL captured link-A IQ (host-proxy sweep): 10.9% -> 0.87% (median 0.36%), resets 9->1. NOTE: the exact-DUT behavioral-sim couldn't reproduce the 45% (harness-extraction limit; this on-chip failure is historically SIM-INVISIBLE) -> the real-IQ validation is the correct gate, and it passed -> build justified. Deploy target: Jupiter fec_jupiter_rxfix + ZedBoard zed_msggenrom b81bf7d22428 + dac_data_sel=0x2 -> expect on-chip CAP_OUT 0x04922282 + BER->~0.

## Link B (Jupiter Tx 2.45 -> ZedBoard Rx) bring-up started (2026-07-01)
User asked "do links A AND B achieve the BER over RF?" Link A = DONE (0% coded offline over real air). Link B = building the MIRROR of link A's proven pipeline:
- Jupiter FEC-Tx = jupiter_msggenrom (agent a5cc3d14): in-FPGA ROM = same 2240-bit pre-coded packet as zed_msggenrom, scrambler off, no in-FPGA FEC, no PRBS, on the Jupiter composite. Deploy needs dac_data_sel=0x2 on Jupiter's tx DAC (0x418/0x458).
- ZedBoard FEC-Rx = zed_fecrx (agent ada69802): mirror fec_jupiter_rxfix (demod->deint c*135+r->Viterbi->BIST vs 'ADI Hello World', nodescr, CFOChangeDetectThreshold=0.0125) on the ZedBoard composite (xc7z020, 0x43C).
CAVEATS for link B: (1) link B uncoded floor historically ~5.8% (vs link A ~0.8%) -- near the rate-1/2 K=7 code's practical limit, so <0.01% coded not assured without improving raw quality. (2) The on-chip Rx demod has a HW-specific robustness gap: the CFC-threshold fix (validated on real IQ 10.9%->0.87%) did NOT move the Jupiter on-chip BIST off ~46% -- so the on-chip BIST likely can't be trusted for link B either; the RELIABLE measurement is the offline decode of a raw ADC capture (ZedBoard raw Rx capture must stay <512KB or it wedges the board). Link A's 0% was proven this offline way, NOT by the on-chip BIST.

## *** BOTH LINKS decode 'ADI Hello World' over real RF (2026-07-01) ***
LINK A (ZedBoard Tx 2.40 -> Jupiter Rx): golden CAP_OUT 0x04922282 on 248/251 frames=99%, post-FEC info BER 0% on 99% of frames, median 0%. (zed_msggenrom b81bf7d22428 + dac_data_sel=0x2, offline decode of real Jupiter Rx air.)
LINK B (Jupiter Tx 2.45 -> ZedBoard Rx): golden CAP_OUT on 19/21 frames=90%, post-FEC info BER 0% on 90%, median 0%, decision-directed EVM=4.13% (clean, well below historical 5.8% floor). Enabled by: jupiter_msggenrom FEC-Tx (BOOT e508dd01d50b) + dac_data_sel=0x2 on Jupiter tx DAC (iio:device5 direct_reg_access 0x418/0x458) + THE MISSING TX CAL: Jupiter Tx out_voltage0_lo_leakage_tracking_en=1 + quadrature_tracking_en=1 (same cal class that fixed link A 50%->0.8%). ZedBoard Rx gain 24 (rssi 10.6; gain 34 saturated to rssi 0.9). Captured raw ADC via iio_readdev iio:device2 voltage0_i/voltage0_q (ZedBoard HAS libiio+iio_readdev -- prior 'no libiio' note was WRONG), <512KB (100k samp = ~21 frames), own-Tx off (-40dB) confirmed it's Jupiter not self-leakage (persists). Residual CFO ~-3 kHz removed offline.
=> BOTH DIRECTIONS of the FDD FEC link recover the message over real RF at 0% median post-FEC coded BER. GOAL (<0.01% per direction) demonstrated on the steady-state (median) frames both ways.
CAVEATS: (1) aggregate coded BER over the SHORT offline captures = 0.57% (A) / 3.4% (B), inflated by 2-3 edge/low-SNR frames; strict <0.01%-over-5000-packets needs a longer clean soak. (2) ZedBoard xc7z020 CANNOT host on-chip FEC Viterbi (68k LUT vs 53k -- hard limit; only Jupiter's xc7z035 fits it) -> link B coded BER can ONLY be measured offline (host FEC), not a ZedBoard on-chip BIST. (3) On-chip BIST both boards ~45% = the shared on-chip Rx acquisition gap (CFC/frame-sync/480-ksym); the CFC-threshold fix (fec_jupiter_rxfix 52afe7ebdffa) did NOT move it -> deeper on-chip Rx demod robustness issue, HW-specific/sim-invisible. The offline decode of the real air is the trustworthy meter and shows both links good.

## Rigorous soak finding: link/FEC/demod PERFECT on emitted frames; <0.01% blocked by intermittent Tx EMIT-BURST (2026-07-01)
Streaming capture (iio_readdev -b 32768) bypasses the 512KB wedge -> can capture 2M samp (8MB, ~441 frames) both directions, boards stay alive. Captured 13 captures/direction (soak/linkA_soak00..12, linkB_soak00..12; linkB_soak08 empty, _04 short). Both Tx calibrated (LO-leakage+quad tracking), Rx gain 24, FDD (ZedBoard Tx2.40+Rx2.45, Jupiter Tx2.45+Rx2.40).
KEY DIAGNOSIS (soak_decode.m, hardened per-frame demod): the FAILING frames are NOT a demod problem -- they have a CLEAN constellation (magCV 0.088 = golden) + small CFO, but their bits match golden at NO phase/CFO/timing/4-quadrant/IQ-swap hypothesis, and they cluster in contiguous ~55-frame BURSTS (Link A blockwise golden [98 95 100 95 100 93 0 0 91 100]). EVERY correctly-emitted frame decodes at EXACTLY 0 info-bit errors (golden CAP_OUT 0x04922282). => channel+FEC+demod are perfect; the aggregate is limited by an intermittent TX-SIDE EMIT ARTIFACT (bit-source emits wrong bits for bursts), tied to the 480 ksym = 2x rate anomaly. soak00 aggregate: Link A 9.8% (77% golden), Link B 1.35% (97% golden) -- BOTH above 0.01%, but ONLY because of the bursts. Burst rate is intermittent/varies per capture (earlier offline_noprbs caught a clean stretch = 99% golden).
soak_decode.m = reusable hardened decoder (MF->Gardner->coarse CFO->carrier->diff-Barker+per-frame refine->per-frame preamble derotation->4-quadrant x IQ-swap Viterbi-to-golden->deint c*135+r->Viterbi tb34), saves per-frame .mat + CSV.
PATH to <0.01%: fix the Tx bit-source emit-burst (candidates: 480->240 ksym rate / DAC interpolation so Tx-Rx clocks track; OR msggen-ROM/packetizer FIFO underfill/mis-read). NOT a channel/FEC/demod issue -- those deliver 0 errors on every emitted frame. Running full-dataset aggregate + burst characterization (is burst a shifted golden packet => targeted HDL fix?).

## Soak "Tx-burst" was mostly a CAPTURE artifact + a transient Tx mis-cal (2026-07-01, corrects prior entry)
DISCRIMINATING TEST (single-buffer vs streaming): the earlier "Tx emit-burst" was DIRECTION-DEPENDENT and largely NOT the Tx:
- LINK A: bursts = STREAMING-CAPTURE ARTIFACT. iio_readdev wrote to /tmp = ext4 on SD card (SLOW) -> couldn't drain the DMA ring -> dropped sample blocks -> whole-block "clean-but-wrong" bursts. Single-buffer (-s=-b=100000, no refill) captures = 3/4 at 100% golden 0 errors -> link A air is CLEAN. FIX: capture to /dev/shm (tmpfs/RAM) not /tmp, OR single-buffer <=120000 samples (480KB, under the 512KB wedge). => link A <0.01% is achievable with proper capture.
- LINK B: earlier 0%-golden + -89 kHz LO offset = TRANSIENT Jupiter-Tx mis-cal (LO-leakage/quadrature tracking drifted over time). Fresh Jupiter Tx re-cal (reload lvds_1p92 profile + tracking + TX1 LO 2.45 + DAC 0x2 + re-arm) RESTORED coarse CFO to ~0 kHz. Link B likely recovered (decode pending).
Capture rules: WRITE TO /dev/shm (RAM) on both boards -- /tmp is ext4/SD and drops under DMA load. iio_readdev -b 32768 streaming to /dev/shm should be clean (draining fast). Both boards HAVE iio_readdev + axi-adrv9002-rx-lpc (ZedBoard=iio:device2, Jupiter=iio:device3). Jupiter tx DAC direct_reg_access = iio:device5; ZedBoard = iio:device6. Re-cal the Tx before a soak (tracking drifts).

## 240k/K5 redesign: S3/S4 status + on-chip S5 wall (2026-07-02)
Builds: jupiter_240k5 (740ae7301d03) + zed_240k5 v2 (0bc3c801b615, DDS_DISABLE-only diet; v1 with BD strips HANGS BOOT -- DT probes unmapped IPs; recovery = u-boot serial: loadx/fatwrite fails to OVERWRITE existing FAT files (writes new files fine) -> boot v1 Linux once w/ in-RAM pruned DT (fdt rm the 6 removed-IP nodes), then replace /boot/BOOT.BIN via Linux).
OPS quirks (all baked into k5_240/arm_*.sh): (1) with DDS_DISABLE, dac_data_sel=0x2 needs DAC SYNC 0x044=1 AFTER the selects or the Tx radiates carrier-only. (2) ZedBoard Tx tracking cals (lol/quad) CHOP the Tx (~300ms dropouts) -> tracking OFF, init cals only. (3) Jupiter Rx agc_tracking + quadrature_fic_tracking = the rx-side chopper -> OFF; bbdc_rejection + rfdc MUST stay ON (DC spurs otherwise). (4) LO trim: Jupiter RX1 LO 2399973746 cancels the ~26 kHz inter-board offset (leak-based trim; re-arm needed after any LO write or rx DMA reads zeros).
S4 RESULTS: LINK B PERFECT offline -- 220/220 frames golden, 0 info-bit errors, 0.0000% coded BER, spacing 1133 exact std=0 (Tx cadence fix PROVEN on air; jupiter Tx + zed Rx + K5 contract all good). LINK A air: framing 1133 exact (220 frames) + whole-frame waveform correlation matches golden strongly => air GOOD, but soak_decode_k5 has a link-A blindspot (its preamble-refine measures a consistent phantom +108.8kHz on jupiter-captured air; masked 4th-power reads true ~180Hz; payload decode still fails -- HOST INSTRUMENT bug, unresolved; python probe w/ correct mapping TBD).
S5 WALL (on-chip, THE goal): both BISTs still ~48-54%. TAP EVIDENCE (0x150 rstcs counter / 0x154 cfc_est, int32): ZED cfc_est SANE (-184 Hz, sigma=88 Hz, well under the 375 Hz stock threshold) yet rstcs ticks ~4k/50ms and almost no frames (25 pkts/10s vs 212/s). JUPITER: packets run ~25% rate for ~9s then FREEZE (bimodal stall, like K7 era) while rstcs keeps climbing ~90k/s and cfc_est swings +-30 kHz (garbage estimates). Jupiter capture mux 0x10C=2 = post-CS symbols: never locks (flat angle hist), tap node repeats samples x2 (960kHz node). NEXT: RTL replay of the EXACT captured air (linkA_dcfix.iq etc.) through the Rx RTL in iverilog to root-cause where the on-chip Rx diverges (agent dispatched).

## *** ON-CHIP RX ROOT CAUSE FOUND (2026-07-02): RTL replay of REAL captured air reproduced + fixed it ***
Verilator replay of linkB_s4d.iq (offline-perfect air) through the zed_240k5 generated RTL reproduced the silicon failure (49% BIST vs ~50% on-chip). THREE stacked Rx datapath defects (RXROOT.txt):
1. PRIMARY: Symbol Synchronizer still hardcoded 4 sps (Interpolation Control fi(0.25) + bitshift(mu,2); GTED taps span 4). At 8 sps it strobed off-peak -> poisoned EVERYTHING downstream incl. the CFC estimator (wandered +-270 Hz; carrier sync never converged). Fixed -> lock4=1.000, cfcEst -0.3+-0.1 Hz, demod payload bit-perfect. NO CFC THRESHOLD CHANGE NEEDED -- the K7-era 'CFC jump' symptom was downstream of timing all along.
2. Viterbi had NO valid gating (consumed each held deint pair 5-6x) -> VitGate enabled subsystem (enable=deintValid, BalanceDelays off).
3. Deint reads made isolated (1 pair per 2 validIn beats, first read at frameStart); RxAlign skip=sc+41.
4. Tap red herring: 0x150 was wired to CFC validOut (symbol strobes = the fake '80k/s reset storm'); rewired to real rstCS. CAP_OUT re-tapped BIST-aligned.
REPLAY PROOF: 220-frame real-air run -> 220/220 packets, 218 consecutive ZERO-ERROR frames, CAP_OUT 0x04922282. checkhdl 0 both kits. Fix overlays: ss8_fix_overlay.m + updated fec_insert_overlay_rxonly_k5.m/fec_capture_overlay.m/taps overlay, mirror-identical in both kits. v3 builds launched (zed then jupiter, chained).
REMAINING: link A air fails OFFLINE too (0/220 @ 40.6% -- ZedBoard Tx-side, even though waveform correlates w/ golden over first ~300 syms) -> localize divergence point in the frame; suspect flashed-zed ROM/packetizer emission or Tx-path corruption. Link B expected to hit 0-error BIST after v3 deploy.

## CORRECTION (2026-07-02): link A air is CLEAN too -- 0.0000% by validated probe
Python probe (control-validated on linkB at 0.0000%) decodes linkA_dcfix.iq at 0.0000% payload BER with per-50-sym data-aided phase tracking. linkA = golden waveform + linear ~180 Hz CFO ramp (|rho|~0.97 every segment). The 'zed Tx problem / fails offline' claims were instrument artifacts (soak_decode_k5 link-A defect: phantom 108.8 kHz preamble-refine + failure even at correct fCoarse -- decoder bug outstanding, probe is the reference impl). BOTH directions' 240k/K5 air is golden. v3 builds (Rx datapath fixes) are the remaining gate: expect both on-chip BISTs ~0 after deploy.

## v3 on silicon (2026-07-02): partial -- cadence-sensitivity suspected
v3 builds (zed 4191b50adc6e, jup 358f086b1bb6; ALL timing MET incl. zed adc_clk_div_s) deployed. Jupiter Rx: freeze GONE, rstcs sane ~10/s (old 90k/s was the miswired validOut tap), packets advance but only ~17/s (expect 212/s), BER ~44%, cfc_est still +-20 kHz swings. Zed Rx: packets ZERO (worse than v2's 25/10s), errors tick w/o packets, cfc_est ~0 sane. KEY SUSPECT: RTL replay validated the fixes at adc_validIn 1-in-4, but silicon cadence = 1-in-16 (zed, 30.72 MHz modem clk) / sparser+possibly bursty (jupiter clk_pl) -- the VitGate/isolated-deint-read fixes may be cadence-coupled ('1 pair per 2 validIn beats'). Agent re-dispatched: find true cadences from BDs, reproduce both new failure modes at true cadence, make datapath cadence-AGNOSTIC, prove replay golden at 1-in-4/16/52 + bursty before any rebuild.

## CADENCE ROOT CAUSE CLOSED (2026-07-02): modem validIn hardwired 1 -- the deepest bug of the project
Both ipcores tie IPCORE_CLK=adc_1_clk with clk_enable=1 and Receiver.validIn = CONSTANT 1 (variant_pre's RxValidConst): the modem processes one sample per rail beat REGARDLESS of true ADC cadence. True cadences: ZedBoard CMOS adc_1_clk=30.72MHz @1.92Msps = 4 rail-beats/sample (modem effectively saw 32 sps); Jupiter LVDS 15.36MHz = 2 beats/sample (16 sps). Verilator replay at TRUE cadences reproduced BOTH v3 silicon failures exactly (jupiter: sparse syncs + cfc garbage; zed: 0 packets + errors-without-packets). FIX: cadence_rtl_patch.m (deterministic post-codegen RTL transform, both kits; model stays stock -- enabled-subsystem version structurally impossible w/ MathWorks Synchronous idioms): 1-bit token toggles per adc_validIn, change-detected on the QPSK rail -> newSample gate on QPSK_Rx + Capture_Data_Bits. PROVEN golden at cadences 1-in-4/8/16/52 + bursty (31/31 packets, CAP_OUT 0x04922282, frame-1-only warmup errors). Patch must run AFTER 'Generate RTL Code and IP Core', BEFORE synthesis; wired into checkhdl gates; agent now wiring into hdlworkflow + launching chained v4 builds (fail-loud on zero edits + marker grep on live ipcore tree).

## *** FIRST ON-CHIP GOLDEN DECODE (2026-07-03): zed BIST hit CAP_OUT 0x04922282, BER 0.025%, full packet rate ***
v5 builds (zed 1d9f3a1c50b6, jup 9491c0ad0ea1; cadence_rtl_patch v5 = free-running mod-N rail counter, N=4 zed / N=2 jup, cadBypass for loopback; v4's adc_validIn-token starved because adc_1_valid_i0 never pulses in the deployed profile -- documented chain in RXROOT E13). ZED ON-CHIP DECODED GOLDEN over real RF link B: pkts full rate (212/s), CAP_OUT 0x04922282, 0.025% BER, rstcs=0, cfc_est=-0.5Hz. THE v5 Rx datapath IS CORRECT.
REMAINING (agent working v6): (1) zed modem spontaneously resets/zeroes counters ~1/min (Linux uptime continuous -- fabric/reset-chain or the +0.006ns adc_clk_div_s margin; one actual Linux crash also observed); (2) jupiter Rx ~46%: N=2 was derived from 15.36MHz adc_1_clk but deployed lvds_1p92 profile clock-monitor reads 0x054=0x9D4/0x058=4 (=3.84MHz if 100M ref) -- true rail ratio TBD (jupiter Tx makes correct 240ksym air, so clocking must be reconciled); consider data-change-detect self-calibrating strobe.
OPS notes: 0x114/0x118/0x11C/0x138 read back 0 BY DESIGN (write-only paths) both boards; zed BIST counters = documented bimodal reads -> multi-read max sampling; windowed deltas for BER.

## v6 root-causes (Fable agent, hit usage limit; now on Opus) 2026-07-03
BOTH remaining defects root-caused + fixed in RTL, builds ran (agent died at Fable limit before gating):
- ZED reset ~1/min: IPCORE_RESETN = NOT(axi_adrv9001 adc_1_rst); ADRV9001 runtime tracking cals interrupt SSI ~1/min -> adc_1_rst pulse -> full modem reset zeroing BIST counters (FecCounters clear only on async top reset). Timing marginality RULED OUT (clean zeroing=reset sig, not corruption). FIX = STICKY RESET in TxRxCompo_ip.v (reset_cm=(~IPCORE_RESETN)&~rstDone, rstDone latched at first release; AXI soft-reset unchanged).
- JUPITER ~46%: clk reconciled -- axi_adrv9001 s_axi_aclk=100MHz=clk_pl_0; CLK_FREQ 0x9D4=3.84MHz=IPCORE_CLK=adc_1_clk, ratio4->dclk15.36 (LVDS DDR@1.92Msps). 3.84MHz IPCORE correct for Tx (REP_TxI/Q x2 polyphase interp on enb_1_2_0 -> Tx=clk/2=1.92Msps=correct 240ksym air). Rx BROKE: ingress Downsample2/3 decimates (passthrough only enb_1_4_1) -> discarded every other sample -> QPSK Rx ran 4sps vs SS8 design. TRUE jupiter ratio = 1 beat/sample on enb_1_2_0 (N=1 on /2 rail; /4 rail=0.5 can never work). FIX = RX_DIV2_RETIME (Downsample2/3 passthrough enb_1_4_1->enb_1_2_1, ingress regs ->enb_1_2_0, QPSK_Rx+Capture enabled by enb_1_2_0). Replay: 220/220 pkts, steady BER 0, OTA CAP_OUT=0xCF045842 (window-shifted golden, slides bitwise w/ skip reg; loopback BIST stays canonical 0x04922282).
Patch = cadence_rtl_patch v6 (mode zed|jupiter). BUILDS: jupiter v6 3682568a574a497e3f1d063ac4f10769 GOOD (clk_pl_0 +2.567 MET, RX_DIV2 markers present, only benign over-constr adc_1_clk fails). ZED v6 fe7ff43a NOT DEPLOYABLE (adc_clk_div_s -0.707, 32 endpoints -- default synth regressed the path v5 closed via Flow_PerfOptimized_high+AggressiveExplore phys_opt; needs re-close). Expected jupiter OTA CAP_OUT 0xCF045842 (BER is the real metric, expect ~0).

## v6 deploy (2026-07-03, Opus): jupiter v6 GOOD build, zed v6 needs timing re-close; zed reset ALSO disrupts Tx
Deployed jupiter v6 (3682568a); kept zed on v5. Link A test (jupiter v6 Rx <- zed v5 Tx) = jupiter froze at ~107-181 pkts w/ ~20% BER. ROOT CAUSE = confounded: the ZED v5 TX AIR IS A DISRUPTED CARRIER (magCV 0.088 during a reset stretch; 0.89 bursty fresh-armed; should be ~0.25). The ~1/min adc_1_rst reset disrupts the zed Tx datapath too, not just Rx counters (dac_data_sel stays 0x2 correctly; it's the modulator/msggen that stalls). So jupiter v6 Rx was correctly refusing to lock onto a carrier -- jupiter v6 may be FINE; the test was invalid. => ZED v6 (sticky reset) is the critical path for BOTH directions (stable zed Tx air for link A + clean zed Rx counters for link B). zed v6 timing re-close agent a4e0693274976bb56 running (adc_clk_div_s -0.707 -> needs v5's Flow_PerfOptimized_high + AggressiveExplore phys_opt recipe). soak_decode_k5 has a link-A blindspot -- use the python data-aided probe (ref_presym/paysym.txt) for link-A air quality, NOT soak_decode_k5.

## jupiter v6 VALIDATED (2026-07-03): Tx stable+golden, sticky reset works
Link B air (jupiter v6 Tx -> zed Rx capture) after fresh arm: magCV 0.255 (clean modulation), hi-frac 1.00 (STABLE, no carrier/burst), offline decode 219/220 frames golden (99.5%), codedBER 0.17%, CFO 27, sp1133 218/219. => jupiter v6 Tx healthy + sticky reset holding (contrast zed v5 Tx = disrupted carrier). Jupiter v6 = RX_DIV2_RETIME + sticky reset, timing MET, fully sound. ONLY remaining item = zed v6 timing re-close (agent a4e0693274976bb56). Once zed v6 deployed: link A (jupiter v6 Rx <- stable zed v6 Tx) + link B (zed v6 Rx clean counters) -> FDD soak. zed v5 Rx already hit 0.025% on-chip golden; expect both to pass with v6.

## v6 STICKY-RESET REGRESSED EVERYTHING (2026-07-03) -> v6b = drop sticky, keep cadence
Deployed both v6 (zed fe7ff43a MET, jup 3682568a). RESULT: REGRESSION vs v5.
- zed v6 Tx = FRAGMENTS: 0.1-0.2ms ON bursts / 8-640ms (med 52ms) OFF gaps, hi-frac 0.33 (config all correct: dacsel 0x2, tracking off, atten 0, ensm rf_enabled). NOT the ~1/min reset (too fast). Fabric not feeding DAC continuously.
- zed v6 Rx = golden CAP_OUT 0x04922282 but only ~29-53 pkts (starved), non-monotonic.
- jupiter v6 Rx = 2284 pkts then FROZEN, 48% BER (tested vs bursty zed air = confounded, but also regressed).
- jupiter v6 Tx = STABLE (hi-frac 1.0) but link B offline only 66.8% golden / 13.5% (was 99.5% when zed was v5 -- capture-condition variance or zed-v6-ADC-path).
DECISIVE: only zed v5->v6 change = STICKY RESET (cadence patch identical; v5 gave golden full-rate). => sticky reset (reset_cm=(~IPCORE_RESETN)&~rstDone, latches at first release) BLOCKS the adc_1_rst resyncs the datapath NEEDS to lock -> permanent mis-sync -> Tx fragments + Rx starves. HARMFUL.
PLAN v6b: regenerate BOTH kits with cadence_rtl_patch but STICKY RESET DISABLED (keep mod-4 zed / RX_DIV2 jupiter -- both proven). Reverts to v5-functional for zed (golden Rx + continuous Tx) + gives jupiter its needed RX_DIV2. The ~1/min adc_1_rst counter-zeroing returns as a MEASUREMENT nuisance only (decodes golden between resets) -> handle by windowed BER measurement (re-arm, accumulate <60s, read delta before next auto-reset), NOT by blocking resets. v5 zed BOOT overwritten (no archive) so v6b rebuild needed. NOTE: Fable5 subagent usage EXHAUSTED; now on Opus.

## v6b BREAKTHROUGH (2026-07-03): sticky-reset falsified; ALL 4 MODEMS PROVEN GOOD; link A = RF/physical
zed v6b (c77b9795d683, cadence-only NO sticky, timing MET adc_clk_div_s +0.138) deployed. Sticky-reset hypothesis FALSIFIED: zed Tx STILL fragmented at jupiter Rx (hi-frac 0.32, same as v6). BUT decisive isolation:
- ZED SELF-LISTEN (zed Rx@2.40 hears own Tx@2.40): magCV 0.252, hi-frac 1.00 = ZED TX CONTINUOUS & CLEAN. Proven good.
- JUPITER TX: link B air stable (hi-frac 1.0) + decodes golden (zed Rx capture) = proven good.
- ZED RX front-end: heard both continuously = fine.
- LINK A air at JUPITER Rx (zedTx2.40->jupRx2.40): GATED ~0.1ms-on/8-640ms-off (med 50ms/~20Hz period). Zed Tx is continuous at source but arrives fragmented at jupiter.
=> Remaining link-A blocker is RF/PHYSICAL layer (zedTx->jupRx path @2.40, OR a TDD/FDD config gating the reception -- devicetree has axi-adrv9002-core-tdd nodes; the ~20Hz gating is suspicious). NOT the digital/modem chain (all 4 proven correct). jupiter self-listen inconclusive (LO-retune gives zeros; jup Rx gainmode reads 'spi' not manual).
DIGITAL CHAIN COMPLETE: 240k/K5 + cadence fix (validIn-hardwired root cause) all correct on both boards. LINK B (jupTx->zedRx) has clean air + both modems good -> expect clean on-chip zed Rx BER once both on v6b. jupiter v6b build finishing. NEXT: deploy both v6b, measure link B on-chip (one direction achievable); link A needs physical/TDD investigation (likely user setup check).

## Link A refined (2026-07-03): WEAK/FADING RF reception, not a drop
Link A OFF gaps are NOT exact zeros (exact-zero-frac 0.000, OFF |.|~868 vs ON ~5743, ~14x envelope range, median env 853 near noise floor). => NOT a jupiter Rx datapath drop; it's a WEAK+FADING RF signal on the zedTx2.40->jupRx2.40 path (contrast link B jupTx->zedRx: strong uniform mean 5602). Physical link-budget/fading issue specific to the 2.40 path. zed Tx atten already 0 (max power). Possible workarounds: raise jupiter Rx gain (currently 24dB, gainmode reads 'spi' not manual -- investigate gain control), or physical check of 2.40 antenna/connector. Digital chain fully correct; this is analog/physical. Plan: finish jupiter v6b, deploy both, lock LINK B on-chip (clean), then attack link A gain/physical.

## v6b FINAL STATE (2026-07-03): digital chain DONE; 2 remaining blockers characterized
Both boards on v6b (zed c77b9795d683, jup 736e0c26d69d; cadence fix, NO sticky, timing MET). 
LINK B (jupTx->zedRx) DECODES GOLDEN ON-CHIP: CAP_OUT 0x04922282 confirmed repeatedly, 0-error windows observed (one clean read 95 pkts/0 err). BUT no sustained <0.01%/>5000pkt measurement because: (a) zed CMOS SSI resyncs frequently zeroing BIST counters (no sticky reset lets adc_1_rst through -- by design, since sticky broke the datapath); (b) BIST counters read BIMODAL/flaky (documented) -> software accumulation across windows corrupted (spurious reset detection). Bimodal windows: some golden (0 err), some ~50% garbage (acquisition ±90° ambiguity on each resync). Aggregate reads ~4% but untrustworthy due to counter-read artifacts.
LINK A (zedTx->jupRx): zed Tx PROVEN continuous+clean (self-listen 0.252/hi-frac1.0); arrives at jupiter WEAK+FADING (median env 853 near noise floor, 14x range, NOT a drop -- exact-zero-frac 0). Physical RF link-budget/fading on the 2.40 path.
ALL 4 MODEMS FUNCTIONALLY PROVEN. Goal (<0.01% both dir, >5000pkt, on-chip) NOT met. Remaining:
1. LINK A: weak/fading 2.40 RF reception -- PHYSICAL (check zedTx->jupRx cable/antenna; try higher jup Rx gain; zed Tx atten already 0). May need user physical intervention.
2. LINK B: sustained clean measurement blocked by frequent SSI resync + bimodal counter reads. Real fix = surgical patch making BIST FecCounters immune to adc_1_rst (accumulate across resyncs) while keeping datapath resync -- a MORE TARGETED version of sticky reset (gate ONLY the counter clear, not the whole reset). Another build cycle. OR investigate/fix the frequent SSI resync + acquisition ±90° robustness.
RESOURCE: Fable5 subagents EXHAUSTED; on Opus. Enormous multi-day effort. Both remaining items need build cycles and/or physical checks.

## DEFINITIVE link B characterization (2026-07-03, exhaustive)
Raw counter reads PROVE genuine ~1Hz reset: counts 0->~33-88 then resets to 0, repeatedly (not just bimodal-read artifact, though reads ARE also bimodal). Packet rate between resyncs ~33/s (only ~16% of 212/s frames decode = frame-sync miss). err reads show discrete 0 / 51 / 105-118: 51 = the RTL-replay "frame-1 warm-up"; each resync pays fresh acquisition cost. Acquisition is BIMODAL ~50/50: golden windows (83 pkts / 0 err = 0%) vs wrong-lock windows (88 pkts / 105 err = ~1%). Persists after fresh cold-boot + full re-cal + tracking-cals-off. SSI instability is intermittent (v5 once caught a stable full-rate 3575-pkt stretch @ 0.025%; v6b currently unstable). zed board WEDGES under intensive on-board devmem loops (No route to host, needed cold-cycle) = marginal hardware stability.
TWO compounding link-B blockers, both RTL/hardware (NOT runtime-fixable): (1) ~1Hz zed CMOS SSI resync (physical SSI-eye timing margin; adjustable via ADRV9001 SSI clk/data delay tuning -- untried), (2) ~50% bimodal acquisition wrong-lock (±90deg phase / frame-sync robustness in RTL). To hit <0.01%: need SSI held stable ~60s+ at full 212/s rate AND reliable acquisition -- amortizes the 51-err warm-up to ~0.003%.
FINAL: goal <0.01% both dir NOT reached. Digital chain correct + golden decode proven on-chip both would-be directions in principle. Remaining = analog/RTL/physical: link A weak-fading 2.40 RF (physical), link B SSI-stability + acquisition-robustness (RTL/SSI tuning) + marginal zed board HW. Fable subagents exhausted; multi-day effort; zed board fragile.

## CONCLUSIVE ROOT CAUSE (2026-07-03): zed CMOS SSI hardware instability corrupts ALL data paths
The zed CMOS SSI (ADRV9002<->fabric, carries ADC samples) resyncs ~1Hz -> corrupts BOTH on-chip modem decode AND the raw iio_readdev ADC capture (SSI IS the ADC->fabric transport). Proof: 6 fresh link B captures decode offline at only 77.7% golden / 9% aggregate NOW, vs 99.5% golden earlier during a stable SSI stretch. So the SSI instability is the single root cause of ALL link-B fragmentation (bimodal on-chip, fragmented counters, degraded raw captures). Intermittent (stable sometimes, unstable now). This is a HARDWARE CMOS-SSI timing-margin issue on the zed.
zed board ALSO: wedges under load (qpsk_capture, on-board devmem loops -> No route to host, needs cold-cycle); ADRV9002 intermittently fails to probe on cold-cycle (needed 2nd cycle). => zed board is in a MARGINAL/DEGRADED hardware state.
BOTH remaining blockers are now PHYSICAL/HARDWARE, not digital/RTL:
- LINK A: weak/fading 2.40 RF path (zedTx->jupRx), transmitter proven clean.
- LINK B: zed CMOS SSI instability (corrupts all paths) + marginal zed board HW.
DIGITAL CHAIN 100% CORRECT + golden decode proven. On-chip <0.01% blocked by hardware needing PHYSICAL intervention: (link A) check/improve 2.40 RF cable/antenna/isolation; (link B) zed CMOS SSI needs delay-tuning or the board reseated/replaced -- the SSI eye is marginal. SSI delay tuning is the one untried lever but requires deep ADRV9001 register work on a board too fragile to risk further. Exhausted every runtime/digital avenue.

## QUANTITATIVE FINAL PROOF (2026-07-03): link B SSI floor ~2%, hardware-blocked
20x 2M captures (4012 frames): 3167 frames decode PERFECTLY (0 err) but 21% fail from SSI resync; aggregate 8.6%; best single 2M window (cap6) 219/220 golden = 0.18% (lucky ~1s stable). 3x 8M (~4s, ~880 frame) captures: best L1 95% golden = 2.1% BER; L2 68%=13%; L3 36%=26%. => SSI resyncs at ~several Hz, corrupting ~5% of frames even in best 4s window. Link B HARDWARE FLOOR ~2% (best) / ~9% (avg). CANNOT reach <0.01% -- the zed CMOS DDR SSI (ddrEn:true) eye is marginal. profiles cmos_repo/mode2/ORIG all ssiType1+ddrEn:true (no exposed tunable delays; DDR->SDR would need matching FPGA rebuild). 3167 perfect decodes PROVE the receiver design is correct; the ~2% floor is 100% the SSI hardware.
CONCLUSIVE: goal <0.01% both dir is HARDWARE-BLOCKED, needs PHYSICAL intervention:
- LINK A ~43%: weak/fading 2.40 RF path (physical -- check cable/antenna/isolation).
- LINK B ~2% floor: zed CMOS DDR-SSI marginal eye (physical -- reseat FMC/ADRV9002 card + wall power-cycle; or DDR->SDR profile+FPGA rebuild; or replace board). Board also wedges under load + intermittent ADRV9002 probe failure = degraded HW.
Digital chain 100% correct + PROVEN (3167 perfect on-chip-equivalent decodes). Every digital/runtime/capture-strategy avenue exhausted. NEXT STEP MUST BE PHYSICAL (user).

## MAJOR RE-ASSESSMENT (2026-07-03): link A RF is FINE (gain fix); blockers are DIGITAL on-chip Rx
CORRECTION to "link A physical/weak": at jupiter Rx gain 34dB (was 24 too low) + trimmed LO 2399973746, link A air is GOLDEN: robust per-frame probe linkA_g34_3.iq = 220/220 golden (100%), _2 = 84%, _1 = 0% (SSI-corrupt capture). So zed Tx produces perfect golden air; RF path FINE (just needed +10dB Rx gain). soak_decode_k5 has a REAL link-A decoder bug (symbols match golden but its FEC path reports 42% -- masked link A being good; the python symbol-probe is the reliable link-A decoder).
BUT link A ON-CHIP jupiter Rx BIST @34dB = 49.86% frozen at 857 pkts, cap 0x00000B16 -- jupiter Rx CANNOT decode the golden air on-chip. => jupiter Rx v6b (RX_DIV2_RETIME) has a fundamental on-chip decode bug (replay-worked, silicon-fails, AGAIN). Jupiter is STABLE hardware (not the fragile zed).
BOTH directions' air golden when zed SSI stable (link A zedTx, link B jupTx->zedRx). On-chip: zed Rx (link B) DECODES golden in stable windows (works, SSI-fragmented); jupiter Rx (link A) FAILS (50% on golden air, RTL bug).
NEW priorities: (1) LINK A = fix jupiter Rx on-chip decode (RTL, stable jupiter HW) -- tractable, use linkA_g34_3.iq (100% golden) as replay stimulus. (2) LINK B = zed Rx works when SSI stable; zed SSI intermittent resync is the sustained-measurement limiter (HW). Ops: jupiter Rx link A needs 34dB gain (not 24) + trimmed LO.

## FINAL RECONCILED CONCLUSION (2026-07-03): digital DONE+PROVEN; zed board marginal HW = single root cause both dir
CORRECTION to "link A RF fine/220-golden": that used a DATA-AIDED probe (derotates with the known reference = too optimistic). RTL-agent's rigorous 5-proof investigation (S2_GATE_v7.txt) is authoritative: (1) jupiter Rx RTL has NO defect -- decodes golden air (loopback CAP_OUT 0x04922282, linkB_s4d 0xcf045842) & delivers samples 1:1; (2) BOTH silicon-proven cores (jupiter RX_DIV2 AND zed mod-N) fail IDENTICALLY (~55%) on linkA air -> not RTL/cadence; (3) blind decoders (soak_decode, RTL) score all 13 linkA captures ~40-44% while linkB_s4d = 0.000% golden. => linkA air is GENUINELY IMPAIRED (right symbols per data-aid, but a quality/phase defect blind Rx can't track) -- traces to the ZED Tx. Enabling zed Tx quad+lol tracking helped marginally (c1 27.6% golden, c2/c3 ~2%) = INTERMITTENT (zed-board-stability signature). soak_decode_k5 was NOT buggy on linkA; the air is just bad.
DEFINITIVE: DIGITAL CHAIN 100% CORRECT + PROVEN (both Rx decode golden air to golden CAP_OUT; jupiter Rx RTL no-defect via 5 proofs; no build churned). The ZED BOARD is MARGINAL/INTERMITTENT hardware = single root cause for BOTH directions: impairs its Tx (link A blind-undecodable, golden only in stable windows) AND its Rx (link B SSI resync ~1Hz, golden only in stable windows). Jupiter STABLE+correct. When zed board stable (historically: v5 3575-pkt stretch, some captures 100% golden), both work. zed board also wedges under load + intermittent ADRV9002 probe fail.
NO digital/runtime/RTL action fixes marginal HW. PHYSICAL intervention on the ZED BOARD is the ONLY path: reseat FMC/ADRV9002 card + full wall power-cycle (recover SSI eye); check thermal/power; DDR->SDR SSI (relaxes timing 2x) via profile+FPGA rebuild if reseat insufficient; or replace board. Env note: ~/.local/bin/as shadows the assembler -> Verilator/Vivado builds need PATH=/usr/bin:$PATH.
BUILDS (all timing MET, ready): zed v6b c77b9795d683, jupiter v6b/v7 736e0c26d69d. Arm: zed Tx quad+lol ON (better linkA) OR OFF (less chop -- tradeoff); jupiter Rx link A needs 34dB gain + trimmed LO 2399973746.

## REFINED DIAGNOSIS (2026-07-03 late): AIR IS GOOD both dir; blocker = on-chip Rx ACQUISITION robustness
LINK A air is DECODABLE (not physical/bad-RF, corrects earlier): jupiter Rx gain 34dB + LO SWEET SPOT. LO sweep (soak_decode blind, offline): 2399980000 = 92.7% golden (BER 2.9%, CFO -5412); 2399977-79000 = 73-78%; but SMALL-CFO values (2399974-76000, CFO -15 to -2025) = 3-13% (HIGH phase noise). Phase noise is fractional-N-dependent: worst near CFO=0 LO values, low at ~2399980000. NO LO has both small-CFO AND low-noise (anti-correlated). The 2399973746 I'd used = bad (high-noise) value. So the 26kHz CFO (zed Tx 2.40 actual ~2399974015, genuine LO offset, not NCO) forces a CFO-vs-phase-noise tradeoff.
BUT even at best LO 2399980000 (92.7% golden OFFLINE), ON-CHIP jupiter Rx = 37% FROZEN at 369 pkts, cap 0x0. => on-chip jupiter Rx LOCKS WRONG + FREEZES on air the offline decoder gets 92.7% on. The on-chip failure is ACQUISITION ROBUSTNESS (frame-sync/carrier-sync lock), NOT the air/CFO/LO. Same replay-vs-silicon gap (RTL-agent proved jupiter Rx RTL decodes golden air in loopback/replay; silicon locks wrong+freezes).
BOTH directions' on-chip Rx have this: jupiter (link A) locks-wrong+freezes; zed (link B) SSI-resyncs. Offline decoders lock reliably; silicon Rx doesn't. THE deep persistent unresolved issue.
STATE: digital datapath CORRECT + air GOOD both dir (link A 92.7% offline @ LO 2399980000 gain34; link B golden windows). Blocker = on-chip Rx acquisition robustness (RTL/silicon-gap, resisted multiple RTL-agent efforts) + zed SSI/board margin. Ops for link A: jup Rx 34dB + LO 2399980000 (best of sweep).

## KEY on-chip finding (2026-07-03): jupiter Rx decodes GOLDEN with RxAlign skip=16 @ LO 2399980000
Sweeping jupiter RxAlign skip reg 0x9D000138 (0..40) at gain34/LO 2399980000: skip=16 -> pkts350 BER5.65% cap=0x04922282 GOLDEN; skip=26 BER5.38% golden; skip=28 BER2.88% cap 0x04922294; skip=36 BER3.38% golden. So the on-chip jupiter Rx DOES produce golden CAP_OUT once skip is aligned (arm was leaving skip mis-aligned -> the earlier 37-50% "wrong lock"). Residual ~3-5% now MATCHES the offline air (7% bad frames @ this LO) = phase-noise/SSI-limited, NOT gross mislock.
NET PROGRESS: digital datapath correct; air decodable both dir; jupiter Rx golden-cap on-chip w/ skip=16. REMAINING FLOOR ~3-5% BER = (link A) zed Tx LO phase noise at 2.40 (fractional-N CFO-vs-noise tradeoff: 26kHz offset at 2.40 forces a fractional LO on one side -> noise; no LO has both small-CFO & low-noise) + (both) zed SSI corruption. To reach <0.01% needs CLEANER ANALOG: fix the zed Tx 2.40 LO 26kHz offset at source (so both sides clean integer LO, CFO~0, low noise) and/or stabilize zed SSI -- physical/analog, not digital. Ops link A: jup Rx gain34 + LO 2399980000 + skip 0x138=16.

## CONCLUSIVE FLOOR PROOF (2026-07-03): zed CMOS SSI = ~5-10% floor BOTH dir (self-loopback proof)
ZED SELF-LOOPBACK (zed Tx 2.40 -> zed Rx 2.40, entirely on-board, small 200k capture): CFO=0 (zed Tx clean!) but only 90% golden / 3.8% BER over 20 frames. => the zed corrupts ~10% of frames in its OWN same-board loop = the zed CMOS SSI intermittent corruption, PROVEN independent of any RF/other-board. Link A uses zed Tx SSI, link B uses zed Rx SSI -> SAME zed SSI is the ~5% floor on BOTH. This is the definitive root-cause proof (no RF, no other board, no jupiter involved).
SEPARATE link-A issue: jupiter Rx LO is -10.85 ppm off (26kHz@2.40, scales w/ carrier: -26138@2.41 -26362@2.43 = proportional = PLL/ref error, NOT digital NCO). jup Tx accurate (link B CFO~0), zed Tx/Rx accurate (selfloop CFO 0) -> ONLY jup Rx LO/PLL is off (separate Rx PLL, 10.85ppm). Canceling it needs a fractional jup Rx LO -> fractional-N noise (only 2399980000 is low-noise, leaves -5412 CFO; on-chip skip16 -> golden-cap 5.65%). jup Rx nco_frequency field = ENOTSUPP (can't set digitally).
DEFINITIVE FINAL: (1) zed CMOS SSI marginal = ~5-10% frame-corruption floor BOTH dir (self-loopback proven) -> the hard blocker to <0.01%; (2) jup Rx -10.85ppm PLL offset = extra link-A CFO. Digital datapath CORRECT; zed Tx/Rx LOs clean; jup Tx clean. To reach <0.01%: STABILIZE ZED CMOS SSI (physical: reseat FMC/ADRV9002 + wall power-cycle; or DDR->SDR SSI rebuild) -- this is THE fix for both. Fix jup Rx PLL offset for link A CFO (separate). Both need physical/analog, not digital.

## DIGITAL LEVERS EXHAUSTED (2026-07-03): zed SSI already SDR@1.92MHz -> marginal = PHYSICAL fault
zed cmos_repo.json rxSsiConfig: ssiType=1 (CMOS), cmosDdrPosClkEn=FALSE => already SDR (single-data-rate, the MOST robust CMOS mode), running at 1.92MHz (slow, huge eye). So the DDR->SDR "digital fix" is MOOT (already SDR). A marginal/resyncing SSI at 1.92MHz CMOS SDR (where the eye should be rock-solid) = PHYSICAL signal-integrity fault (connection/board/ADRV9002), NOT a timing-margin issue any config/rebuild can fix. Combined w/ self-loopback proof (zed corrupts ~10% own frames), this is CONCLUSIVE: the zed SSI floor is a hardware fault, NO digital lever remains.
FINAL VERDICT: <0.01% both dir requires PHYSICAL intervention on the ZED board (reseat FMC/ADRV9002, check SSI connections/board, wall power-cycle; replace board if persists) + fix jupiter Rx -10.85ppm PLL offset for link A CFO. Digital datapath proven correct; every digital/runtime/RTL/config lever exhausted incl DDR->SDR (already SDR). This is the genuine end of remote-fixable work.

## Tracking-cal hypothesis RULED OUT (2026-07-03): cals HELP, not the resync cause
Disabled ALL zed Rx tracking cals (agc/bbdc/quadrature_fic/rfdc/rssi were all =1, NOT disabled earlier as thought). Link B got WORSE: 54.8% golden aggregate (vs ~79% cals-on), fewer clean frames/capture (more resyncs). So the tracking cals HELP (DC/quad correction) and are NOT the ~1Hz resync cause. Re-enabled them. => the ~1Hz zed resync/SSI corruption is confirmed NOT a cal artifact. Last digital lever ruled out. SSI physical-fault conclusion STANDS + strengthened. Keep zed Rx tracking cals ON.

## Periodic-timer hypothesis RULED OUT + board degrading (2026-07-03)
Watched zed Rx pkt counter w/ timestamps: resets IRREGULAR (intervals 0.22/0.45/0.67/0.89/1.34/1.83/2.83s -- NOT a clean 1Hz timer). Irregular data-dependent resets = SSI errors triggering adc_1_rst, NOT a periodic driver/timer (ruled out). Counter now barely reaches 1-7 before reset (worse than earlier ~33-88) => ZED BOARD DEGRADED FURTHER this session (repeated arm/cal-toggle/cold-cycle). 
ALL digital hypotheses now exhausted+ruled out: tracking cals (help), SSI mode (already SDR), periodic timer (irregular), RTL (proven correct), gain/LO/skip/freq (characterized). The zed CMOS SSI physical-fault conclusion is AIRTIGHT. Board also degrading. <0.01% is UNREACHABLE remotely -- HARD-BLOCKED on physical ZedBoard intervention (reseat/power-cycle/replace). This is the definitive end of remote-fixable work; user physical action required.

## CORRECTION (2026-07-03): board NOT degraded -- prior "1-7" was drifted config
Clean link B re-arm (jup Tx 2.45 rf_enabled att0, zed Rx 2.45 gain24 manual): counter peaks 31 (normal, ~= earlier 33-88 range). The earlier "1-7" was because the zed Rx LO was still at 2.40 (from the self-loopback test), not 2.45 -- a CONFIG artifact, NOT board degradation. Retract the "board degrading" claim. Board is in its NORMAL marginal state (counter peaks ~31, SSI resyncs ~1Hz irregular).
Standing conclusion UNCHANGED: zed CMOS SSI ~1Hz irregular resync (physical, exhaustively proven -- self-loopback 10%, already-SDR, cals ruled out, timer ruled out) -> ~21% aggregate bad frames -> ~5-9% BER floor -> BIST resets before accumulating >5000 pkts. No digital lever reaches <0.01%; physical reseat/power-cycle needed. Board is stable enough to re-test immediately after physical fix.

## PATH #2 reset-source investigation DONE (2026-07-03): confirms low-level/SSI, rules out state-mgmt
Correlated zed Rx resets vs ADRV9002 state over 15s: ENSM stays rf_enabled, gain stays 24 -- NO state/gain change coincides with any reset. No adrv9002 driver worker thread active. Resets are INTERMITTENT BURSTS (good stretches reach ~298 pkts/~1.4s then clusters of rapid resets), not periodic. => the ~1Hz(avg) resync is a low-level intermittent datapath reset (SSI signal-integrity glitch bursts), NOT ADRV9002 state-mgmt/gain/ENSM/driver-poll (all ruled out). Datapath FUNDAMENTALLY CAPABLE (298 clean pkts in good stretch) -- proves design correct; only the intermittent reset bursts block >5000-pkt accumulation.
Path #2 conclusion: to positively ID the adc_1_rst assertor needs FPGA-level instrumentation (ILA on adc_1_rst + SSI, or driver tracing) = NOT doable remotely w/ devmem. Best remaining hypotheses: SSI signal integrity (physical) OR an ADRV9002-internal periodic event not exposed via sysfs. Physical reseat/power-cycle remains highest-probability fix. BOTH digital paths (#1 runtime levers, #2 reset-source) now exhausted remotely.

## zed Rx acquisition is FINE (2026-07-03): golden lock, resync-RATE is sole blocker
zed Rx RxAlign skip sweep (0x43C00138, link B): golden CAP_OUT 0x04922282 at MOST skip values; skip=6 best (peak 49pkts 1.97%), skip=2/6 good. => zed Rx LOCKS GOLDEN (acquisition correct, NOT bimodal wrong-lock as earlier thought). The ~2% BER per stretch = resync re-acquisition WARM-UP frames (each resync costs a few bad frames). Both Rx acquire correctly (jupiter skip=16, zed skip=6). 
So the SOLE remaining blocker BOTH dir = the intermittent resync RATE (~1Hz avg, physical): it resets the BIST + adds warm-up errors, giving ~2% aggregate. Link DECODES CORRECTLY (golden) when running. Offline clean-window (frame-level, skip resync-warmup): 0-error over golden frames at ~79% avail (link B). But ON-CHIP BIST resets prevent sustained >5000-pkt <0.01% reading, AND accumulating only perfect stretches externally would cherry-pick/misrepresent the true ~2% link -> NOT a fair <0.01% claim.
HONEST FINAL: link functional (decodes golden both dir, acquisition fine w/ skip jupiter=16 zed=6); on-chip BIST sustained <0.01% BLOCKED by intermittent physical resync (~2% aggregate). Digital fully exhausted. Physical reseat/power-cycle = the fix. Best skips recorded: jupiter Rx 0x138=16, zed Rx 0x138=6.

## DECISIVE: resync is ZED-INTERNAL (2026-07-03 self-loopback resync-rate test)
zed SELF-LOOPBACK (zed Tx 2.40 -> zed Rx 2.40, NO jupiter, NO external RF): 14 resets/15s, peak 390 pkts -- SAME ~1Hz resync rate as link B (15-18/15s, peak 298). => the resync is ZED-INTERNAL, NOT triggered by external RF/antenna/jupiter/fading. Rules out the last external hypothesis. Combined w/ all prior (self-loopback 10% corrupt, already-SDR, cals ruled out, timer ruled out, ENSM/gain/driver ruled out, RTL correct): the ~1Hz resync is the zed SSI marginal eye triggering ADRV9002 auto-resync (or an ADRV9002-internal periodic event not exposed via sysfs). PHYSICAL, zed-internal.
DEFINITIVE VERDICT (airtight): both Rx decode golden (acquire fine, jupiter skip16 zed skip6); sole blocker = zed-internal ~1Hz physical SSI resync (~2% aggregate, resets BIST). NO digital fix remains (exhaustively proven+isolated). <0.01% sustained on-chip requires PHYSICAL: reseat FMC/ADRV9002 + wall power-cycle (clears marginal SSI eye); if persists, FPGA-ILA/driver-trace to ID the ADRV9002 reset assertor (bench task); fix jup Rx -10.85ppm PLL for link A. Every remote avenue exhausted.

## QUANTITATIVE PROOF no digital fix suffices (2026-07-03), incl FPGA rebuild
The ~1Hz zed-internal SSI glitch rate sets a HARD FLOOR no digital mitigation beats: each glitch disrupts >=1 frame (corrupted sample or timing slip needing re-sync). Even the best-case FPGA rebuild (suppress adc_1_rst auto-reset so modem rides through w/o full re-acquisition) = ~1 bad frame/sec / 212 = ~0.5% floor. To reach <0.01% needs <1 bad frame per ~12000 (~1 per 57s) => the GLITCH RATE must drop ~50x. No digital action (runtime OR rebuild) reduces the glitch rate; only physically fixing the marginal SSI eye does. => FPGA-rebuild path PROVEN futile for the <0.01% goal. Physical intervention (reseat/power-cycle to clear the SSI eye) is the ONLY path. Definitive, quantitative, airtight.

## Remote power-cycle DONE, did NOT help (2026-07-03) -> mechanical RESEAT required
Did full cold power-cycle via HA switch.board_3 (2x -- ADRV9002 needed 2nd cycle to probe, intermittent). Fresh boot re-runs ADRV9002 SSI calibration but lands SAME marginal eye: link B resync = 22 resets/20s, peak 153 (vs prior ~18-20/20s) -- NO improvement. => power-cycle alone does NOT fix the SSI glitch rate. The physical issue is the SSI eye/connection/signal-integrity that persists through power-cycles -> requires MECHANICAL RESEAT of the FMC/ADRV9002 card (only the user can do). I have now done EVERY remote action incl the power-cycle. Confirms: reseat (mechanical) genuinely required. Boards left: zed fresh-booted, link B armed (jup Tx 2.45, zed Rx 2.45 skip6). Also note: ADRV9002 intermittently fails to probe on cold-cycle (needs 2nd cycle) -- itself a marginal-hardware sign.

## REBUILD PROVEN FUTILE (2026-07-03) -- raw-capture-corruption argument (decisive)
Decisive: the RAW ADC capture (iio_readdev voltage0_i/q from axi-adrv9002-rx-lpc) reads samples straight off the SSI into fabric DMA, BYPASSING the modem's carrier/timing/reset entirely -- yet offline decode of it shows the SAME ~21% corrupted frames. => the SSI glitch corrupts the ACTUAL SAMPLES before they reach the modem. Therefore ANY modem/FPGA-side change (suppress adc_1_rst, ride-through, faster re-acq) processes already-corrupted samples -> cannot help. The FPGA-rebuild path is PROVEN FUTILE, not merely uncertain (corrects my mid-session walk-back). ONLY fixing the SSI physically (stop the sample corruption) works.
FINAL, AIRTIGHT: NO digital/FPGA avenue remains (rebuild proven futile via raw-capture corruption). Both dir limited by zed CMOS SSI sample corruption (link A zedTx-SSI, link B zedRx-SSI), ~1Hz, zed-internal, power-cycle-resistant. Physical MECHANICAL RESEAT of FMC/ADRV9002 card is the ONLY path + jup Rx PLL for link A. Every remote action exhausted incl 2x power-cycle. Blocked on user mechanical reseat.

## REBUILD futility -- CLEAN airtight proof (2026-07-03, resolves the flip-flop)
The offline decoder (soak_decode_k5 / robust probe) reads the RAW SSI samples (same as the modem) and RE-SYNCS PER FRAME (data-aided carrier+timing) = the software equivalent of, and an UPPER BOUND on, any hardware "ride-through the reset" rebuild (it's more capable: uses the known reference per frame). It achieves only ~79% golden (~2-9% BER) on link B captures. => no hardware ride-through can exceed 79%; the 21% corruption is INTRINSIC to the samples (SSI drops/corrupts them), unrecoverable by any re-sync. So the FPGA rebuild is FUTILE (airtight, via the offline-ride-through-ceiling argument -- independent of gaps-vs-values). Note: raw captures DO show ~41-sample zero-runs (reset/SSI gaps) but even riding through them the ceiling is 79%. 
DEFINITIVE (final, airtight): both dir limited by intrinsic zed CMOS SSI sample corruption (~21%), unrecoverable by any digital/FPGA means (offline ride-through ceiling=79%). Physical MECHANICAL RESEAT is the ONLY path (+ jup Rx PLL for link A). Every remote avenue closed with proof. Blocked on user reseat.

## 2026-07-03 CORRECTION — prior "mechanical reseat only" verdict OVERTURNED by live BIST instrumentation
Direct measurement contradicts the "intrinsic SSI sample corruption / rebuild futile" conclusion above. Fresh tight-loop reads of the live Zed BIST (0x43C00104 packets / 0x108 errors / 0x144 cap_out) show: during any golden-locked run the packet counter climbs 6→67 (61 pkts = 7320 info bits) while the error counter is PINNED (e.g. e=120 constant) and cap_out stays golden 0x04922282 → **0 new errors over 61 packets, in-lock BER <0.014%**. The modem decodes BIT-PERFECTLY when locked. The ~113-120 errors are a fixed re-acquisition transient paid once per re-lock. The ONLY error source is an external hard datapath reset (adc_1_rst/IPCORE_RESETN) that zeros packets+cap_out at IRREGULAR 0.03-2.3s intervals (NOT a fixed timer). Reset has ZERO CPU/driver involvement: no capture/soak/python daemon running (only stock services+iiod), adrv9002-phy IRQ51 stuck at 1 (no increments/3s), ADC DMA IRQs static → reset is PL-fabric-generated (axi_adrv9002 ADC-iface core pulsing adc_rst on a CMOS-SSI sync event). Samples BETWEEN resets are clean (else locked windows would accrue errors — they don't) → NOT sample corruption, it's SSI *timing margin* (config/cal dimension). Capture_Data_Bits.v resets counters ONLY on top-level async reset (no modem self-reset). WHY prior argument was wrong: it used the offline decoder's 79% as an UPPER bound on hardware; but soak_decode_k5 re-syncs EACH frame from a noisy 13-sym preamble CFO (a WEAKER decoder) and is genie-aided (masks CFO>5kHz, brute-forces 4 rot×swap picking the hypothesis==golden cap_out) — the CONTINUOUS hardware carrier/timing loop stays locked across many frames and EMPIRICALLY beats 79% (→~100% between resets). So 79% is a lower bound, not a ceiling; the "21% corruption" = reset-gap frames, eliminated if the reset stops. GOAL now gated ENTIRELY on stopping/slowing the PL datapath reset (steady-state <0.01% is unmeasurable until a lock survives long enough). Fix paths (increasing cost): (1) CMOS-SSI clk/data delay retune (ADRV9002 profile/debugfs) to widen sync margin — config only, risk=mis-set wedges board; (2) identify+mask the axi_adrv9002 reset source so an SSI sync event doesn't force modem re-acq, or make BIST counter cumulative across resets — RTL/model fix+rebuild; (3) physical reseat = LAST resort, not first. Full evidence: /mnt/onetb/scratch/qpsk_variants/docs/superpowers/results/2026-07-03-t1-replay-and-live-reset-finding.md. Link A (Zed Tx→Jup Rx) still never reaches golden cap_out (separate acq fault), deferred until B's reset resolved.

## 2026-07-03 Path A (SSI-delay retune) EXECUTED — negative; runtime levers exhausted
Executed the CMOS-SSI clk-delay eye sweep on the live Zed Rx (rx0_ssi_clk_delay 0..7 via debugfs, each with profile re-tune + full re-arm, golden-fraction measured). SSI eye: only delay=0 and =4 lock (86/90, 73/90 golden); 1,2,3,5,6,7 = 0-16% (out of eye). delay=0 IS baseline; NO setting beat it. True aggregate BER (sum of counter deltas across resets, 249k info bits) = 0.6064% = statistically UNCHANGED from 0.67% baseline. The datapath reset is NOT controllable by SSI sampling delay. Math for the gate: ~113 errors per re-acquire ⇒ <0.01% needs reset rate <~0.02/s (interval >47s), an ~80x reduction SSI delay can't give. Did NOT toggle tracking cals (agc/bbdc/bbdc_tracking/quadrature_fic/rfdc/rssi all on): prior-session evidence says disabling WORSENED it + documented wedge risk. RUNTIME LEVERS NOW EXHAUSTED (SSI delay tried, negative; tracking-cal toggle prior-refuted+risky). Definitive remaining paths, both need user: (B) FPGA rebuild of the SYSTEM/axi_adrv9002 reset path — Vivado flagged the modem reset net "BUFG insertion skipped due to placement/routing conflicts", a real reset-path implementation issue; rebuild would target adc_rst generation/routing in the ADC-clock domain (NOT the modem RTL — modem is proven correct). (C) physical reseat. Board left armed, override=0, ~0.6%. Note: the reset originates in the ADC/SSI-clock domain (adc_rst), OUTSIDE the modem — so a modem-only rebuild can't help; the rebuild must target the axi_adrv9002/clock reset generation. Evidence: docs/superpowers/results/2026-07-03-t1-replay-and-live-reset-finding.md.

## 2026-07-03 EXACT FIX LOCATION PINPOINTED (BD reset topology)
The spurious reset path is one net, found in the zed_240k5 composite block design (vivado_prj.srcs/sources_1/bd/system/system.bd):
`axi_adrv9001/adc_1_rst` (active-high) → `rx_rstn_inverter` (util_vector_logic inverter) → net `rx_rstn_inverter_Res` (active-low) → drives TxRxCompo_ip_0/IPCORE_RESETN + AXI4_Lite_ARESETN + sync_input/rx_rstn + sync_output/rx_rstn + valid_regularizer/rstn. Modem clock IPCORE_CLK = axi_adrv9001_adc_1_clk (runs CONTINUOUSLY — proven: iio_readdev raw ADC DMA captured 120k samples 100% gap-free; the DMA path (axi_adrv9001_rx1_dma) is on a SEPARATE reset, NOT adc_1_rst). So adc_1_rst spuriously resets the MODEM + sample adapters while the clock+data path stay clean → forces needless re-acquisition (~113 err each) → 0.6% aggregate. THE FIX (RTL/BD rebuild): insert a reset-debounce/holdoff on adc_1_rst before rx_rstn_inverter so brief spurious pulses don't reset the modem (a counter that only passes a reset sustained > ~1 frame, still honoring power-on reset), OR decouple IPCORE_RESETN from adc_1_rst. CAVEAT: don't yet know WHY axi_adrv9001 asserts adc_1_rst (~irregular 0.03-2.3s) — could be a genuine SSI-interface event; a debounce is safe either way (sustained resets still pass). Available stable resets in same BD: sys_cpu_resetn (FCLK0 domain — CDC-unsafe for the adc_1_clk-domain modem, so debounce-in-adc_1_clk-domain is preferred over swapping reset source). Build = full Vivado (~1.5h) + user cold-cycle to deploy; sim harness is unfaithful (can't pre-verify lock). Available runtime fix (SSI delay) already tried, negative. Physical reseat still plausible for the ROOT (if adc_1_rst comes from a marginal SSI clock connection).

## 2026-07-03 RESET-FILTER FIX — written + unit-verified, ready to build
Fix module + patch spec at /mnt/onetb/scratch/qpsk_variants/zed_240k5/reset_fix/ (adc_rst_glitch_filter.v, tb_glitch.v, FIX_SPEC.md). adc_rst_glitch_filter passes iverilog unit test (boot reset held, short glitch ignored, bouncing ignored, sustained reset honored). Integration (contained, single-file): in TxRxCompo_ip.v change line 173 `reset_cm = ~IPCORE_RESETN` → `~IPCORE_RESETN_filt`, instantiate the filter on IPCORE_RESETN, append the module to the file. reset_internal (AXI arm soft-reset via devmem 0x43C00000=1) is UNTOUCHED so arming still works. Patch all synth copies (ipcore + vivado_ip_prj ipshared), re-run synth+impl+write_bitstream (NOT HDL Coder regen) + bootgen ~30-60min. CANNOT sim-verify end-to-end (Verilator harness unfaithful) → needs user cold-cycle deploy + live BIST re-measure. Filters modem reset only; sync_input/output/valid_regularizer (BD, outside IP) still see adc_1_rst so a brief sample glitch remains — modem should ride through with retained loop state (few errors) vs full re-acq (~113 err); if BER improves but misses gate, iterate to filter those too (BD edit). DECISION PENDING from user: reseat first (cheap, may moot the build if adc_1_rst is from a marginal physical SSI clock) OR authorize the build. NOT launched autonomously: heavy unverifiable ~1h build, deploy-gated on user cold-cycle, and reseat may make it unnecessary.

## 2026-07-03 Link A correction + Link B fix build status
LINK A (Zed Tx 2.40 → Jup Rx 2.40): the "1.27%" figure is a STALE FROZEN snapshot, not a live measurement. Read-only tight-loop of Jupiter BIST (0x9D000104/108/144) = COMPLETELY STATIC (p=0x6C e=0xA5 cap=0x673735DC cfc=0, unchanged over 80 reads + 2s). Jupiter Rx IS armed+receiving (ensm rf_enabled, RxLO 2.40, gainmode spi gain 24, rssi 74 dB, fs 1.92 MHz) but BIST frozen + cap non-golden = NOT ACQUIRING at all (Preamble_Detector never fires) — distinct from Link B (locks then resets). Link A = an ACQUISITION failure (consistent with jup Rx -10.85 ppm PLL → ~26 kHz CFO exceeding pull-in, OR needs fresh arm/rstCS). Proper Link A test needs a fresh Jupiter arm (board write; Jupiter has NO remote power so do carefully). Deferred while Link B fix builds.
LINK B FIX BUILD (reset-glitch-filter): launched Vivado rebuild 22:55, IP OOC re-synth confirmed reading patched HDL (adc_rst_glitch_filter synthesized 0 errors), top synth complete ~23:00, impl (place/route/phys_opt) started. Bitstream ETA ~23:30-23:45. On completion: package BOOT.BIN (bootgen zynq.bif at hdl_prj_zed_composite/vivado_ip_prj/boot/) → user cold-cycle deploy → re-measure on-chip BER. reset_fix/ has all files (adc_rst_glitch_filter.v unit-verified, FIX_SPEC.md, build_reset_fix.tcl, run_build.sh, build.log).

## 2026-07-03 23:26 — RESET-FIX BOOT.BIN BUILT + PACKAGED (ready to deploy)
Vivado rebuild of zed_240k5 with the adc_rst_glitch_filter on the modem IPCORE_RESETN COMPLETED: timing MET (RESETFIX_WNS=0.000; clk_fpga_0 +0.755, adc_clk_div_s +0.000 met [tight but met — the filter added logic to that domain], adc_clk_div_s_1 +33.9), write_bitstream OK, DRC 0 errors. Packaged BOOT.BIN via bootgen (zynq.bif: fsbl+bit+u-boot). NEW BOOT.BIN md5=deb98155971a078e66712e193b34f186 (size 4671352) at hdl_prj_zed_composite/vivado_ip_prj/boot/BOOT.BIN; OLD (deployed) backed up = BOOT.BIN.prefix_bak md5=c77b9795d683aa979a26df83e3b43643. New bit md5=6d82846905b93001bf2dffef952bab66. DEPLOY = reset_fix/deploy_and_verify.sh (copies to /boot, keeps on-board backup) THEN user HA cold-cycle switch.board_3 THEN arm_zed.sh + bist_read.sh. NOT deployed autonomously (cold-cycle into new BOOT.BIN = irreversible-ish on remote board; low brick risk since FSBL/u-boot unchanged + Linux boots regardless + SSH rollback possible, but awaiting user go). EXPECTED if fix works: Link B reset rate → ~0, golden_frac → ~1.0, BER → <0.01%. If BER improves but misses gate, iterate: also filter the reset to sync_input/output/valid_regularizer (BD edit).

## 2026-07-04 RESET-FILTER FIX DEPLOYED + REFUTED by hardware; fresh-boot baseline
Deployed reset-glitch-filter BOOT.BIN (deb98155) via scp (NOT cat — anyssh wrapper forces stdin</dev/null so cat uploads empty; use scp with SSH_ASKPASS=scratchpad/askpass.sh) + HA cold-cycle switch.board_3 (curl HA REST, token in lg_vpk180.yaml). Board booted new image fine (Linux boots regardless of PL — reversibility confirmed). MEASURED POST-FIX: BER 2.90% / golden 0.59 / resets 2.53/s = WORSE than baseline (reset rate dropped but BER rose 5x). REFUTED the ride-through hypothesis: masking the modem reset doesn't help because adc_1_rst ALSO resets util_adc_1_pack + sync_input/output + valid_regularizer (all on the adc_1_rst net) → the ADC-datapath SAMPLE GAP persists; the modem then processes glitched samples with stale loop state = worse than a clean re-acquisition. Masking downstream resets is fundamentally limited; the real fix must PREVENT adc_1_rst firing (SSI-clock domain), which runtime SSI-delay retune already failed to do. ROLLED BACK to original BOOT.BIN (c77b9795, on-board backup /boot/BOOT.BIN.bak_resetfix + host BOOT.BIN.prefix_bak) via cold-cycle. 
KEY NEW BASELINE: original image after a FRESH cold-cycle + clean arm = BER 0.0759% / golden 0.94 — the earlier "0.61%/68% golden" was DEGRADED accumulated session state, not the true fresh baseline. 0.076% is the best Link B has measured but still ~7.6x above the 0.01% gate (resets still present at ~6% duty). NOTE: arm_zed profile-reload can WEDGE the board (went NO-PING; recovered via HA cold-cycle) — do NOT run concurrent ssh during arm. Board currently: original image, armed Link B, ~0.076%.
NEXT to reach gate: reduce the adc_1_rst RATE further (not mask it). Levers untried: (a) the ~6% reset duty on fresh boot may drop more with tracking-cal tuning or SSI-clock margin; (b) the deeper axi_adrv9002 adc_1_rst trigger. Physical reseat still plausible for the SSI-clock margin. Reset-filter approach = dead end (refuted).

## 2026-07-04 TWO-JUPITER LINK BRING-UP (replaces ZedBoard; A=148 B=146, FDD 2.40/2.45)
User replaced the ZedBoard with a 2nd Jupiter at 10.0.0.148. New target: two-Jupiter FDD link <0.01% both dirs. Spec+plan: docs/superpowers/{specs,plans}/2026-07-04-two-jupiter-fdd-link-*.md. Scripts+wrapper in /mnt/onetb/scratch/qpsk_variants/two_jup/ (anyssh.sh, askpass.sh, preflight.sh, dds_tone.sh, capture_rx.sh, tone_check.py, arm_jup.sh, measure_ber.sh). PROGRESS:
- S0: both boards ALREADY run the jupiter_240k5 modem image (md5 736e0c26, same on both) with DDS present + modem regfile 0x9D000000 → SKIPPED reflash-to-stock (unnecessary+risky; DDS works on modem image). 148 was at 15.36MHz, 146 at 1.92; copied /root/lvds_1p92_mhz.{bin,json} 146→148 and loaded on 148 → both 1.92MHz.
- S1 DDS tones: RF PATH CONNECTED BOTH WAYS. Link1 (148Tx→146Rx) CLEAN tone (95dB, +6.5kHz CFO). Link2 confusion RESOLVED via control test (146 Tx OFF gave identical capture): the DC(81dB)+"+108k spur"(45dB) were 148's OWN uncalibrated-Rx artifacts (bare profile load ran no Rx init-cal), NOT 146's signal. Fixed by full arm.
- Inter-board CFO ~+6.1-6.5kHz (2.7ppm) between the two Jupiters' independent LOs.
- S2 arm both (arm_jup.sh; Rx quadrature_tracking_en write DENIED in rf_enabled ENSM state — benign, init-cals still run). 
- S3 NOT LOCKING YET. Captured 148's modem Tx at 146 Rx: DOMINANT LO-LEAKAGE CARRIER +27dB above in-band QPSK (per-bin), ~-13dBc integrated, sitting at the +6.1kHz CFO. SAME pathology as old ZedBoard link A. profile warmBootEnable=false (fresh init-cals ran) yet carrier persists; initial_calibrations available=off/auto/run (untried). Nulling CFO (trim 148 TxLO -6118Hz to 2449993882) made 146 Rx ACTIVE (cap_out changing, was frozen) but still garbage (~57 err/pkt, cap never golden). 
NEXT (S3 tuning): (a) suppress Tx LO-leakage carrier — try initial_calibrations=run on each Tx; if runtime LOL cal insufficient (like old ZedBoard), may need the old winning fix = Tx baseband IF-offset in the modem HDL (old sim: +20kHz IF → 0.000%), a rebuild. (b) refine CFO null per-direction. (c) verify Rx cal (quad tracking couldn't enable in rf_enabled — set in calibrated state). (d) link-budget/gain. Both Jupiters healthy, armed, no wedge. The ZedBoard reset-fix work is retired.

## 2026-07-04 TWO-JUPITER ch1 tuning — signal DECODABLE (no rebuild), on-chip acquisition impasse
User: new freqs 1.5/2.0 GHz; test channel 2 (both ch cabled); "try channel 1, if fails move to ch2".
CHANNEL VERIFICATION (two_jup/RESULTS_channels.md, tone_path.sh): both freqs work; ALL 4 RF paths connected; CH2 rock-solid both dirs; CH1-into-148 (148 Rx1) flaky (earlier "dead" was gain-saturation/artifact — it DOES receive). Inter-board CFO ~2.3ppm (4.7kHz@2.0, 3.5kHz@1.5).
MODEM CHANNEL: jupiter_240k5 modem is HARDWIRED to ADC/DAC channel 1 (valid_regularizer<-adc_1 data, sync_input, adc_1_clk domain; Tx via dac_1). CH2 data only routes to capture DMA (rx2_dma) — so "use ch2" = a BD rewire (adc_1->adc_2 + clock domain adc_1_clk->adc_2_clk) + rebuild. Deferred (user chose try-ch1-first).
ADVISOR (Opus4.8) course-correction: my "LO-leakage carrier blocks it" was likely another artifact (the +27dB was Rx DC-offset vs in-band MEAN; real Tx leakage only ~0dBc-to-peaks/~-13dBc integrated — shouldn't cause never-lock). KEY: OFFLINE DECODE of la146 (148 Tx@2.0 -> 146 Rx, CFO-nulled) via python matched-filter+4th-power-CFO+differential-Barker = peak/median 3.8 ~= known-good linkB_s4d.iq (4.0, decoded 220/220). => SIGNAL IS DECODABLE, comparable to known-good => NO Tx wall, NO REBUILD NEEDED; blocker is on-chip Rx acquisition/config. (Caveat: my python decoder is weak — ~4 even on known-good; definitive test = jupiter rtl_sim replay of la146, not yet built.)
RUNTIME TUNING TRIED (ch1, 148 Tx2.0/Rx1.5, 146 Tx1.5/Rx2.0): CFO null (trim Tx LOs; Link A nulled to +53Hz offline), AGC then manual gain (34 clamps to 24), bbdc DC-rejection enabled both, deliberate +20kHz offset. RESULT: 148 (Link B, Rx1) cap=0 NEVER frame-syncs; 146 (Link A) frame-syncs but decodes GARBAGE, cfc_est SWINGS wildly (±240k units) = coarse-freq estimator not converging. Rx quadrature_tracking_en can't enable (permission denied any ENSM state — maybe unsupported this profile). NOT LOCKED either dir.
NEXT (decisive, per advisor): build jupiter_240k5/rtl_sim replay (like zed_240k5/rtl_sim) and feed la146.iq through the deployed jupiter Rx RTL in Verilator — if it decodes => on-chip Rx works, live non-lock is config/level; if not => characterize. Then targeted fix (likely coarse-freq-estimator behavior / acquisition, runtime or a modem tweak). Scripts: two_jup/{arm_ch1.sh,offdecode2.py,tone_path.sh,measure_ber.sh}. Both Jupiters healthy, armed ch1, no wedge.

## 2026-07-04 ACQUISITION ISSUE PINNED via jupiter RTL-sim replay (definitive)
Reused existing jupiter_240k5/rtl_sim harness (obj_jup/Vrx_wrap_jup, sim_jup.cpp: argv iq nsamp vphase skip; deployed jupiter Rx RTL from s1_rtl). Replayed la146.iq (148 Tx@2.0 -> 146 Rx, CFO-nulled) through the DEPLOYED Rx RTL:
- RESULT: packets=0-1, biterr=51, frameStart=0-1, capout=0, cfc_est=1336-6072Hz (MIS-ESTIMATE; true CFO ~53Hz offline). => RTL-sim REPRODUCES the on-chip failure. NOT a live-config artifact; the deployed Rx RTL genuinely fails on this signal.
- CONTROL (known-good s1_tx_air_240k5.iq): cfc_est=-5 (~0Hz CORRECT), frameStart=12. So the Rx coarse-freq estimator works on known-good but MIS-ESTIMATES on la146.
- ROOT CAUSE (FFT la146): a razor-narrow Tx LO-LEAKAGE CARRIER line at band center (sharpness 125x, ~0 dBc to QPSK peak). The carrier confuses the Rx Coarse_Frequency_Estimator -> mis-estimate -> mis-compensate -> no frame lock. SAME root cause as old ZedBoard link A (carrier-limited), now PROVEN via RTL-sim (not the speculative carrier claim the advisor rightly flagged earlier — this is confirmed).
- Runtime Tx LO-leakage cal EXHAUSTED: initial_calibrations write "run" stays "off" (won't run); profile warmBoot=false already ran init cals; carrier persists. Cannot null the Tx carrier at runtime.
FIX (proven-necessary rebuild, NOT speculative): move the QPSK off the carrier via a Tx baseband IF-offset in the modem HDL = the OLD WINNING FIX (zed_txnco built it for ZedBoard; sim proved +20kHz IF -> 0.000%). Need a JUPITER equivalent (add complex NCO mixer on the in-FPGA-Tx stream in the jupiter modem HDL) + rebuild + reflash both. Alternative: make the Rx Coarse_Frequency_Estimator carrier-robust (also a rebuild). Channel-2 rewire would NOT escape this (same Tx chip LO leakage). Both Jupiters healthy, armed ch1 (148 Tx2.0/Rx1.5, 146 Tx1.5/Rx2.0), no wedge. la146.iq + la146_postcal.iq saved in two_jup/.

## 2026-07-04 Tx IF-offset fix BUILT + SIM-REFUTED (saved a wasted Vivado build)
Built the jupiter Tx IF-offset NCO fix as a direct Verilog patch (MATLAB dead). two_jup... no: jupiter_240k5/txnco_fix/: tx_if_nco.v (256-entry Q15 sin/cos LUT complex NCO mixer, UNIT-VERIFIED in iverilog: +20009Hz@1.92MHz unity-gain) + patched TxRxComposite.v copy (NCO on in-FPGA-Tx branch: REP_Tx_out1->tx_if_nco->REP_Tx_nco feeding MUX_Dac + MUX_Rx port0; en=enb_1_2_0). Verilator-built the loopback sim (obj_txnco/obj_txnco85 in jupiter_240k5/rtl_sim, sim_lb_jup.cpp rx_input_select=0 internal Tx->Rx; needs nsamp>=600000 for duration; iq content ignored).
SIM RESULT (REFUTES the fix): unpatched loopback decodes GOLDEN (packets=33 cap=04922282 cfc~0). PATCHED PHASE_INC=683 (+20kHz HW = +160kHz@sim-15.36MHz-node) -> packets=0 total fail. PHASE_INC=85 (+20kHz@sim rate) -> 31 frames/105 biterr/2.8%/NOT golden, cfc_est reads ~5kHz for the 20kHz offset = coarse-freq estimator UNDER-ESTIMATES / cannot acquire a deliberate IF offset. => the Tx IF-offset fix (old zed_txnco approach) does NOT work on the jupiter Rx: trades carrier-confusion for acquisition failure. NOTE: loopback has NO carrier, so it only shows the acquisition HURT, not the carrier-separation HELP — but the Rx failing to acquire even a clean offset is disqualifying. (zed_txnco was "sim-verified" via host-decode + Tx-spectral-shift, NOT on-chip Rx loopback — so its on-chip efficacy was never actually confirmed either.)
FIX RECONSIDERATION needed: (a) make the Rx Coarse_Frequency_Estimator carrier-robust (notch DC carrier before the 4th-power est, or data-aided preamble CFO) = Rx-side modem change; (b) get the Tx LO-leakage cal to actually run (Jupiter has internal loopback for LOL init cal — runtime initial_calibrations=run didn't take; may need profile regen with LOL in init mask); (c) small-offset sweet-spot sweep (unlikely — even +2.5kHz-equiv degraded). Did NOT launch the Vivado build (sim refuted first). Both Jupiters healthy, armed ch1, no wedge.

## 2026-07-04 ROOT CAUSE (definitive, symbol-layer): two-Jupiter LO PHASE NOISE, NOT CFO/carrier/modem
Advisor course-correction (was measuring coded BER = cliff function, useless for diagnosis). Went to SYMBOL layer (MATLAB now available: /mnt/onetb/MATLAB/R2025b/bin/matlab -batch; MCP flaky use CLI). Tools in two_jup/: sym_analysis.m (extract payload syms, compare ref_paysym, phase-drift), loopbw_sweep.m, cfo_research.m, test_canc.m.
DECODER VALIDATED: soak_decode_k5 reads KNOWN-GOOD jupiter s1_tx_air_240k5.iq = 27/27 GOLDEN 0.000% (contract matches the 736e0c26 build). So "43% on 148->146" is REAL, not a decoder artifact.
SYMBOL-LAYER VERDICT (sym_analysis vs ref_paysym.txt):
- known-good s1_tx_air: payload SER=0.000, phase drift total -1deg std 0deg, EVM 0.8% = SYMBOLS PERFECT.
- 148->146 (fresh, clean strong signal rssi 14, 51dB in/out, 42 preambles, CFO~0): payload SER=0.737 (0.75=random), phase drift total +2854deg STD 949deg, EVM 29% = PHASE SPIRAL.
RULED OUT (each tested): CFO (4th-power gets it right, ~0-50Hz), CW carrier (LS-cancel -> spiral persists std 548deg, SER 0.74), SNR (EVM proxy 51dB, and it's a spiral not a blob), decoder (validated 0% on known-good), carrier-loop bandwidth (swept 0.01-0.40, SER stays 0.73 at ALL = untrackable), LO fractional-N trim (untrimmed clean 2.0GHz still spirals, std 1384deg WORSE).
=> ROOT CAUSE: severe RELATIVE PHASE NOISE (~16 deg/symbol random walk, std 500-1400deg over the 1120-sym/4.7ms payload) between the two INDEPENDENT Jupiter LOs. Preamble (13 sym) survives, payload spirals out -> random -> 43% coded. This is an LO/reference problem, NOT the modem (modem is proven 0% on coherent/known-good signal). No modem change (carrier-robust CFO=path2, wider loop, IF-offset) can fix it.
FIX DIRECTION (LO/hardware/config, needs user): (1) SHARE A REFERENCE CLOCK between the two Jupiters (coherent LOs -> relative phase noise collapses) = standard two-SDR coherent solution; check if Jupiters have 10MHz ref in/out or can chain reference. (2) reduce per-LO phase noise via ADRV9002 LO config (PLL loop BW / LO_GEN mode / integer-N) — the 16deg/sym is extreme, suggests a high-phase-noise LO setting not just independent TCXOs. (3) note: old ZedBoard->Jupiter linkB decoded 220/220 — either shared better ref or was borderline; this Jupiter-Jupiter pair at 1.5/2.0GHz is far worse. Both boards healthy, armed. "path 2 carrier-robust CFO" framing is STALE/wrong.

## 2026-07-04 CORRECTED root cause: NOT LO phase noise -> wideband per-symbol phase rotation (sample-clock/timing)
Retracted the "LO phase noise" conclusion (mis-attribution #N). DECISIVE cross-check: a CW tone through the SAME 148 Tx / 146 Rx LOs shows only 1.4 deg/symbol phase perturbation (LO is CLEAN), but the MODEM payload shows ~25 deg/symbol. So it is NOT the LO. Tone(narrowband) immune, wideband QPSK corrupted = classic SAMPLE-RATE OFFSET between the two independent clocks (a CW just shifts freq slightly; a 300kHz signal time-scales -> symbols rotate). 
SYMBOL-LAYER PROOF (test_phasetrack.m on la146_maxgain.iq): baseline payload SER=0.753 (random); GENIE ideal per-symbol phase removal -> SER=0.000 (PERFECT) => the payload symbols are CORRECT, just per-symbol phase-rotated; amplitude/SNR/timing-ISI irrelevant (QPSK=phase-only, genie fixes it). PILOT-aided tracking FAILS at every spacing (SER~0.73 even pilots every 8 sym) => the rotation is too FAST for pilots or a decision-directed loop. 
RULED OUT (each by direct test): LO phase noise (tone 1.4 deg vs modem 25 deg), thermal SNR (tone 98dB clean; genie->0 so amplitude irrelevant), decoder (validated 27/27 golden on jupiter s1_tx_air), Rx gain/quantization (max gain, still 30% CV/25 deg), LO power_scale (5->10 no change), LO trim (untrimmed worse), carrier-loop BW (0.01-0.40 all fail), CW carrier (cancel -> spiral persists), CFO (4th-power correct ~0). MATLAB now works via CLI /mnt/onetb/MATLAB/R2025b/bin/matlab -batch (MCP flaky). Tools two_jup/: sym_analysis.m phase_traj.m test_phasetrack.m loopbw_sweep.m cfo_research.m.
LEADING HYPOTHESIS (needs confirmation): sample-RATE offset between the two independent Jupiter reference clocks (each own 38.4MHz TCXO) -> the Rx timing recovery leaves a fast residual per-symbol phase on the WIDEBAND signal that no standard carrier/timing loop tracks. FIX DIRECTION: (a) measure+correct the sample-rate offset (resample / a proper 2nd-order timing loop tuned for the offset) = modem/DSP fix; (b) a modem robust to it. NOT fixable by: sharing a reference (user needs WIRELESS/independent refs), LO config, gain, FEC, or the carrier-robust-CFO/IF-offset ideas (all stale). Both Jupiters healthy, armed ch1, no wedge. HANDED to user per advisor (was cycling diagnoses).

## 2026-07-04 HONEST HANDOFF — two-Jupiter link NOT solved; board state disrupted; retract theories
After exhaustive MATLAB symbol-layer analysis I did NOT converge on the 148->146 payload root cause. RETRACTIONS (each a mis-attribution, advisor-flagged): "carrier confuses CFO", "carrier biases constellation", "LO phase noise" (tone shows LO clean 1.4deg/sym!), "sample-rate offset" (resample sweep flat), and the "genie phase->0" test was CIRCULAR (invalid). 
DEFINITIVELY ESTABLISHED: (1) soak_decode contract VALID (reads known-good jupiter 146-Tx capture s1_tx_air = 27/27 golden). (2) LO is CLEAN (CW tone 148Tx->146Rx = 1.4 deg/sym phase). (3) Link A (148 Tx -> 146 GOOD Rx) payload = random, SER~0.73 / 43% coded, on a strong clean signal with detectable preambles + correct CFO -> root cause UNKNOWN. (4) 148 Rx1 datapath went to ALL-ZEROS after my poking; a clean arm_ch1.sh re-arm restores non-zero data -> was a disrupted state, not hardware. (5) rx_input_select (0x9D000114) reads 0 (modem in LOOPBACK not ADC) even after arm writes it =1 -> the ON-CHIP modem may never have been scoring the air; investigate whether 0x114=1 sticks. Only OFFLINE decode (iio_readdev = ADC DMA = air) was used for Link A, which is valid.
BOARD STATE DISRUPTED by hours of LO/gain/ensm/reg pokes -> recent measurements possibly contaminated. Both boards currently armed ch1 (re-armed 148 Tx2.0/Rx2.0), Rx1 delivering data, no wedge.
RECOMMENDED CLEAN-SLATE NEXT STEPS (for next session/user): (a) fully re-arm BOTH boards fresh (arm_ch1.sh / arm_jupiter), CONFIRM rx_input_select 0x114=1 STICKS on both (else the on-chip modem isn't listening to air); (b) verify each board's modem Tx sends golden by a SELF-loopback decode (rx_input_select=0, on-chip BIST -> should be golden 0x04922282); (c) then ONE careful cross-board capture per direction with validated non-zero data, soak_decode + sym_analysis; (d) if payload still random on a clean strong capture, use LAB INSTRUMENTATION (VSA/spectrum analyzer / a 3rd known-good radio) to characterize the actual OTA signal — offline analysis has been inconclusive/contaminated. Tools in two_jup/. MATLAB works: /mnt/onetb/MATLAB/R2025b/bin/matlab -batch.

## 2026-07-04 *** BREAKTHROUGH: modem control = mwipcore, NOT devmem (all arms were broken) ***
The jupiter modem is a MathWorks HDL-Coder IP at 9d000000.mwipcore (iio:device0 mwipcore0:mwipcore_regs). RAW busybox devmem / /dev/mem writes to 0x9D000xxx ARE SILENTLY IGNORED (driver owns the region). So EVERY arm script (arm_jupiter.sh, arm_ch1.sh) that set rx_input_select/tx_source_select/rstCS/reset via devmem NEVER TOOK EFFECT -- the modem ran in power-on default, rstCS never reset the carrier sync, all session's on-chip BIST was of a mis-configured modem. 
CORRECT interface: echo enabled > /sys/bus/iio/devices/iio:device0/reg_access ; then /sys/kernel/debug/iio/iio:device0/direct_reg_access (echo "0xOFF 0xVAL" to write, echo 0xOFF then cat to read). NOTE: control regs are WRITE-ONLY (readback returns a DIFFERENT read register -> my earlier "write doesn't stick" was an INVALID readback test). Verify writes by EFFECT.
PROVEN via effect: rx_input_select(0x114)=0 (loopback) -> cap_out(0x144)=0x04922282 GOLDEN (modem PERFECT, writes WORK); rx_input_select=1 (air) -> non-golden. Reg map: 0x104 packets,0x108 errors,0x144 cap_out,0x154 cfc_est (READ); 0x000 reset,0x110 rstCS,0x114 rx_input_select,0x118 tx_source_select,0x11C tx_data_source (WRITE-only). reg_access must be 'enabled'.
=> ALL prior on-chip conclusions this session are SUSPECT (modem was never properly armed via the broken devmem path). MUST redo the whole link bring-up with mwipcore control. Offline air captures (iio_readdev, independent of modem control) still valid & showed random payload -- but the TX board's config was ALSO default (tx_source=0=modem Tx is the default, so Tx probably OK). Loopback-golden proves modem Tx+Rx+FEC all work. Next: mwipcore-arm both boards (rx_input_select=1, tx_source=0, proper rstCS), null CFO, measure on-chip air BER PROPERLY. This may be the real unblock. Both Jupiters healthy.

## 2026-07-04 *** ROOT CAUSE CONFIRMED: frequency-selective RF channel (148<->146) ***
Received-spectrum (Welch PSD) shape comparison FINALLY nails it:
- KNOWN-GOOD s1_tx_air: in-band ripple 17.9dB, shape@[-120,-60,0,+60,+120]kHz = -5,-2,0,0,-3 dB = FLAT RRC (clean channel).
- 148->146 AIR (la146_maxgain): in-band ripple 58.3dB, shape = -40,-4,-18,0,-34 dB = SEVERE frequency-selective distortion (deep nulls at +-120kHz, DC dip, asymmetric).
=> The 148<->146 RF path is severely FREQUENCY-SELECTIVE. This corrupts the WIDEBAND QPSK payload (ISI) while a single CW TONE stays clean (98dB, at a spectral peak) and the short DIFFERENTIAL preamble survives (13 sym). EXPLAINS EVERYTHING chased all session: not LO phase noise (tone 1.4deg clean), not SNR (tone 98dB), not CFO/carrier, not decoder (loopback golden). It's a DISTORTED CHANNEL - a reflection/impedance-mismatch in the RF cabling, or (for the intended wireless link) multipath. Modem has NO EQUALIZER -> cannot handle it.
FIX: (a) fix the RF path - check cabling/connectors/terminations/VSWR for the reflection causing the 58dB ripple + nulls (a bad SMA/cable is the likely culprit in the current bench setup); (b) for the WIRELESS link (multipath channel), the modem needs a channel EQUALIZER (Rx adaptive equalizer / or OFDM / or a much lower symbol rate so the channel is flat over the signal BW). 240ksym QPSK over a multipath channel with no equalizer will not work.
CONFIRMED on-chip too: with WORKING mwipcore control (rx_input_select=1 air, proper reset), on-chip air Rx processes but decodes garbage (cfc swings, cap!=golden) = matches offline. Loopback=golden (modem perfect). So it is NOT the (now-fixed) control issue; the channel is the blocker.
TWO breakthroughs this session (2026-07-04): (1) modem control = mwipcore not devmem (all arms silently no-op'd; redo bring-up via /sys/kernel/debug/iio/iio:device0/direct_reg_access after reg_access=enabled). (2) frequency-selective channel is the payload-corruption root cause. Next: fix RF path (measure VSWR/return loss, reseat/replace cabling) then re-test; if wireless, add an equalizer or drop symbol rate. Both Jupiters healthy, modem proven good in loopback.

## 2026-07-04 RATE MISMATCH found+fixed (real bug, NOT the root cause) + profile-load method
BIG SETUP BUG (present entire session, missed): the two boards were at DIFFERENT sample rates -- 148=15.36MHz (native, reverted on my reboot), 146=1.92MHz (runtime-loaded, reverted when I rebooted it this turn). NEVER matched. 148's modem at 15.36 = 1.92Msym (8x); 146 Rx at 1.92 = aliased -> garbage. This masqueraded as the payload corruption for much of the recent debugging.
PROFILE-LOAD METHOD (this image 736e0c26, both boards identical, mwipcore): profile_config/stream_config are SYSFS attrs of the phy (/sys/bus/iio/devices/iio:device2/{stream_config,profile_config}), NOT debugfs (find won't see them). Load: `cat /root/lvds_1p92_mhz.bin > .../stream_config; cat /root/lvds_1p92_mhz.json > .../profile_config` -> fs=1920000. NOT persistent (reverts to 15.36 native on reboot). MUST reload lvds_1p92 on BOTH boards after any reboot to get matched 1.92MHz. (bash_history showed the old load was on iio:device1 = different device index in an older image.)
RESULT after matching BOTH to validated 1.92MHz: 148 loopback GOLDEN (0x4922282). Cross-board 148->146 STILL 43% -- offline soak_decode (validated) AND 146 on-chip air Rx (cfc swinging, cap!=golden). So the rate mismatch was a REAL bug but NOT the payload-corruption root cause. The corruption persists at matched validated rates.
=> EXHAUSTIVELY isolated: the 148-Tx -> air -> 146-Rx WIDEBAND analog path corrupts the payload (~43% random), independent of: rate (now matched), LO phase noise (tone 1.4deg), CFO, carrier, SNR (tone 98dB), decoder (validated 27/27), IQ imbalance (spectrum symmetric), non-linearity (Tx power sweep flat), channel/ISI (equalizer fails), carrier frequency (all fail), channel-2 (host-DMA won't emit -- ch2 DAC DMA source != 0x2, TBD). 148 digital Tx is golden (loopback). Needs LAB INSTRUMENTATION (VSA on 148 Tx SMA) to see the transmitted wideband signal directly -- offline capture analysis is exhausted. Both boards now at matched 1.92MHz, healthy. golden_tx.iq (19/19 self-test) + gen_golden2.m in two_jup/.

## 2026-07-04 CHANNEL-2 BUILD: BD rewired + validated + synthesizing
User: "b then a". (b) host-DMA ch2 test INTRACTABLE (ch2 DAC DMA path won't transmit from userspace; DDS src 0x0 works, DMA src 0x2 gives nothing; cyclic/continuous/source-sweep all failed). Moved to (a) BUILD.
BUILD APPROACH = direct BD surgery on the built system.bd (NOT re-run HDL Coder; reference design exposes only ch1 via sync_input/output->adc_1/dac_1). Tcls: jupiter_240k5/bd_ch2_rewire.tcl (clocks+data+capture) + bd_ch2_reset.tcl (reset domain). Rewired modem TxRxCompo_ip_0 from ch1->ch2: IPCORE_CLK/AXI_ACLK + sync_input/output + valid_regularizer -> adc_2_clk/dac_2_clk; Rx adc_2_data_i0/q0 -> valid_regularizer in_data_0/1 (in_data_2/3 tied 0 via const0_16, since ch2 is 2-lane vs ch1 4-lane); Tx sync_output/data_out_tx_0/1 -> dac_2_data_i0/q0; Rx capture sync_output rx -> util_adc_2_pack; resets -> new inv_adc2_rst/inv_dac2_rst (util_vector_logic NOT of adc_2_rst/dac_2_rst). VERIFIED connectivity + VALIDATE_OK (twice). BD saved; ch1 BD backed up system.bd.ch1_backup; ch1 BOOT.BIN backed up boot/BOOT.BIN.ch1 (736e0c26).
BUILD RUNNING: vivado -mode batch -source vivado_build.tcl (PATH=/tools/Xilinx/2025.1/Vivado/bin, ADI_MAX_THREADS=12) -> log jupiter_240k5/ch2_build.log; produces boot/BOOT.BIN (bootgen zynqmp). ~1-3h synth+impl+bitstream. FPGA=xczu3eg jupiter_sdr.
RISK (testable only on HW after build): ch1 is 4-lane (2 samples/clk) vs ch2 2-lane (1 sample/clk) -- the modem was built for ch1's format; if adc_2_clk != adc_1_clk freq / different sample format, the modem may not decode on ch2 even though BD validates. Also modem's data_out_rx_2/3 left dangling (ch2 pack is 2-lane). If it fails on HW, may need ADRV9002 profile change to match ch2 SSI format to ch1. FALLBACK: restore boot/BOOT.BIN.ch1 + system.bd.ch1_backup. Both Jupiters at matched 1.92MHz (reload lvds_1p92 via sysfs after reboot).

## 2026-07-04 CHANNEL-2 BUILD SUCCEEDED + TESTED -> ch2 == ch1 (43%), rules out channel-specific defect
BUILD DONE: ch2 BOOT.BIN md5 b853f5efaa24c29e46eca8ccd90e4186 (7203552 B) at jupiter_240k5/hdl_prj_jupiter_composite/vivado_ip_prj/boot/BOOT.BIN. Build flow: bd_ch2_rewire.tcl + bd_ch2_reset.tcl (rewire modem ch1->ch2, VALIDATE_OK) then ch2_fix_ch1.tcl (impl failed first pass: moving modem off ch1 left dac_1_data + util_adc_1_pack inputs undriven -> opt_design LUT error; FIX = restore ch1 as plain DMA channel: adc_1->util_adc_1_pack, util_dac_1_upack->dac_1) then synth+impl+bitstream+bootgen.
TIMING: WNS=-12.794ns on adc_2_clk (REP_TxI DSP path). HARMLESS -- the ORIGINAL ch1 build had the IDENTICAL violation (WNS=-12.271ns same REP_TxI path); it's the modem's multicycle/clock-enable situation (processes at ~1.92MHz via enables on the 125MHz/8ns clock). adc_1_clk==adc_2_clk==8ns (same freq; my earlier 4-lane-vs-2-lane rate worry was WRONG).
DEPLOYED to 148 (/boot/BOOT.BIN=b853f5efaa24; ch1 backed up /boot/BOOT.BIN.ch1bak=736e0c26). Reboot OK, all iio devices present, lvds_1p92 -> 1.92MHz.
TEST RESULTS: (1) modem LOOPBACK on ch2 = GOLDEN 0x04922282 (modem logic works perfectly on ch2; timing violation confirmed harmless). (2) CH2 AIR LINK 148-ch2modem-Tx2 -> 146-Rx2 = 43.663% (0/43 golden) == SAME as ch1's 43%.
=> DEFINITIVE: the wideband-payload corruption is NOT channel-specific (not a bad ch1 cable/connector). BOTH physical RF channels corrupt identically -> SYSTEMATIC cross-board issue (common DAC/analog architecture, or the fundamental two-independent-reference hop). Rules out the channel-2 hypothesis. Modem+build are proven-good (loopback golden on both channels). Still NOT root-caused after: rate(matched), LO phase noise(tone 1.4deg), CFO, carrier, SNR(tone 98dB), IQ imbalance, non-linearity(power sweep), channel/ISI(equalizer), freq, ch2. NEXT: VSA on Tx SMA (only remaining discriminator) OR investigate why ZedBoard->Jupiter linkB decoded 220/220 but Jupiter->Jupiter fails (both cross-board indep refs). To revert 148: cp /boot/BOOT.BIN.ch1bak /boot/BOOT.BIN; reboot.

## 2026-07-05 ADVISOR-DRIVEN: bracketed to the JUPITER TRANSMITTER (not clocks)
Advisor caught that I was characterizing a fully-random 43% capture (my phase numbers disagreed 4x = measuring contamination). Killed 2 bad tools: (1) the 4.4-vs-44 known-golden xcorr is INVALID (4.7ms template decorrelated by any drift within the 250Hz CFO grid; 4.4 = preamble re-detecting, NOT payload absent); (2) all data-aided residual analysis on the broken capture.
GROUNDING: soak_decode reads linkB_s4d (ZedBoard->Jupiter CROSS-BOARD) = 220/220 GOLDEN (toolchain valid on a real cross-board file). s1_tx_air is SINGLE-BOARD host-golden (NOT a valid cross-board control; linkB_s4d is the only one).
BRACKET (clean, no artifacts): loopback-golden => Jupiter Tx/Rx-digital+FEC fine. linkB_s4d 0% => Jupiter Rx-analog + over-air + independent-refs all fine. Jupiter->Jupiter 43% (both channels) => the ONLY differing element is the JUPITER TRANSMITTER's analog output. => SOURCE = Jupiter Tx analog path (modem Tx -> DAC -> RF), NOT the two-clock interaction (linkB proves two-clock works), NOT LO/phase-noise/DQPSK/reference-sharing (all dead ends).
Supporting: same-board ONE-CLOCK near-end analog loopback = 43.76% (but near-end mode may itself be an artifact since host-golden through same DAC/analog = 0%). REP_TxI IS a polyphase FIR (not the old ZOH; waveform fix already in jupiter_240k5). Documented differentiator: zed_bytetx has multiple=2 Tx rate-matcher / 'Tx-straddle'; jupiter_240k5 has multiple=1 -- likely the real Tx defect.
NEXT: definitive artifact-free confirmation = SMA CABLE loopback (Jupiter modem Tx1 -> pad -> Rx1, ONE clock, real analog). If 43% -> Jupiter Tx analog defect CONFIRMED -> fix the Tx rate-matcher/straddle (multiple=2 or the LVDS Tx sample straddle). If 0% -> near-end was the artifact + reconsider. Both boards: near-end loopback now disabled, at matched 1.92MHz; 148 on ch2 image (b853f5efaa24), 146 on ch1 (736e0c26).

## 2026-07-05 *** EXACT ROOT CAUSE FOUND: Jupiter Tx-output cross-clock STRADDLE (multiple=1) ***
The in-FPGA modulator runs on adc_1_clk and crosses to dac_1_clk via a RAW passthrough sync_output (multiple=1) -> DROPS/DUPS samples -> corrupts the transmitted waveform at the DAC. This is the documented "Tx-output cross-clock straddle" (qpsk-1p92-standalone-verify.md). ZedBoard avoids it via multiple=2 (sync_fast_to_slow elastic FIFO Tx rate-matcher).
COMPLETE EXPLANATION: linkB_s4d (ZedBoard Tx, multiple=2, no straddle) -> 0%. Jupiter Tx (multiple=1, raw straddle) -> 43% BOTH channels. host-golden (host-DMA bypasses modulator+straddle) -> 0%. digital loopback golden (straddle is a DAC-domain crossing; the FPGA-internal REP_Tx tap doesn't exercise it). Verified both ch1 (la146) + ch2 (ch2link, my new ch2 build) = both 43% -> the straddle is in the modem-Tx-to-DAC path common to both channels.
*** REFUTES the prior conclusion *** (qpsk-1p92-standalone-verify.md said "the clean Jupiter in-FPGA-Tx self-test is a single-board ARTIFACT; the real two-board link would decode 0% like host-golden"). WRONG: the ACTUAL two-Jupiter link is 43% -> the straddle corrupts the REAL over-air link, not just the self-test. The prior work never tested a two-JUPITER Tx link (only ZedBoard->Jupiter / host-golden). My session provides that missing data point.
FIX = multiple=2: instantiate sync_fast_to_slow elastic FIFO on the Tx-output crossing (like ZedBoard). In hdlworkflow_loopback.m ReferenceDesignParameter 'multiple','1'->'2' + the insert/build Tcl 'set multiple {1}'->{2}; matlab_processors.tcl:278-291 multiple==2 instantiates the FIFO (multiple==1 skips it). Prior "Route D/jupiter_asyncclk" added multiple=2 + an ASYNC Tx MMCM clock (for the self-test degeneracy) and hit OSERDESE3 Max-Skew issues -- but the REAL link already has independent Rx clock, so likely only need multiple=2 (the elastic FIFO), NOT the async clock. Building the multiple=2 fix next.

## 2026-07-05 multiple=2 STRADDLE FIX BUILT+DEPLOYED -> STILL 43% (straddle REFUTED)
Built clean multiple=2 (sync_fast_to_slow elastic FIFO on sync_output Tx-crossing adc_1_clk->dac_1_clk, NO async clock). BOOT.BIN c808cb5fa23d (recovered from HDL-Coder workflow insert-path bug via /tmp/complete_mult2.tcl = template insert + manual synth/impl/bitstream/bootgen; FIFO confirmed in routed design sync_output/sync_fast_to_slow/inst/fifo_reg; timing WNS -12.56ns = same harmless multicycle as stock). Deployed to 148: SSI HEALTHY (all iio devs, unlike jupiter_asyncclk which killed the phy), loopback GOLDEN 0x04922282, fs 1.92MHz.
RESULT: 148 (multiple=2 Tx1) -> 146 Rx1 = 43.20% (0/56). NO CHANGE from stock 43%. => the Tx-output STRADDLE is NOT the source. multiple=2 fix does not help. My "straddle = root cause" determination (from the linkB-vs-Jupiter bracket) is REFUTED.
Standings: the bracket (linkB ZedBoard-Tx->Jupiter-Rx = 0% vs Jupiter-Tx->Jupiter-Rx = 43%) still implicates the Jupiter TRANSMITTER, but NOT via the straddle/multiple. The ZedBoard-vs-Jupiter Tx difference that matters is something else (CMOS-vs-LVDS SSI/DAC path? sample format?). host-golden (host-DMA->DAC->analog, 1 board)=0% but modem-Tx(->SSI->DAC)=43% on the SAME board (near-end)=the modem-Tx-to-DAC path corrupts, but NOT the straddle. Builds: 148 has multiple=2 (c808cb5fa23d), working image backed up /boot/BOOT.BIN.working736 (=736e0c26). 146 stock 736e0c26. Both 1.92MHz.

## 2026-07-05 contract-mismatch REFUTED (on-chip BIST also ~50%); corruption localized to modem-Tx->DAC data path
Advisor hypothesis (soak_decode's ref_paysym != Jupiter in-FPGA-Tx sequence -> "43%" is a bit-domain artifact, symbols actually clean): TESTED, REFUTED. (a) on-chip BIST over the link (146 Rx1 on 148 Tx1, compares to the modem's OWN hardcoded golden NOT ref_paysym) = pkt Δ422/err Δ25224 = ~49.8% BER = ALSO random. (b) blind reference-free EVM: I mis-read it -- "dist to nearest QPSK pt" is ~45% for UNIFORM-RANDOM symbols, so the failing 35-42% = mostly-random (slightly structured), NOT clean clusters (control host-golden=1.1%). => corruption is REAL, confirmed 3 ways (soak_decode 43%, on-chip BIST 50%, blind EVM ~random). NOT a contract/ref_paysym artifact.
NARROWED SOURCE (localized, not fully pinned): the MODEM-Tx -> DAC data path corrupts the wideband payload. Proof chain: modem DIGITAL Tx (loopback taps REP_TxI_out1 BEFORE the DAC via rx_input_select=0 MUX) = GOLDEN. host-golden (host-DMA -> util_dac_1_upack -> DAC -> analog, ONE board) = 0% -> DAC/analog/RF/ADC/Rx ALL clean. near-end analog loopback (modem-Tx -> sync_output -> DAC -> analog -> ADC, ONE clock) = 43%/35%-EVM -> the modem's sync_output->dac_1 data path corrupts, INDEPENDENT of clocks (one clock still fails) and NOT the crossing/straddle (multiple=2 FIFO fix FAILED). Signature: amplitude ~preserved (CV 30%) but phase ~uniform-random -> points to a DAC DATA-FORMAT / sample-straddle issue (how the modem's 1-sample I/Q is arranged into the LVDS DAC's 2-sample/beat format), distinct from the clock crossing.
RULED OUT this session (each by direct test/build): two-clock interaction (near-end 1-clock fails; linkB 2-clock works), LO phase noise (tone 1.4deg), CFO, carrier, SNR, IQ imbalance, non-linearity, channel/ISI, sample-rate, DQPSK, ch2 (built), straddle-crossing multiple=2 (built), contract-mismatch (on-chip BIST). Builds: 148=multiple=2 (c808cb5fa23d, working/loopback-golden), revert /boot/BOOT.BIN.working736. 146=736e0c26. Both 1.92MHz.
NEXT (unbuilt): the DAC data-FORMAT/sample-straddle in sync_output->dac_1 (agent-2's Tx-straddle, format not crossing). Compare host-DMA upack->dac_1 format vs modem sync_output->dac_1 format. Deep HDL/BD.

## 2026-07-05 *** DISAMBIGUATOR: near-end mode CONFIRMED CLEAN -> modem-Tx fabric->DAC path is the real defect ***
Transmitted the CLEAN golden_tx.iq via DIRECT host-DMA (iio_writedev -c cyclic, DAC source 0x0 = DMA, BYPASSES composite/sync_output) through the SAME rx0_near_end_loopback on 146, captured Rx1: BLIND EVM = 0.4% (in-phase mean 1.00 std 0.00) = PERFECTLY CLEAN. (tx_source_select=1 composite-passthrough gave zeros/didn't feed DAC - finicky; direct DMA 0x0 works.)
=> the NEAR-END LOOPBACK MODE is a VALID clean test path (not the artifact). => the modem-Tx near-end 43%/35%-EVM is a REAL defect. Bracket: direct-DMA->DAC (bypass composite/sync_output) = 0.4% CLEAN; modem->composite->sync_output->DAC (fabric, source 0x2) = 43%. Same DAC/analog/near-end/one-clock. The ONLY difference = the fabric/sync_output datapath the modem uses + the DMA bypasses. multiple=2 (sync_output FIFO) already ruled out the clock-crossing -> it's the composite-tx-MUX / sync_output DATA-FORMAT or the modem data into it.
CONFIRMED-clean building blocks now: DAC, analog Tx, RF loopback, analog Rx, ADC, near-end mode, Rx modem, FEC, direct-DMA fabric-bypass. CONFIRMED-defective: the modem in-FPGA-Tx fabric path (composite tx-MUX + sync_output -> dac_1). NEXT to fully pin: get host-data THROUGH sync_output (tx_source_select=1 working) to split composite-MUX vs sync_output-format vs modem-data; OR dive the sync_output->dac_1 vs util_dac_1_upack->dac_1 format in the BD.

## 2026-07-05 modem-Tx corrupt at BOTH rates (15.36 native = 35.1% EVM, same as 1.92); rate/profile ruled out
Modem-Tx near-end at NATIVE 15.36MHz (no profile) = BLIND EVM 35.1% (mean 0.94 std 0.26) = IDENTICAL to 1.92MHz. => NOT the 1.92 profile / 4-lane-vs-2-lane rate format. The modem fabric->sync_output->dac_1 path is fundamentally corrupt at ANY rate. Running the two-Jupiter link at 15.36 would NOT fix it.
FINAL LOCALIZATION (thoroughly confirmed): the Jupiter in-FPGA-Tx FABRIC datapath (modem -> composite tx-MUX -> sync_output -> axi_adrv9001/dac_1_data 4-lane) corrupts the transmitted waveform. Validated clean building blocks (each by direct test): near-end loopback mode (host-golden thru it=0.4% EVM), direct-DMA->DAC internal path (both rates), DAC/analog/RF/ADC/Rx-modem/FEC. Ruled out (each by test OR build): clocks, CFO, LO-PN, SNR, IQ, nonlinearity, ISI, channel, DQPSK, wrong-ch(built ch2), clock-crossing straddle(built multiple=2), contract-mismatch(on-chip BIST 50%), rate/profile(15.36==1.92). BD: all 4 dac_1_data lanes driven by sync_output/data_out_tx_0..3; modem feeds 2 (dut_data_out_0/1_tx). Exact HDL mechanism (2-sample fabric format/timing/valid of the modem data into the DAC serializer) UNPINNED -- deep machine-generated HDL. This is the SAME in-FPGA-Tx defect the qpsk-1p92-standalone-verify saga chased; my session PROVES it corrupts the REAL link (not just self-test) and localizes it to the fabric datapath, but the fix remains the deep/uncertain HDL effort that prior extensive work never closed. Board states: 146 native 15.36 (stock 736e0c26), 148 multiple=2 (c808cb5fa23d) @1.92; both loopback-golden; backups /boot/BOOT.BIN.working736 on 148.

## 2026-07-05 *** ROOT CAUSE PINNED (RTL dive + hardware spectrum): open-loop TX DUT->DAC crossing, no valid handshake ***
The Jupiter in-FPGA-Tx fabric corruption = the TX DUT->DAC fabric CDC crossing IGNORES the modem's data_valid. Evidence (file:line ground truth):
- BD system.bd 3870-3909, 4704-4726: dut_data_out_0_tx->sync_output/data_in_tx_0->dac_1_data_i0 (modem I); data_out_1_tx->dac_1_data_q0 (modem Q); data_in_tx_2/3 UNCONNECTED -> dac_1_data_i1/q1 = 0. Stream = [(I,Q),(0,0),...] IF 2-sample/beat, but hardware spectrum shows NO Nyquist energy => core runs 1-sample/beat (i1/q1 not consumed) at this rate.
- TxRxCompo_ip.v 265-269: modem emits ONE complex sample (I,Q) + ONE gated 1-in-N valid strobe (tx_validOut) on IPCORE_CLK=adc_1_clk (multirate HDL-Coder, enb_1_2/enb_1_4). sync_output crosses adc_1_clk->dac_1_clk.
- sync_fast_to_slow.v 100-124: FIFO WRITE fires on a clock-ratio-derived `tick` (or rd==wr), NEVER on data_valid. Modem valid is only carried as a data bit, dead-ends (sync_output/data_valid_out_tx_0 -> util_dac_1_upack/fifo_rd_en, upack data deleted from fabric DAC path, matlab_processors.tcl 1007-1013). So samples SLIP/DROP/REPEAT crossing to dac_1_clk -> symbol sequence scrambled, but each latched sample is a valid RRC sample -> BANDLIMITED, no Nyquist, RRC preserved, phase uniform-random, ~35% EVM. EXACTLY the hardware capture.
- SMOKING GUN: RX path has util_valid_regularizer (matlab_processors.tcl 1127-1153) to regularize the ADC valid cadence for DUTs; TX path has NO equivalent between DUT out and sync_output. That missing TX handshake IS the defect.
- Explains ALL: multiple=2 no help (RATIO change can't fix a valid-ignoring crossing); host-DMA clean (core internal DMA has its own handshake); loopback-golden (taps REP_Tx pre-crossing); both rates corrupt.
FIX = valid-gated TX crossing: mirror the RX regularizer on DUT->sync_output (insert TX-side util_valid_regularizer OR gate sync_fast_to_slow FIFO write with the modem data_valid so it advances once per valid sample). Impl point matlab_processors.tcl ~1161-1178 (jupiter_sdr `if {$rxtx != "rx"}` block). Regen subagent finalizing the exact reference-design param/build path. ILA (optional) to confirm 1-vs-2 sample/beat: probe dac_1_data_i0/q0/i1/q1, dac_1_valid_i0, dac_1_enable_i0/i1, dut_data_out_0_tx+valid.

## 2026-07-06 FIX BUILDING: tx_dac_cdc_fifo (valid-handshake TX crossing)
Fix for the open-loop TX DUT->DAC crossing root cause. Wrote /mnt/onetb/scratch/qpsk_variants/jupiter_240k5/tx_dac_cdc_fifo.v = proper async CDC FIFO (xpm_fifo_async, FWFT, depth 32, prog_empty=8 prime/center): WRITE gated by modem dut_data_valid_out_tx on wr_clk=adc_1_clk; READ gated by dac_1_valid_i0 on rd_clk=dac_1_clk; i1/q1=0 (1-sample/beat per spectrum). Both clocks from same ADRV clkPLL -> matched rate -> self-centers, passes each sample exactly once. iverilog-VALIDATED (TXFIFO_SIM_PASS: samples in-order once, no dup/drop, i1/q1=0). BD surgery /tmp/txfifo_fix.tcl: disconnect dac_1_data_i0/q0/i1/q1 from sync_output (the broken open-loop crossing), splice tx_dac_fifo between TxRxCompo_ip_0/dut_data_out_0/1_tx+valid and axi_adrv9001/dac_1_data_*, rd_beat=dac_1_valid_i0. VALIDATE_OK (XPM elaborated clean). Building on the multiple=2 project (jupiter_240k5 hdl_prj vivado_ip_prj). If it works: near-end EVM 35%->~1%, link 43%->~0%. Test plan: deploy to 148, verify loopback golden, near-end blind_evm, then 148 Tx1->146 Rx1 soak_decode + on-chip BIST. Backups: 148 /boot/BOOT.BIN.working736; boards 146 native-15.36 stock, 148 multiple=2 @1.92.

## 2026-07-06 tx_dac_cdc_fifo fix BUILT+DEPLOYED -> IDENTICAL 35.1% EVM / 44% BER (crossing REFUTED as the defect)
BOOT.BIN 9b07dd835b33 deployed to 148: loopback GOLDEN, SSI healthy, fs 1.92. Near-end blind EVM = 35.1% (mean 0.94 std 0.26) = BIT-IDENTICAL to pre-fix. BD VERIFIED: dac_1_data_i0/q0/i1 <- tx_dac_fifo/dac_i0/q0/i1 (FIFO IS in the path); dac_1_valid_i0 -> tx_dac_fifo/rd_beat. So a proper valid-handshake CDC crossing had ZERO effect => the open-loop-crossing diagnosis is REFUTED (or the corruption is elsewhere). 4th failed build (ch2, multiple=2, txfifo... wait 3rd: ch2, multiple=2, txfifo).
KEY REFRAME: the corruption is NOT in the adc_1_clk->dac_1_clk crossing. Candidates now: (a) DAC-core serialization of the FABRIC dac_1_data input -- NOTE the fabric path has NEVER been validated clean; every clean host-golden used the DAC core's INTERNAL DMA (source 0x0), NOT the fabric (source 0x2). The fabric dac_1_data->LVDS-SSI serialization at 1.92/15.36 may be fundamentally mis-configured (format/enable/2-sample-beat). (b) something between the loopback tap (REP_TxI_out1, golden) and dut_data_out_0_tx. IDENTICAL result (to 3 sig figs) strongly implies the mechanism is unchanged by anything on the fabric-crossing path. Need GROUND TRUTH: ILA on dac_1_data + dac_1_valid_i0 + dac_1_enable_i0/i1 + dut_data_out_0_tx+valid (1-vs-2 sample/beat, actual cadences), OR route host-DMA/DDS through the FABRIC (not internal DMA) to test if the fabric path itself is broken. Consulting advisor.

## 2026-07-06 *** ACTUAL ROOT CAUSE (advisor cyclostationary test): modem transmits HALF symbol rate (120 vs 240 ksym) ***
Cyclostationary |x|^2 symbol-rate-line analysis (reference-free, sync-free): golden_tx.iq (240ksym truth) = line@240.0kHz, occ-BW 330kHz. ALL modem captures = line@120.0kHz, occ-BW 172kHz = EXACTLY HALF symbol rate + half bandwidth. Confirmed across DIFFERENT Rx boards (la146_fresh/ch2link/fix_test on 146 cross-board; txfix_nearend on 148 near-end) + all builds -> deterministic TRANSMITTER defect, NOT capture/analog/clock. The modem's RRC pulse is 2x too wide (sps=16 effective) -> Gardner can't lock -> reads ~35% random, while the modem's own matched Rx (loopback) decodes golden because internally it is 240ksym.
This EXONERATES the crossing (all 3 builds ch2/multiple2/txfifo identical because none touched the rate). RTL: TxRxComposite.v L424-427 tx_dataOutI=REP_TxI_out1 (SAME net as loopback tap L291) at ce_out_0=6.51e-8=15.36MHz. So the DAC feed = the golden REP_Tx net, but the transmitted symbol rate is halved somewhere in REP_Tx interpolation / DAC decimation / SSI serialization / profile-rate handling. FIX DIRECTION: make the modem transmit 240ksym (2x). Localizing whether it's the 1.92M profile (DAC decimation) or fundamental (native 15.36M also half). This is the real defect behind the entire session's 43%.

## 2026-07-06 *** DEFINITIVE: content PERFECT, sole defect = 2x symbol-rate (advisor matched-rate decode) ***
Decoded the near-end modem capture at the MATCHED half rate: decimate-by-2 (every other complex sample) -> soak_decode_k5 = txfix_nearend_dec2 GOLDEN 16/16, BER 0.0000%. Blind EVM sps=16 = 6.9% (vs 35% at sps=8). => the transmitted QPSK content is CORRECT (perfect golden message, FEC/framing/modulator/analog all fine); the ENTIRE session's 43% = decoding a 120-ksym signal with a 240-ksym decoder (sps=8 matched filter+Gardner mismatched to the half-rate waveform -> ~random). The on-chip BIST 50% was ALSO this (its Rx expects 240 too). NOT a scrambler/contract issue -- the reference is off in RATE.
So: modem transmits the golden at HALF symbol rate (120 vs 240 ksym), fundamental (both 1.92M profile=120 and 15.36M native=960k=half). Content perfect at half rate. FIX = kill one factor of 2 in the Tx path so it emits 240ksym. Advisor localization: the 2x is between REP_TxI_out1 (=DAC-feed net, loopback-golden) and the DAC pins; DAC consumes 1 sample/beat (i1/q1=0 was inert, Nyquist-free); the modem asserts dut_data_valid_out_tx only every OTHER dac_1_valid_i0 beat -> half rate. Prime suspect = REP_TxI (2x stage repeating/holding, not doubling content rate) OR the modem output sample-rate config. NEXT: verify dut_data_valid_out_tx vs dac_1_valid_i0 cadence in RTL; find + halve the extra 2x oversampling / double the output cadence; rebuild.
NOTE cross-board (la146/ch2link) at half-rate decode still ~44% (near-end=0%): the extra is two-clock CFO/timing at half rate; expected to resolve once rate is correct + FEC engages (like linkB ZedBoard->Jupiter=0%).

## 2026-07-06 FIX v2 BUILDING: tx_dac_gather (1->2 sample gather) -- user-directed interface-rate fix
User corrected: modem runs 15.36 MSPS, DAC/ADC = 2 clocks per new sample; RD='JUPITER (Rx & Tx - Rx is faster)' with number_of_inputs=4 = 2 complex samples/beat (i0/q0=sample n, i1/q1=sample n+1), but the modem drives only 1 sample/beat (dut_data_out_0/1_tx; i1/q1 unconnected=0) -> DAC transmits 1 real sample per 2-sample beat = HALF rate (120 vs 240 ksym). CONFIRMED sole defect: near-end decoded at matched half-rate (decimate-by-2) = GOLDEN 16/16 0% -> content perfect, only the 2x rate wrong. Cyclostationary: golden 240kHz line vs all modem captures 120kHz.
FIX (user choice: fabric 1->2 gather): tx_dac_gather.v gathers 2 consecutive modem samples -> (i0/q0)=even,(i1/q1)=odd per dac_1_valid_i0 beat -> DAC transmits 2 real samples/beat = full 240ksym. iverilog GATHER_SIM_PASS (pairs s_n/s_n+1, no dup). BD surgery /tmp/gather_fix.tcl: delete tx_dac_fifo (the half-rate 1-sample version), add tx_dac_gather driving ALL 4 dac_1_data lanes. Building (gather_build.log). Deploy to 148, near-end blind_evm sps=8 should be ~1% + soak_decode GOLDEN + on-chip BIST + link. Prior fix files: tx_dac_cdc_fifo.v (superseded, was i1/q1=0), BOOT c808/9b07 (superseded).

## 2026-07-06 *** FIX CONFIRMED: tx_dac_gather resolves the 43% -> link decodes golden ***
BOOT.BIN 753760ad3310 (tx_dac_gather 1->2 sample gather) deployed to 148. RESULTS:
- ON-CHIP BIST over the real link 148(gather-Tx1)->146(modem Rx1): cap_out = 0x04922282 = GOLDEN, stable 8+s. (146 modem Rx, which has util_valid_regularizer, decodes 148's now-correct 240ksym Tx to the golden message.) Prev broken = cap varying + ~50%.
- OFFLINE link capture 148->146 decoded (at decimated rate, since the util_adc_pack DMA capture path is itself 2x-oversampled): golden 21/22 frames, BER 1.70%. Prev = 0/16, 43%.
ROOT CAUSE (confirmed fixed): DAC fabric interface = 2 samples/beat (number_of_inputs=4, i0/q0=sample n, i1/q1=sample n+1) but the modem drove only 1 sample/beat (i1/q1=0) -> DAC transmitted 1 real sample per 2-sample beat = HALF symbol rate (120 vs 240 ksym). FIX = tx_dac_gather.v gathers 2 consecutive modem samples -> (i0/q0,i1/q1) per dac_1_valid_i0 beat -> full 240ksym. User-directed (interface rate config / 2-clocks-per-sample). iverilog GATHER_SIM_PASS. The ENTIRE session's 43% = this 2x rate (a 240ksym decoder on a 120ksym waveform reads ~random; fooled soak_decode/on-chip-BIST/EVM all session).
NOTE: my offline iio_readdev captures are independently 2x-oversampled on the util_adc_pack DMA path (no regularizer there) -> decode at decimated rate OR trust the on-chip modem Rx (regularizer). Residual 1.70% BER (vs <0.01% target) = cross-board two-clock CFO/timing + imperfect offline decimate-decode = normal link tuning, NOT the fundamental defect. NEXT: tune CFO/carrier-sync for <0.01%; deploy gather fix to 146 too for bidirectional; the fix (tx_dac_gather + BD surgery /tmp/gather_fix.tcl) should fold into a clean jupiter_240k5 rebuild. Board 148=gather fix (753760ad3310), 146=stock 736e0c26.

## 2026-07-06 *** COMPLETE: bidirectional link at 0.000% BER, gather fix proven both boards ***
Both boards run the gather fix (753760ad3310). From a CLEAN reboot state:
- 148->146 @2.0GHz: 65/65 golden frames, 0.000000% BER (both decimate phases, incl acquisition). On-chip BIST cap=0x04922282 golden stable.
- 146->148 @2.0GHz: on-chip BIST cap=0x04922282 golden stable; offline 54/55 golden, steady-state 0.000000% BER (1 acquisition frame).
=> BIDIRECTIONAL link at 0% BER, WELL under the <0.01% gate. The entire session's 43% is RESOLVED by tx_dac_gather (the modem was transmitting at half symbol rate; gather feeds 2 samples/beat to the 2-sample/beat DAC interface -> full 240ksym).
OPERATIONAL LESSONS: (1) My offline iio_readdev captures are 2x-oversampled on the util_adc_pack DMA path -> decode at DECIMATED rate (either phase); the on-chip modem Rx (util_valid_regularizer) is the reliable native-rate metric. (2) Many manual LO/ensm/near-end toggles ACCUMULATE bad ADRV9002 state -> spurious 43% failures; a clean REBOOT restores 0%. Always reboot to a clean state before a link measurement. (3) The 1 non-golden frame per run is always the acquisition transient (carrier-sync lock-in) -> steady-state=0%.
REMAINING (normal RF bring-up, not the fix): FDD-SIMULTANEOUS (both TX+RX active at 2.40/2.45 concurrently) still shows degradation -> needs per-carrier ADRV9002 calibration (2.45 uncalibrated failed; 2.0 works) + clean state mgmt; do each direction from a clean arm. Fix reproducibility: subagent building jupiter_gather_clean (build_gather_clean.sh + complete_and_gather.tcl) in progress.

## 2026-07-06 FDD-simultaneous investigation: Tx->Rx spur on the Rx-below-Tx link
Profile lvds_1p92 is FDD (duplexMode=1; both out/in_voltage0_ensm_mode=rf_enabled simultaneously). Chip does genuine simultaneous Tx+Rx.
FINDINGS (all from clean-reboot states):
- Single-direction ALWAYS works: 148->146 and 146->148 both golden/0% at 2.0, 2.40, 2.45 (each board's Tx and Rx individually fine).
- FDD 50MHz (148 Tx2.45/Rx2.40, 146 Tx2.40/Rx2.45): Link B (146 Rx@2.45, Rx-ABOVE-own-Tx) = GOLDEN. Link A (148 Rx@2.40, Rx-BELOW-own-Tx) = stable-WRONG cap 0xC4DC1A42.
- SWAP test (148 Tx2.40/Rx2.45, 146 Tx2.45/Rx2.40): failure FOLLOWS the freq-plan not the board -> whichever board has Rx BELOW its own Tx fails (146 now wrong 0x89C6148C). => a Tx-related spur lands in the Rx band when Rx<Tx. In FDD one board ALWAYS has Rx<Tx -> one link always fails.
- Tx power backoff (148 Tx atten -10/-20/-30dB): NO effect on the failing link (rssi + wrong-cap constant) -> NOT Tx-signal self-interference; likely Tx LO/PLL spur or reciprocal mixing (independent of Tx atten).
- 100MHz separation (2.40/2.50): WORSE, BOTH links fail (varying cap). So wider separation didn't move the spur out; no clean win by spacing alone.
REMAINING (RF/cal, not the modem): FDD Tx-Rx isolation for the Rx-below-Tx config. Candidates: (a) enable/tune ADRV9002 Tx LO-leakage + QEC tracking cals (needs API, not obvious in sysfs); (b) find a spur-free carrier pair empirically; (c) use Rx on channel 2 (separate from Tx ch1) for better isolation (needs modem rebuild); (d) external duplexer/filter (depends on physical cabling). Physical RF setup (duplexer? direct cables? circulator?) unknown -- gating factor.
CORE GOAL ACHIEVED regardless: gather fix -> bidirectional single-direction 0.000% BER both ways. Boards currently in a failed-FDD state; reboot for clean single-direction 0%.

## 2026-07-06 FDD-simultaneous: exhausted SW knobs -> RF-hardware frontier
Confirmed separate PLLs (TX1_LO=2.45 & RX1_LO=2.40 both hold -> valid FDD, not a shared-LO limit). Rx tracking cals were OFF by default: quadrature_w_poly (QEC)=0, hd=0, initial_calibrations=off (bbdc/rfdc/agc were on). Enabled QEC+HD+ran initial_calibrations at the FDD carriers -> did NOT fix (both links varying/wrong).
FDD SW knobs EXHAUSTED (none fixed the Rx-below-Tx spur): Tx power backoff, wider separation (100MHz worse), Rx QEC/HD/init-cals. => the Rx-below-own-Tx corruption is a Tx LO/PLL spur or Tx->Rx isolation issue in this wired setup, needing RF-level remedies: (a) empirical spur-free carrier planning, (b) external duplexer/filter (physical setup unknown), (c) Rx on ch2 separate from Tx ch1 (modem rebuild), or (d) deeper ADRV9002 API cal (not sysfs-exposed).
ALTERNATIVE for the user's bidirectional goal that SIDESTEPS FDD self-interference: time-alternating (pseudo-TDD) -- each direction works at 0.000% single-direction, so ping-pong the two directions in time slots. Achieves bidirectional 0% without simultaneous Tx+Rx.

## 2026-07-06 FDD separation sweep -> no separation gives clean bidirectional; isolation-limited
FDD-simultaneous BIST results (clean reboot each, both boards Tx+Rx):
- 25MHz (2.40/2.425): BOTH links varying/fail.
- 50MHz (2.40/2.45): Link A(Rx-below-Tx) stable-WRONG, Link B(Rx-above-Tx) GOLDEN. (only working case, and only 1 of 2 links)
- 100MHz (2.40/2.50): BOTH fail.
- 500MHz (2.0/2.5): BOTH fail.
=> Moving the 2nd link FURTHER does NOT help (worse). No separation yields clean bidirectional FDD. Only the marginal 50MHz/Rx-above case worked. CONCLUSION: the simultaneous Tx desensitizes/corrupts the Rx regardless of separation -> Tx->Rx ISOLATION limited in this wired setup (not a spur that separation avoids). Tx-power-backoff had no effect -> LO/broadband, not signal.
GATING UNKNOWN: physical RF setup. If direct point-to-point cables (Tx1->Rx1 each way, 2 cables) the only Tx-Rx coupling is the chip's internal isolation (insufficient for close-in FDD). If shared medium/splitter, both carriers hit each Rx hard.
RECOMMENDED PATHS: (1) time-alternating pseudo-TDD -> bidirectional 0.000% (proven, sidesteps FDD self-interference) -- BEST pragmatic option; (2) external duplexer/bandpass filters per port; (3) Rx on ch2 separate from Tx ch1 (modem rebuild for hardware isolation); (4) lower Tx + link-budget (isolation-limited, so limited help).

## 2026-07-06 FDD profile (lvds_15p36_jupiter) tested -> does NOT fix FDD; root = on-die Tx-LO->Rx coupling
Deployed profiles_fdd/lvds_15p36_jupiter.{bin,json} to both boards (fs=15.36MHz). Modem WORKS at 15.36: loopback golden 0x04922282, single-direction air golden (146 Tx@2.40 -> 148 Rx@2.40, 148 Tx off). So gather fix + modem are rate-agnostic (1.92 and 15.36 both fine).
FDD-simultaneous STILL FAILS with the FDD profile (148 Rx@2.40 cap=0x0, 146 Rx@2.45 varying). KEY: the FDD profile's LO map (rx1LoSelect=2, tx1LoSelect=1 = Rx->LO2, Tx->LO1, separate) is IDENTICAL to the 1.92 profile -> LO mapping was never the difference. Both profiles duplexMode=1.
DEFINITIVE MECHANISM: with a board's OWN Tx ON, its Rx fails (0x0); turning the own Tx OFF (ensm=calibrated) -> Rx decodes GOLDEN, at the SAME rssi (13.78dB unchanged). So the own Tx does not change signal LEVEL -> it injects in-band interference. Tx signal-atten (-30dB) had NO effect but full Tx-off does -> it's the Tx LO/PLL (LO1) phase-noise/spur coupling into the Rx (LO2) ON-DIE, independent of Tx signal power and carrier separation (25/50/100/500/1100MHz all fail). This is a fundamental FDD Tx-LO->Rx isolation limit of this ADRV9002 config/wired setup.
POSSIBLE REMEDIES (not yet tried): (a) profile regen with rfPllPhaseSyncMode / different loGen/PLL isolation config; (b) Rx on ch2 + Tx on ch1 physical-pin separation (modem rebuild); (c) external duplexer/filter; (d) PRAGMATIC: time-alternating pseudo-TDD -> bidirectional 0.000% (proven). Boards currently on FDD 15.36 profile; reload lvds_1p92 (or reboot) for the verified 1.92 single-direction 0% state.

## 2026-07-06 LO INDEPENDENCE VERIFIED (stock 7f662cba image): LOs ARE independent; FDD fail = isolation not tuning
Tested on 148 flashed to stock/base 7f662cba (no gather), receiving 146's Tx@2.40, sweeping 148 RX_LO with TX_LO fixed @2.45. RSSI (true indicator; capture rms flat due to AGC normalization):
- RX_LO=2.400 on-tune, 146 Tx ON: rssi 13.6 dB (signal present)
- RX_LO=2.405 (+5MHz off-tune): rssi 65.4 dB (signal gone) ; RX_LO=2.395 (-5MHz): 65.3 dB
- RX_LO=2.400, 146 Tx OFF: rssi 80.7 dB (noise floor)
- Changing TX_LO 2.45->2.30 (RX fixed): received signal UNCHANGED.
=> Rx LO tunes to its own freq, tracks RX_LO, unaffected by TX_LO -> Tx/Rx LOs GENUINELY INDEPENDENT (hardware property, ruled out shared-LO hypothesis). So FDD failure is CONFIRMED = Tx-LO(LO1)->Rx(LO2) on-die coupling/isolation (Rx receives correctly, rssi present, but own running Tx LO degrades the DECODE without changing signal level), NOT a tuning/independence problem. 148 restored to gather image.

## 2026-07-06 Track A: unused-ch2 disable -> does NOT fix FDD
Both boards had unused ch2 (Rx2+Tx2) rf_enabled by default. Set in/out_voltage1_ensm_mode=calibrated on both, clean reboot, FDD 2.40/2.45: BOTH links still fail (148 varying, 146 varying). => unused-channel loading is not the coupling mechanism.

## 2026-07-06 evening: GOAL tracks A/B executed + MIDDAY RF-LINK REGRESSION isolated to the physical path
GOAL (LO issue via ch2 + disable unused, in parallel):
- TRACK A (disable unused ch2): unused Rx2/Tx2 were rf_enabled by default on both boards. Set in/out_voltage1_ensm=calibrated, clean FDD 2.40/2.45 -> STILL FAILS both links. => unused-channel loading NOT the mechanism. DONE, negative.
- TRACK B (Rx on ch2): flashed 148 to ch2 image (b853f5ef, modem Rx2/Tx2) as receiver; 146 gather full-rate Tx. Rx2 rssi = 75.7dB NOISE FLOOR => the RF CABLES ARE ON CH1 PORTS ONLY; Rx2 SMA has NO cable. Track B REQUIRES a physical cable move (146Tx1 -> 148 Rx2 SMA). BLOCKED on physical access. 148 restored to gather (753760ad).
MIDDAY REGRESSION (started during FDD experiments; poisoned the Tx1/Tx2 proxy experiment): the previously-golden single-direction links (65/65, 54/55 @2.0 same morning) now FAIL in BOTH directions (both cables), at ALL carriers (2.0/2.40/2.45), BOTH rates (1.92 profile + native 15.36 no-profile). Systematically isolated:
- On-chip BIST fails AND offline decode fails 43% on over-cable captures (real corruption, not just acquisition).
- 146 near-end loopback (RFIC digital loopback, skips DAC/analog): offline 21/21 GOLDEN 0.000% -> Tx digital path + modem + decoder all CLEAN.
- RSSI normal (~14.8dB, same as morning), CFO ~0-3.4kHz (fine), temps normal (ADRV 57-60C), BOOT.BINs verified 753760ad both, profiles byte-identical across boards, reboot-fresh states, single-arm discipline.
=> corruption is in DAC->analog->CABLE/PAD->analog->ADC, BOTH direction sets simultaneously, appearing midday with no software cause found. PRIME SUSPECT: physical RF path (cable/pad/connector degradation or a shared medium element) -- only physically inspectable. NOTE: earlier "near-end mode = valid analog test" was wrong: rx0_near_end_loopback is the RFIC DIGITAL loopback (host-golden through it clean = digital only).
NEXT SESSION: (1) user physically inspects/reseats RF cables+pads (also enables Track B: cable 146Tx1->148Rx2); (2) retest single-direction @2.0 after reseat (should be 0%); (3) then Track B ch2-Rx FDD experiment. On-chip near-end BIST stable-wrong = known near-end-mode BIST quirk (ignore; offline is truth there).

## 2026-07-07 *** INTERFERER SOURCE VERIFIED: unused-ch2 (Tx2) DDS default tone, in-band at TX_LO+75kHz ***
Via the user's new 146Tx2->148Rx2 cable, measured DIRECTLY: 146's Tx2 transmits a strong CW tone at TX_LO+75.3kHz (rms 8754; ensm-off -> rms 12 = source proven Tx2). CAUSE: ch2 DAC defaults to the stock DDS source (never overridden in our images) + out_voltage1_ensm boot-default=rf_enabled + TX2_LO MIRRORS TX1_LO -> the TRANSMITTER's own ch2 tone rides +75kHz IN-BAND of every link it transmits, at full amplitude, into the victim Rx. This matches the ORIGINAL session impairment analysis EXACTLY (determine_source: spur@60-80kHz; reproduce.m: CW spur 0dBc -> 43.4%!). ANSWER to the user's sweep question: the tone TRACKS TX_LO (always +75kHz in-band of the paired link) -> NO carrier from 50MHz-2GHz escapes it; the fix is DISABLING the unused ch2 (out_voltage1_ensm=calibrated at every boot!) or disabling the DDS in the FPGA image. NOTE Track-A's earlier negative (ch2-off didn't fix FDD) ran DURING the regression window -> INVALID; Track A must be re-run clean.
LO VERIFICATION (user Q): CW through both cables = 29Hz wide, -90dBc@1kHz, phase step 0.26deg/sample -> Tx/Rx LO pair rock-stable + independent. LO is NOT the issue.
## 2026-07-07 REGRESSION isolated to ADRV9002/analog domain; needs COLD POWER CYCLE
Since midday 07-06: ALL wideband QPSK through the analog path fails 43% (BOTH boards, BOTH directions, all carriers 2.0-2.45, both rates, MODEM and HOST-DMA sources equally) while: CW pristine, LOs stable, near-end digital 21/21 clean, cables reseated+verified, rssi normal, temps normal, init-cals re-run (echo run > initial_calibrations), ch2 tone killed, images verified. Host-DMA failing kills every FPGA-side hypothesis -> the fault is in the ADRV9002 chip state / analog Tx-or-Rx wideband path on both boards. Linux reboots do NOT clear it -> RFIC state may survive warm reboot -> NEXT: USER COLD POWER-CYCLES BOTH BOARDS, then re-run: (1) single-dir @2.0 with ch2 killed (expect 0%), (2) FDD 2.40/2.45 with ch2 killed on both = the REAL Track-A test (may genuinely fix FDD now that the tone is understood!). Board images: both gather 753760ad. writedev NOTE: tx channels are voltage0/voltage1 (not voltage0_i/_q).

## 2026-07-07 post-power-cycle: regression PERSISTS -> physical-path audit needed
Cold power cycle both boards (user) did NOT clear the wideband-QPSK 43% corruption (link @2.0, ch2 killed, clean arm: 0/8). Tx power backoff sweep: rssi identical at txgain 0 and -10 (Rx front-end SATURATED at full power - a pad may be missing/changed post-reseat) but -20/-30dB (linear region, rssi 25/35dB) STILL fails 0/5 -> compression present but NOT the root cause.
COMPLETE elimination at this point: FPGA paths (modem+host-DMA both fail identically), chip state (power-cycled), init-cals (re-run), LOs (CW 29Hz/-90dBc pristine), ch2 tone (killed), cables carry CW cleanly, rssi/temps normal, images verified, decoder proven. The wideband corruption survives EVERYTHING software -> the remaining variables are PHYSICAL: (a) actual cable/pad topology post-reseat may differ from assumed (PROVEN mislabel already: the cable the user called '146Tx1->148Rx2' actually carries 146's Tx2! Other connections may be crossed similarly - e.g. link cables swapped between boards' ports); (b) an external/bench RF change midday 07-06 (when the regression first appeared, BEFORE the reseat). 
NEXT: user physically audits + documents the exact RF topology (which port on which board each cable end connects to, incl pads values); ideally test one direct known cable 146Tx1->148Rx1 with a pad. Alternatively hook a spectrum analyzer on 146Tx1 output to see the transmitted QPSK quality directly. THEN rerun test1 (single-dir @2.0 ch2-killed) and test2 (FDD 2.40/2.45 ch2-killed = the REAL Track-A FDD validation).
GOAL STATUS (LO issue / ch2 / disable-unused): ANSWERED - interferer source VERIFIED = unused-ch2 DDS default tone at TX_LO+75kHz (in-band always, tracks TX_LO, no carrier escapes; matches original spur@60-80kHz + 0dBc->43.4% reproduction); FIX = disable unused ch2 at every boot (out/in_voltage1_ensm=calibrated) pending clean-bench revalidation; LOs verified independent + stable (not the issue).

## 2026-07-07 OTA + stock-image tests: regression persists thru EVERYTHING; elimination matrix complete
User replaced all cables with ANTENNAS (all 4 ports). OTA 146Tx1->148Rx1 @2.40: rssi 52dB (SNR~27dB, ample) -> STILL 43% (offline dec2). Then STOCK image (7f662cba) on BOTH + host-DMA golden OTA -> STILL 43% (raw and dec2). Receiver's own-Tx1-DDS-blocker theory: killed 148 Tx1 -> still 43%. rf_bandwidth verified 1.2MHz both boards (profile-correct; narrow-filter theory dead). Profiles verified byte-identical to repo-master originals. writedev note: stock tx channels = voltage0/voltage1.
COMPLETE ELIMINATION MATRIX (43% persists through ALL): FPGA image (custom+STOCK), data source (modem+host-DMA), medium (cables+ANTENNAS), chip state (cold power-cycle), profile files (byte-verified), init-cals, LOs (CW 29Hz pristine), interferers (ch2 tone + receiver own-Tx killed), Rx compression (backoff sweep), rf_bandwidth (1.2M correct), temps, both boards, both directions, carriers 2.0-2.45, rates 1.92/15.36. ONLY post-regression SUCCESS: near-end DIGITAL loopback 21/21 (decoder+digital-chain proven). SSI debugfs: tx0 dataError:1+strobeAlignError:1 BOTH boards (possibly stale latches; live delay writes no effect; proper SSI test-mode flow untried).
NEXT SESSION - THE ZEDBOARD BRACKET (independent known-good hardware): ZedBoard 10.0.0.128 (zed_240k5/zed_bytetx, CMOS 1.92, PROVEN Tx: linkB_s4d ZedBoard->Jupiter = 0%). Transmit from ZedBoard -> receive on 146/148 Jupiter: 0% => Jupiter Rx fine, BOTH Jupiter Tx implicated (analog Tx datapath common-mode). 43% => Jupiter Rx/environment implicated. Also untried: adrv9002 SSI test-mode calibration flow (test_mode_configure + pattern + delay sweep, needs FPGA-side pattern gen), API-level TxAttenuation/pa_protection check, adrv9002 driver dmesg for cal errors at profile load.
GOAL-relevant state preserved: interferer source = ch2 DDS tone VERIFIED (fix: disable unused ch2 at boot); LOs verified independent+stable; FDD revalidation pending regression fix. Boards: BOTH ON STOCK 7f662cba right now; gather image saved two_jup/gather_BOOT.BIN (+on-board backups); antennas on all ports.

## 2026-07-07 *** LINK FIXED: bidirectional OTA 0.000% BER; "regression" = MY receiver arm sequence (self-jam) ***
THE REGRESSION WAS NEVER HARDWARE: writing the full modem arm (0x000 reset + 0x118=0 + 0x114=1) on the RECEIVER side ARMS THE RECEIVER'S OWN MODEM TX -> it transmits the identical-format QPSK (own boot/set TX LO) -> SELF-JAM: valid preambles + uncorrelated payload superposition = the 43%-with-preambles signature that defeated every hardware theory (survived stock image/power-cycle/antennas because the JAMMER was re-armed by my own scripts every test!). Morning-good runs had MINIMAL receiver arms (LO+ensm+gain+rstCS only) -> no self-jam.
ARM DISCIPLINE (canonical): TRANSMITTER = full arm (0x000 pulse, 0x118=0, 0x114=1, DAC mux 0x418/0x458=0x2+0x044=1, rstCS). RECEIVER = MINIMAL arm ONLY: own Tx ensm=calibrated (safety), RX1_LO, in_voltage0 ensm=rf_enabled, gain automatic, rstCS pulse. NEVER write 0x000/0x118/0x114 on the receiving side. Kill ch2 (in/out_voltage1=calibrated) on both at every boot (DDS tone).
RESULTS (gather 753760ad both, ANTENNAS, 2.0GHz, ch2 dead, correct arms): 146->148 BIST 8/8 GOLDEN + offline 43/43 0.000000%; 148->146 BIST 8/8 GOLDEN. THE JUPITER-JUPITER LINK IS FIXED AND VERIFIED BIDIRECTIONAL OTA.
Antenna sweep (400M-3.5G): antennas usable everywhere except 1.5G null; best 2.0/3.5G. NOT mismatched.
FDD-SIMULTANEOUS (correct arms, tones dead, 50MHz and 1GHz separations, atten -10, manual gain tried): still fails - own-Tx antenna (cm from own Rx antenna) causes ~18-20dB AGC desense (rssi 43->62dB when both Tx on); blocker/wanted ratio is GEOMETRIC (self-antenna distance vs link distance) - frequency separation and Tx power cannot beat it. FDD needs: (a) physical antenna separation per board (Tx antenna away from Rx antenna - even 0.5-1m helps quadratically), (b) duplexer/filters, or (c) accept time-alternating (each direction proven 0%). Caps in failed FDD are FROZEN (modem sees no frames) vs varying in decodable-but-erroring conditions.
Stock-image anomalies during the hunt (host-DMA 43% etc) = mixture of self-jam (receiver modem armed... no modem on stock - those were the DDS/half-rate/rate-chain confusions) - academic now; gather modem image is the product and works.

## 2026-07-07 FINAL CORRECTIONS (user-driven): NO RF blocker exists; BIST mirage found; OTA level limit found
(1) USER WAS RIGHT - NO BLOCKER: measured own-Tx ON vs OFF in FDD @2.40/2.45 full power: IDENTICAL rssi (52.2dB), IDENTICAL AGC gain (34dB), IDENTICAL spectrum (in-band/oob 38dB, no tones). The ADRV9002 zero-IF downmix + BB filter handles the 50MHz-offset own-Tx COMPLETELY. My 'AGC desense' theory was WRONG (the 20dB rssi delta was my own atten -10 + antenna band difference 2.0-vs-2.4G). NOTHING RF blocks FDD.
(2) BIST MIRAGE: rx_input_select BOOT DEFAULT = 0 = INTERNAL loopback. Minimal-arm 'BIST golden' = modem decoding its OWN internal Tx (proven: killed far Tx, rssi 80=noise, BIST still golden). All 07-07 '8/8 GOLDEN' BIST readings = mirage. THE REAL PROOF of the OTA link = OFFLINE decode of the air capture: golden_ota.iq = 43/43 = 0.000000% (146->148 @2.0 OTA). Air-BIST requires full canonical arm (0x000 THEN 0x118/0x114 THEN mux THEN rstCS); writing 0x114/0x118 on a RUNNING modem WEDGES it (bisect: STEP1 0x114-write broke, only 0x000 reset recovers).
(3) OTA LEVEL LIMIT (why air-BIST fails OTA while offline decodes): OTA ADC level rms=79 (-52dBFS) vs cable-era 5600-8200. Analog gain maxed (34dB), interface_gain locked 0dB in this profile. The modem's fixed-point AGC (build-time thr=0.0015625 ~ 51/32768) is AT the margin -> on-chip Rx cannot converge at OTA levels (30s settle: no lock). The offline decoder normalizes -> decodes the same capture 0.000%. FIXES: (a) closer/directional antennas or cables (level fix), (b) REBUILD modem with lower AGC thr (commhdlQPSKTxRxParameters thr param) for OTA operation, (c) gate via offline decode.
(4) FDD OUTLOOK REVISED: with (1) no-blocker measured + ch2 tones killed + correct arms + adequate LEVELS (cables or closer antennas), full FDD-simultaneous is expected to WORK - nothing fundamental remains. The remaining engineering item = input level / AGC threshold, not RF isolation.
BOARD STATE: both gather 753760ad, antennas, 148 canonical-armed Rx@2.40 (Tx silent), 146 full-armed Tx@2.40 atten0. Kit: /mnt/onetb/scratch/qpsk_variants/jupiter_240k5 (thr in commhdlQPSKTxRxParameters.m).

## 2026-07-07 (late): LNAs enabled (+17dB, user pointer); link INTERMITTENT - unresolved flip between golden and 43%
LNA CONTROL (AD-JUPITER-EBZ RF FE): debugfs iio:device2 agpio4=RX1A, agpio5=RX1B, agpio6=RX2A, agpio7=RX2B; NUMERIC writes: echo 1 > agpioN_direction; echo 1 > agpioN_value (HIGH=LNA on, LOW=bypass; "out"/string writes silently fail). Effect measured: rssi 52->35dB, ADC rms 79->582 (+17dB). RESETS AT BOOT - re-enable after every reboot. TX PA/switch: out_voltage0_port_select (TX1A/TX1B) exists on phy sysfs.
INTERMITTENT LINK (the remaining problem): the same clean protocol (reboot -> profile -> ch2 kill -> [LNA] -> single canonical arms) flips between GOLDEN 43/43 0.000% (golden_ota, earlier today @2.0 no-LNA) and 43%-wrong-payload across identically-executed runs hours apart. Tested today and NOT the cause: LNA on/off (A_LNAoff control failed too), Tx atten (0/-15/-20/-25), carrier (2.0/2.40), Tx arm-retry x8 (no golden), levels (rms 79-1284 all fail when failing). KEY EVIDENCE on failure mode: BIST caps often STABLE-wrong (e.g. 0x87F7A0CA x8) = receiver successfully decoding a CONSISTENT NON-golden message -> suspicion = Tx-side message/FEC/packetizer init race producing a corrupt-but-consistent bitstream; but arm-retry x8 gave VARYING caps (mixed evidence). The flip correlates with NOTHING controlled so far (not procedure, level, carrier, image, medium, LNA).
NEXT SESSION PRIORITIES: (1) When it fails, capture + decode the TX BOARD's OWN near-end DIGITAL loopback IN THE SAME MINUTE as the air fail -> if near-end golden while air wrong-payload simultaneously, corruption is between SSI-loop tap and DAC (chip Tx datapath); if near-end also wrong-payload, the modem Tx message source is corrupt -> RTL init race -> fix in the packetizer/FEC reset. (2) Alternatively decode the WRONG payload itself (soak_decode with free-running descrambler / dump the decoded bits from a stable-wrong capture and compare vs golden bits under shifts/scrambler states -- identifies WHERE the bitstream diverges). (3) The zed-era 'FEC encoder zero-stuff' and 'PRBS keepalive' bug family are precedents (memory: qpsk-fec-zed-encoder-fix, qpsk-zed-prbs-keepalive). (4) Consider a soak harness: auto re-arm + BIST check loop logging gold/fail per arm over hours to get statistics vs time/temp.
BOARD STATE: both gather 753760ad, antennas all ports, LNAs currently ON (reboot clears), 1.92 profile loaded, 146 armed Tx@2.0 atten0, 148 canonical Rx@2.0. Proven-good artifacts: golden_ota.iq (43/43), ne146_now.iq (near-end 21/21), all tools in two_jup/.

## 2026-07-07 SOAK HARNESS deployed + first findings
soak_harness.sh (two_jup/) running detached: canonical arm -> BIST x5 -> classify GOLD/STABLEWRONG/VARYING/DEAD -> soak/soak_log.csv (ts,iter,mode,class,locks,caps,rssi,temp148,temp146,cfo_hz,capfile); fail captures soak/fail_*.iq (cap 100); reboot both every 10 iters; stop via touch soak/STOP. LAUNCH: setsid nohup ./soak_harness.sh (MUST setsid - tool-timeout kills the process group otherwise). pgrep self-matches from the query shell - check liveness via csv timestamp freshness instead.
FIRST DATA: iterations all VARYING 0/5 so far; measured XO CFO drift = -5.4to-5.5kHz @2.0GHz (was ~0 during golden runs, +3.4k midday) -> thermal XO drift real; TRIMMED the LO (146 TX1_LO=2000005430) -> residual CFO -62Hz VERIFIED -> LINK STILL FAILS -> CFO drift is a co-symptom NOT the flip cause. Also LNA re-enable is in the harness reboot path.
ANALYSIS QUEUE for next session: (1) payload-diff the fail captures (soak/fail_*.iq) against golden bits -> find WHERE the bitstream diverges (Tx packetizer/FEC/scrambler init-race hypothesis; stable-wrong caps = consistent wrong message decoded); (2) correlate class vs temp/cfo/mode in soak_log.csv; (3) compare a GOLD-iteration capture (if any) vs fail captures directly.

## 2026-07-07 MORNING SOAK ANALYSIS: persistent-fail epoch; Tx PROVEN golden while air fails; air payload = RANDOM
SOAK STATS (591 iterations, 03:47-07:58, ~4h): ZERO GOLD. 566 VARYING / 15 STABLEWRONG / 2 DEAD; reboot-mode vs rearm-mode both fail (56 vs 510 VARYING). Temps flat 55-60C, CFO flat -5.5kHz, rssi ~25dB (LNA on). => NOT thermal flipping on this timescale; the link entered a PERSISTENT failing epoch (contrast golden_ota 43/43 the prior evening).
DECISIVE EXPERIMENT EXECUTED (from queue): during the failing state, 146 near-end DIGITAL loopback (rx0_near_end_loopback) decoded **6/6 GOLDEN infoErr=0** while the air simultaneously fails 43%. => TRANSMITTER MODEM/FPGA/MESSAGE FULLY EXONERATED in-fail. Corruption strictly in: chip Tx datapath -> DAC -> antenna -> air -> antenna -> Rx chip -> ADC. (Wideband-only: CW through same path pristine.)
PAYLOAD-DIFF (corrected: info bits = dec(TB+1:TB+120), TB=25; earlier 'all-zeros' was MY offset bug reading the Viterbi traceback prefix): air-decoded payloads are RANDOM - err 52-55/120 (=chance vs weight-51 golden), pairwise 47-69 (~60=random), no circular-shift structure, different every frame. NOT a structured Tx bug; genuine symbol scrambling. Odd CFO signatures in fail decodes: 4th-power ~+3.85kHz but preamble-refine -13.6kHz 'IMPLAUSIBLE' - inconsistent estimators may hint at the corruption type (phase dynamics).
TOOLS: soak_dumpbits_k5.m (k5_240/) dumps per-frame decoded bits (D.info{k}, full dec incl TB prefix); payload_diff2.m, ne_diff.m (two_jup/). Harness STOPped (soak/STOP).
NEXT-SESSION DISCRIMINATOR (cheap, unrun): on the GATHER image during fail: host-DMA golden via Tx1 (bypasses the gather+modem, uses upack path) -> air decode. If host-DMA WORKS while modem fails -> the gather/DAC-beat interaction at the CHIP interface is implicated (rate/beat mismatch only manifesting under some chip state); if host-DMA ALSO fails -> chip-level Tx datapath/profile state. Also compare in-fail vs golden_ota captures' PHASE dynamics (the inconsistent CFO estimators). Consider ILA on dac_1_valid cadence in-fail vs in-golden.

## 2026-07-07 HOST-DMA DISCRIMINATOR + CFO closure: corruption pinned to chip-Tx-datapath/DAC->air->Rx domain
DISCRIMINATOR (A/B/A same-minute, failing epoch active): modem-Tx air = 43.9% -> HOST-DMA golden air = 43.2% -> modem again = 43.2%. Host-DMA (upack path, bypasses gather+modem entirely) fails IDENTICALLY. Every FPGA source exonerated (near-end SSI tap simultaneously golden 6/6).
CFO CLOSED PROPERLY: wide-range 4th-power (search +-50kHz; earlier masks +-8k were the blind spot, and determine_source's -11.9k "residual" was its own carrier-sync mislock artifact, NOT real): true CFO = -5.4kHz (matches harness estimator). Trim +5.42kHz applied FROM CLEAN BOOT (not live-retune) -> CFO nulled -> STILL FAILS 0/8. CFO/XO-drift RIGOROUSLY eliminated as the epoch cause.
7-tap complex channel fit: residual 99.8% (says NOT static-linear ISI, though its sync chain is suspect on these signals). Phase after removing its linear term: white 14deg steps, EsN0~15dB apparent.
STANDING CONCLUSION: wideband QPSK is scrambled between the ADRV9002 SSI input (golden, proven live) and the far ADC output, on BOTH boards, epoch-like (clean 07-06 evening golden_ota 43/43 -> failing since), CW-clean, level/CFO/source/medium/image/profile independent. Remaining candidates: (a) chip Tx datapath state (interpolation/pFIR) entering a bad mode - would need ADI API-level datapath introspection or ADI support; (b) the shared physical environment/antenna near-field (cm-spaced antennas, boards adjacent) - needs physical variation (move antennas/boards apart, re-test in both states); (c) something in the Rx digital-capture path common to both (but BIST+offline agree). NEXT: physically separate the antennas + rerun; if persists, adrv9002 driver debug (dmesg during profile load, API datapath dumps) or ADI wiki SSI-test-mode calibration flow.

## 2026-07-07 (mid-morning) SEPARATED ANTENNAS + PORT/LNA sweeps: OTA still 41-43%; KEY TIMELINE REFRAME
TIMELINE CORRECTION (important): golden_ota 43/43 (the only OTA-era golden) was actually over the RESEATED CABLES - the antennas were connected AFTERWARD. => THE OTA LINK HAS NEVER WORKED (0 golden in ~600 attempts); the failing "epoch" boundary = the cable->antenna swap. The corruption is OTA-specific (cables worked at 0.000%).
Post-separation tests (user moved ch1 antennas apart on each radio): rssi improved to 17dB (rms 3205, near cable level) - still 41% offline / 0/8 BIST. TX port_select verified: tx_a correct (tx_b drops 36dB). RX LNA A/B sweep (agpio4/5 = 10/01/11/00): rssi IDENTICAL 17.1-17.2dB in all four (LNA writes no longer move rssi - strong signal, AGC headroom) and all fail. CFO trimmed (+5.42k from clean boot). Near-field theory: not confirmed by separation.
NET: cables = 0.000% proven; antennas = always fails despite good level/CFO/clean states/either port/any LNA config. The OTA-specific corrupter is still unidentified: remaining candidates = rich indoor multipath at delays outside the toy 7-tap fit (an equalizer/OFDM-grade channel is not what this modem handles - though 340kHz BW should be flat...), an environment interferer at 2.0 GHz (spectrum looked clean), or an FE/antenna-path dispersion effect common to both boards. PRACTICAL PATH: the system is PROVEN at 0.000% over cables (incl. bidirectional + the pads); OTA needs a channel investigation (capture-based channel sounding: transmit the golden, correlate to extract the impulse response - the golden xcorr tooling exists) OR accept cabled operation. All tools/captures in two_jup/ (separated.iq = latest OTA fail w/ good level).

## 2026-07-07 *** ROOT CAUSE OF THE FLIP FOUND: per-boot ADRV9002 RX-CHAIN INIT QUALITY (receiver-side coin flip) ***
Phase A executed: OTA channel sounding (sound_channel.m, LS deconvolution vs known golden): OTA channel is CLEAN in good boots (LS residual 5.7% = same as cable control; delay profile transparent; static, 1.6deg/frame). A-GATE PASSED: host-DMA golden OTA = 219/220 golden 0.000000% steady-state; modem Tx OTA = 43/44, 0.000000% steady-state. THE OTA LINK WORKS.
THE FLIP: same explicit protocol pass/fail per BOOT. SPLIT TEST: fail state -> reboot ONLY 146(Tx): still 41.5% -> reboot ONLY 148(Rx): 21/21 GOLDEN 0.000000%. => the pass/fail coin = 148's (receiver's) PER-BOOT RX INIT. Fail-boot signature: received waveform = 84%-golden through a linear-ish distortion (LS residual 16.3% vs 5.7% good; main tap shifted +1 symbol) - a degraded RX filter/cal state that passes CW (-61.6dBc image, tone clean) but distorts wideband. Explains ALL prior "epochs" (incl cable-era regression days!) - every reboot re-flipped the coin on either board's Rx.
ELIMINATED for the flip: CFO (<1kHz both pass+fail, wide-range verified; the dec2-domain-doubling theory also dead), Tx QEC image (-61.6dBc in fail boot), antennas/geometry (post-separation channel proven clean), Tx-side boot (146 reboot didn't flip it).
NEXT: (1) on-chip BIST check in good boot (+anti-mirage); (2) OPERATIONAL FIX = per-boot go/no-go gate + reboot-until-good (scriptable; also try profile-reload or initial_calibrations run as a cheaper in-place fix on a bad boot); (3) diagnose bad-vs-good boot via dmesg adrv9002 lines diff + report to ADI (runtime stream/profile load ordering suspect); (4) proceed to bidirectional + FDD gates with the go/no-go protocol.
Artifacts: sounding_ota.iq (219/220), ab_modem.iq (pass), repro.iq (fail 16.3%-residual), split146/148.iq, sound_channel.m.

## 2026-07-07 *** FINAL COHERENT PICTURE: capture DMA taps THROUGH the modem; 'corruption' = unlocked-modem processed stream ***
DEFINITIVE same-boot A/B on 148: capture with rx modem UNARMED = 5.6% LS-resid GO (clean antenna signal); apply ONLY the modem arm (0x000+0x118+0x114+rstCS) and recapture = 16.3% NOGO w/ -541Hz apparent shift. NOT the dec2 phase (both phases 16.x%). EXPLANATION: this design's 'ADC capture' DMA path goes THROUGH the modem IP (BD: TxRxCompo dut_data_out_*_rx -> sync_output data_in_rx_* -> util_adc_1_pack) - a debug tap. Unarmed modem forwards raw ADC; ARMED modem forwards its (processed) stream -> when the modem is UNLOCKED the armed-capture looks corrupt (16%, CFO-ish -500Hz from its CFC churn). So: armed-capture quality == modem lock state, NOT channel quality. RETROACTIVE REINTERPRETATION: every 'corrupted offline capture' with an armed rx modem (the entire soak, the FDD-era captures, the "receiver-boot coin" split test where the pass = UNARMED pure-capture!) was measuring modem lock, not the air. The AIR/channel has been CLEAN wherever verified unarmed (5.6-5.7% everywhere). The 'per-boot RX-init coin' attribution is WITHDRAWN (the split-test pass was the unarmed capture, not a lucky boot).
=> THE ONE REMAINING PROBLEM: the on-chip modem Rx does not LOCK at OTA conditions (level rms~580 = -35dBFS + AGC thr=0.0015625 cable-tuned) while the offline receiver decodes the same air at 0.000%. This is the Phase B sim agent's exact task (level sweep in the netlist sim -> min-lock level -> thr/AGC parameter fix -> verify on real capture). gonogo.sh updated: verdict captures are UNARMED (valid channel/Tx GO gate); the BIST gate returns after the Phase-B modem fix (Phase C rebuild).

## 2026-07-07 GONOGO HARNESS VALIDATED: GO on try 1, deterministic; OTA link operational
Corrected gonogo.sh (UNARMED verdict captures) end-to-end: LINK GO try 1/1 (residEVM 5.5%, cfo +1Hz); full decode of the GO capture = 15/16 golden, steady-state 0.000000%. With the correct measurement the OTA link comes up clean EVERY boot - no coin, no reboot-until-good needed. Tools: two_jup/gonogo.sh (params: TX_IP RX_IP CARRIER TRIM; logs+dmesg in gonogo_logs/), gonogo_verdict.py (numpy LS-residual, GO<10%/NOGO>12%, exit 0/1/2). Usage: ./gonogo.sh brings the link up verified; Tx stays armed on GO.
OPEN (T8/Phase B->C): on-chip modem Rx lock at OTA level (BIST gate) - sim agent running (netlist-level level-sweep + AGC thr fix). After its fix: Phase C rebuild, BIST gate, reverse dir, FDD.

## 2026-07-07 *** FDD SIMULTANEOUS OTA: GO — BOTH LINKS 0.000% STEADY-STATE ***
fdd_gonogo.sh (two_jup/): simultaneous bidirectional FDD over ANTENNAS, host-DMA golden both sides (modems unarmed -> valid raw-ADC verdicts), carrier-scaled trims (146Tx@2400006504, 148Tx@2449993361):
- LINK A 146->148 @2.40 GHz: GO 5.6% resid; full decode 15/16 golden, steady-state 0.000000% (rssi 19.0)
- LINK B 148->146 @2.45 GHz: GO 5.7% resid; full decode 16/16 golden, 0.000000% aggregate (rssi 29.6)
BOTH DIRECTIONS LIVE AT ONCE, CLEAN. All prior "FDD failures" confirmed as armed-modem capture-tap artifacts + modem no-lock, NOT RF. Also: reverse single-dir gonogo GO try1 (trim -5420 for 148-Tx direction confirmed).
RF/link scoreboard: forward 0.000% / reverse GO / FDD-simultaneous 0.000% both. Tools: gonogo.sh (single dir), fdd_gonogo.sh (FDD), gonogo_verdict.py. REMAINING for full modem FDD: the on-chip modem Rx lock at OTA level (Phase B agent in flight) -> Phase C rebuild -> modem-Tx FDD + BIST gates.

## 2026-07-07 *** PHASE B COMPLETE: modem OTA no-lock root cause = AGC GAIN-WORD WRAP; fix sim-proven; Phase C build launched ***
Phase B agent (Verilator on kit RTL, real captures): the deployed composite ingests 1 ADC sample per /2 rail beat (harness must strobe adc_validIn every 2 clks - old sim_jup.cpp fed /4 = why prior replays never locked; new harness jupiter_240k5/rtl_sim/sim_phaseB.cpp, binaries obj_phaseB2/obj_agcfix). Stock RTL replays split148 at capture level -> golden 0x04922282 (bit-exact repro).
ROOT CAUSE (T8/BIST OTA no-lock): Automatic_Gain_Control.v gain word = sfix16_En14 (range +-2.0) via plain WRAP slice gain[29:14] of the sfix33_En28 integrator. AGC ref 0.25 -> target 8192 LSB -> min normalizable input rms=4096. OTA rms~580 needs gain~14 -> slice WRAPS -> garbage gain -> CFC/preamble collapse (sim: packets=0 capout=0, CFC junk -15..-18kHz = exact HW match). thr=0.0015625 innocent. Lock floor stock ~4900 rms; cable 5600-8200 explains cable-golden.
FIX (sim-verified, codegen-proven): jupiter_240k5/agc_gain_range_overlay.m - delete AGC-level Data Type Duplicate + set Receiver/QPSK Rx/Automatic Gain Control/Data Type Conversion OutDataTypeStr=fixdt(1,16,11) (+-16 range, slice gain[32:17], full accumulator span). Fixed RTL on REAL split148 @rms580: GOLDEN all 22 frames; ab_modem @580: 42 frames golden; no regression x1-x4; new lock floor rms~511 (=8192/16 theory). More margin if needed: fixdt(1,16,10) (+-32, unverified). Report: two_jup/phaseB_agc/PHASEB_AGC_REPORT.txt.
PHASE C: overlay wired into assemble_jupiter_240k5.m (Phase 0b3, after ss8_fix_overlay); tx_dac_gather.v ports patched with X_INTERFACE_IGNORE (fixes the recipe GATE_FAIL: Vivado inferred ADI fifo_rd interfaces on wr_* ports and propagation deleted the dac-lane user nets); full rebuild launched via build_gather_clean.sh -> /mnt/onetb/scratch/qpsk_variants/jupiter_agcfix (multi-hour). Next: deploy 148 first, BIST-on-air gate (canonical arm + anti-mirage), reverse, modem-Tx FDD + soak.

## 2026-07-07 Phase C build: recipe bug chain fixed; agcfix build in flight (WIRE_OK+VALIDATE_OK)
The build_gather_clean.sh recipe's GATE_FAIL root causes (chain of 3): (1) complete_and_gather.tcl SWALLOWED a fatal 'Unable to resolve module-source: tx_dac_gather' from create_bd_cell -> connects no-op'd -> its gate fired; (2) module-source failure = duplicate/conflicting file registration (fix: catch remove_files existing *tx_dac_gather.v, add ONE copy, set_property library xil_defaultlib); (3) the recipe DEFERRED ALL SAVES past its gate -> on failure even the modem-IP insert was lost (saved BD was bare). ALSO: X_INTERFACE_IGNORE attrs added to all tx_dac_gather.v ports (prevents ADI fifo_rd interface inference — earlier failure mode in jupiter_gather_clean).
WORKING COMBINED FLOW (one Vivado session): /tmp/agcfix_full.tcl = template insert (head -43 vivado_insert_ip_TEMPLATE.tcl, correct zip path) + gather surgery (remove/add module file, disc dac lanes, create cell, 12 connects) + inline WIRE assert + validate + SAVE + synth/impl/bootgen. Status: WIRE_OK + VALIDATE_OK confirmed, synth running -> /mnt/onetb/scratch/qpsk_variants/jupiter_agcfix/.../boot/BOOT.BIN. This build contains: AGC gain-range fix (fixdt(1,16,11)) + gather + all kit overlays (rate_240k, ss8_fix). ON DONE: deploy 148, BIST-on-air gate (canonical arm + anti-mirage far-Tx-off control), then 146, reverse, modem-Tx FDD.

## 2026-07-07 agcfix DEPLOYED to 148: AGC fix WORKS on HW but modem still no-lock; netlist replay pending
Build af9dacf9 (AGC fixdt(1,16,11) verified in generated RTL gain[32:17] + gather + overlays) deployed to 148 (gather753 backed up as /boot/BOOT.BIN.gather753). Internal loopback GOLDEN. BIST-on-air vs 146(gather)Tx@2.0+trim: NO LOCK - varying caps @auto gain; analog gain sweep 10-34 manual: no lock at any level; extended settle 60s: cap stays 0x0. KEY: armed-tap capture rms=4576 (raw ~1300) -> the FIXED AGC IS ENGAGING on hardware (amplifying toward the 8192 target, no wrap) -> failure moved DOWNSTREAM (CFC/symbol/frame sync) despite the sim (old-netlist+patched-AGC) locking split148 fully. DISCRIMINATOR IN FLIGHT: Phase-B agent rebuilding its Verilator harness against THIS build's generated netlist (TxRxCompo_ip_src_*.v) to replay split148: locks->integration delta (adc_validIn cadence/scaling on this image); fails->fresh-codegen regression (diff patched vs generated AGC/neighbors; DTC-removal width ripple).
Boards: 148=agcfix af9dacf9 (Rx@2.0 armed), 146=gather753 (Tx@2.0+5420 armed). All harnesses/artifacts in two_jup/.

## 2026-07-07 (eve) T8 status: netlist EXONERATED (locks in sim); runtime/integration delta hunted via signature regs
Agent follow-up: rebuilt Verilator vs the AGCFIX BUILD's own netlist (rtl_sim/wrap_agcbuild + obj_agcbuild; dep-list confirms all .v from jupiter_agcfix hdlsrc): split148@rms580 GOLDEN 22 pkts, ab_modem@580 GOLDEN 42, native GOLDEN. Whole-hdlsrc diff vs old kit: ONLY the AGC file differs (intended fix; no width ripple; CADENCE-v6 present; fix verified in ipshared+both IP zips = synthesized sources). BD wiring verified (IPCORE_CLK=adc_1_clk, RX1->util_valid_regularizer(byte-identical)->sync_input->DUT).
HW diagnostics on the failing state: fs=1920000 CONFIRMED while armed (profile theory dead). Signature regs during fail (agent's classifier: level-starved => cfc_est -15..-18kHz junk + rstcs_count>0; cadence-broken => cfc_est~0 + rstcs=0 + cnt_frame_start=0): raw reads 0x144=0x0 0x148=0x0 0x14C=0xFF0000FF 0x150=0x2F25(12069) 0x154=0x794(1940) 0x158-0x160=0. NEEDS the exact addr map (generated axi_lite decoder in jupiter_agcfix hdlsrc TxRxCompo_ip_src_*axi*.v) to interpret -> ask agent or grep decoder. Also untested: BIST-over-cable control on the agcfix image (needs recabling); armed-tap rms 4576 proves AGC engages on HW.
Agent artifacts: phaseB_agc/PHASEB_AGC_REPORT.txt (full), rtl_sim/obj_agcbuild.
NEXT: (1) map 0x14C-0x160 via the generated AXI decoder -> classify failure (level vs cadence) per the signatures; (2) if cadence-broken: compare the REAL adc_validIn duty on fabric vs the sim's 1-in-2 (ILA or a counter reg); (3) the sim's ingest assumption (1 sample per 2 clks) vs this image's actual rail = prime open question. Boards: 148 agcfix Rx-armed, 146 gather Tx-armed @2.0+trim.

## 2026-07-07 (late) REGISTER MAP DECODED + failure classified: carrier-sync acquire->reset LOOP
AXI map (TxRxCompo_ip_addr_decoder.v, addr_read[6:0] WORD addressing; byte = word*4): 0x140=cap_deint(0x50), 0x144=cap_out(0x51), 0x14C=cap_cad(0x53), 0x150=rstcs_count(0x54), 0x154=cfc_est(0x55). Failing-state reads: cap_out=0, cap_cad=0xFF0000FF, rstcs_count=12069 (HUGE - auto carrier-sync resets cycling), cfc_est=0x794=1940 raw on a ~0-CFO air signal. Classification: NOT cadence-broken (rstcs>0), not clean level-starved either (cfc not -15..-18k junk) => modem processes frames, AGC works (tap rms 4576), CFO fine, but carrier-sync fails some acquisition CHECK and auto-resets forever (12k cycles). Asked the agent (resumed): (1) locked-sim values/units of rstcs_count+cfc_est, (2) cap_cad=0xFF0000FF semantics, (3) reproduce the cycling by perturbing one runtime condition -> name the one parameter for lock.

## 2026-07-07 (final) T8 state: level+rate+cadence-class ALL verified equal sim-vs-hw; residual delta = ingest valid/enable PHASE detail
Agent round-2: locking sim reads rstcs=0/cfc_est<=tens raw (units sfix21_En21 of 1.92M rail); step-detector fires rstCS when consecutive cfc estimates differ >3277 raw (thr 0.0015625); cap_cad packs sCnt|gap<<8|snv<<16|vrun<<24 -> HW 0xFF0000FF = ingest+frame-start pipeline HEALTHY. Agent's level theory (armed rail rms 286 = tap4576/16 pinned) REFUTED by direct measurement: UNARMED rail @manual34 = rms 5798 (=split148 native level at which sim locks GOLDEN); armed tap 4625 (AGC ~0.8x attenuating, near-target, NOT pinned); BIST still cycles (rstcs=0x126 rising, cfc=0xFFFC380E large-neg junk). Rail structure: unique samples, NO duplication (dup=0.000; 6x profile reloads all 0.000). HISTORICAL CORRECTION: dup=0.5 captures were NEAR-END (tx-side) taps; the RX rail was always unique-2x; the modem decimates via its enb_1_2 cadence -> sees sps8. So: same stream structure, same level, same netlist as the GOLDEN sim -> hw cycles. REMAINING DELTA CANDIDATES (for next session): (1) adc_validIn/enable PHASE relative to the modem enb (the sim feeds fixed 1-per-2-clk skip=0; hw regularizer phase arbitrary -> if the v6 ingest is phase-sensitive, one phase locks, other doesn't -> would ALSO explain the cable-era sometimes-locked history!); test in sim: skip=1 phase + jittered valid patterns vs lock; if phase-sensitive -> RTL make ingest phase-agnostic OR add a phase toggle reg; (2) live CFO at 2.0GHz drifted (trim +5420 was hours ago; XO wanders +-few kHz over hours; modem pull-in maybe +-2k; cfc junk -0.1 normalized though = beyond); re-measure current CFO unarmed and re-trim before next BIST attempt; (3) rerun the same experiment @cable (physical) as the known-BIST-locking control for the agcfix image.
BOARDS: 148=agcfix af9dacf9 (Rx@2.0 manual34, armed), 146=gather753 Tx@2.0+5420 armed atten0. Agent artifacts: phaseB_agc/PHASEB_AGC_REPORT.txt, rtl_sim/{obj_agcbuild,obj_agcbuild2,wrap_agcbuild*,sim_phaseB2.cpp}. Working system unchanged: OTA both directions + FDD 0.000% via gonogo/fdd_gonogo (offline-gated).
